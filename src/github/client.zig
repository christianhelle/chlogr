// <auto-generated>
//   This code was generated by openapi2zig 0.5.5 (49b9095) on 2026-09-04 13:59:23 UTC
//   Changes to this file may cause incorrect behavior and will be lost if the code is regenerated
//   Checksum: 533b8c237e2e5f63
// </auto-generated>

const std = @import("std");
const models = @import("models.zig");
const runtime = @import("runtime.zig");
const Owned = runtime.Owned;
const HttpObserver = runtime.HttpObserver;
const RawResponse = runtime.RawResponse;
const ParseErrorResponse = runtime.ParseErrorResponse;
const ApiResult = runtime.ApiResult;
const CancellationToken = runtime.CancellationToken;
const checkCancellation = runtime.checkCancellation;
const CancelableReader = runtime.CancelableReader;
const parseSseReader = runtime.parseSseReader;
const parseSseBytes = runtime.parseSseBytes;
const parseSseBytesTyped = runtime.parseSseBytesTyped;
const parseSseReaderTyped = runtime.parseSseReaderTyped;
const TypedSseCallback = runtime.TypedSseCallback;

pub const Client = struct {
    allocator: std.mem.Allocator,
    io: std.Io,
    http: std.http.Client,
    api_key: []const u8,
    base_url: []const u8 = "",
    organization: ?[]const u8 = null,
    project: ?[]const u8 = null,
    default_headers: []const std.http.Header = &.{},
    http_observer: ?HttpObserver = null,

    /// Optional predicate polled while reading a streaming response body.
    /// When it returns true, a successfully started background watcher interrupts
    /// a blocked SSE socket read within about 10 ms and the stream exits with
    /// error.Cancelled. A CancelableReader also checks the predicate at every read
    /// boundary. If watcher startup fails, cancellation remains read-boundary only.
    /// Point it at an app-level cancel flag; it composes with a CancellationToken.
    /// When null (default), no watcher thread is spawned.
    cancel_check: ?*const fn () bool = null,

    pub fn init(allocator: std.mem.Allocator, io: std.Io, api_key: []const u8) Client {
        return .{
            .allocator = allocator,
            .io = io,
            .http = .{ .allocator = allocator, .io = io },
            .api_key = api_key,
            .http_observer = null,
        };
    }

    pub fn deinit(self: *Client) void {
        self.http.deinit();
    }

    pub fn withBaseUrl(self: *Client, base_url: []const u8) void {
        self.base_url = base_url;
    }
};

fn isQueryChar(c: u8) bool {
    return std.ascii.isAlphanumeric(c) or switch (c) {
        '-', '.', '_', '~' => true,
        else => false,
    };
}

fn writeQueryComponent(writer: *std.Io.Writer, value: []const u8) !void {
    try std.Uri.Component.percentEncode(writer, value, isQueryChar);
}

fn writeQueryValue(writer: *std.Io.Writer, value: anytype) !void {
    const T = @TypeOf(value);
    switch (@typeInfo(T)) {
        .pointer => |ptr| {
            if (ptr.size == .slice and ptr.child == u8) {
                try writeQueryComponent(writer, value);
            } else {
                try std.json.Stringify.value(value, .{}, writer);
            }
        },
        .int, .comptime_int, .float, .comptime_float, .bool => try writer.print("{}", .{value}),
        .@"enum" => try writeQueryComponent(writer, @tagName(value)),
        else => try std.json.Stringify.value(value, .{}, writer),
    }
}

fn appendQueryParam(writer: *std.Io.Writer, first_query: *bool, name: []const u8, value: anytype) !void {
    if (first_query.*) {
        try writer.writeByte('?');
        first_query.* = false;
    } else {
        try writer.writeByte('&');
    }
    try writeQueryComponent(writer, name);
    try writer.writeByte('=');
    try writeQueryValue(writer, value);
}

/// Serializes an `in: header` parameter value to an owned string. Reuses
/// the same primitive-type coverage as query parameters (string, integer,
/// float, boolean, enum). The caller frees the returned slice.
fn formatHeaderValue(allocator: std.mem.Allocator, value: anytype) ![]u8 {
    const T = @TypeOf(value);
    return switch (@typeInfo(T)) {
        .pointer => |ptr| blk: {
            if (ptr.size == .slice and ptr.child == u8) {
                break :blk try allocator.dupe(u8, value);
            }
            @compileError("OpenAPI header values must be strings, numbers, booleans, or enums");
        },
        .@"enum" => try allocator.dupe(u8, @tagName(value)),
        .int, .comptime_int, .float, .comptime_float, .bool => try std.fmt.allocPrint(allocator, "{}", .{value}),
        else => @compileError("OpenAPI header values must be strings, numbers, booleans, or enums"),
    };
}

/// Appends `value` under `name`, replacing (case-insensitively) any
/// existing header of the same name. Used so a declared operation header
/// parameter always wins over an equal-named client default header while
/// emitting exactly one header on the wire.
fn appendOrReplaceHeader(allocator: std.mem.Allocator, headers: *std.ArrayList(std.http.Header), name: []const u8, value: []const u8) !void {
    var i: usize = 0;
    while (i < headers.items.len) {
        if (std.ascii.eqlIgnoreCase(headers.items[i].name, name)) {
            _ = headers.orderedRemove(i);
        } else {
            i += 1;
        }
    }
    try headers.append(allocator, .{ .name = name, .value = value });
}

pub fn requestRaw(client: *Client, method: std.http.Method, url: []const u8, payload: ?[]const u8) !RawResponse {
    return requestRawWithContentType(client, method, url, payload, "application/json");
}

pub fn requestRawWithExtraHeaders(client: *Client, method: std.http.Method, url: []const u8, payload: ?[]const u8, extra_headers: []const std.http.Header) !RawResponse {
    return requestRawWithContentTypeAndExtraHeaders(client, method, url, payload, "application/json", extra_headers);
}

pub fn requestRawWithContentTypeAndExtraHeaders(client: *Client, method: std.http.Method, url: []const u8, payload: ?[]const u8, content_type_value: []const u8, extra_headers: []const std.http.Header) !RawResponse {
    const allocator = client.allocator;
    var headers = std.ArrayList(std.http.Header).empty;
    defer headers.deinit(allocator);
    const content_type: ?[]const u8 = if (payload != null) content_type_value else null;
    try appendClientHeaders(allocator, &headers, client, content_type, "application/json");
    for (extra_headers) |header| try appendOrReplaceHeader(allocator, &headers, header.name, header.value);

    if (client.http_observer) |obs| {
        if (obs.onRequest) |cb| cb(obs.ctx, method, url, headers.items, payload);
    }

    const uri = try std.Uri.parse(url);
    var response_body: std.Io.Writer.Allocating = .init(allocator);
    defer response_body.deinit();

    const start = std.Io.Clock.awake.now(client.io);
    const result = client.http.fetch(.{
        .location = .{ .uri = uri },
        .method = method,
        .extra_headers = headers.items,
        .payload = payload,
        .response_writer = &response_body.writer,
    }) catch |err| {
        if (client.http_observer) |obs| {
            if (obs.onError) |cb| cb(obs.ctx, method, url, @errorName(err));
        }
        return err;
    };
    const elapsed_ns = @as(u64, @intCast(start.untilNow(client.io, .awake).nanoseconds));

    const body = try response_body.toOwnedSlice();

    if (client.http_observer) |obs| {
        if (obs.onResponse) |cb| cb(obs.ctx, method, url, result.status, &.{}, body, elapsed_ns);
    }

    return .{
        .allocator = allocator,
        .status = result.status,
        .body = body,
    };
}

pub fn requestRawWithContentType(client: *Client, method: std.http.Method, url: []const u8, payload: ?[]const u8, content_type_value: []const u8) !RawResponse {
    const allocator = client.allocator;
    var headers = std.ArrayList(std.http.Header).empty;
    defer headers.deinit(allocator);
    const content_type: ?[]const u8 = if (payload != null) content_type_value else null;
    try appendClientHeaders(allocator, &headers, client, content_type, "application/json");

    if (client.http_observer) |obs| {
        if (obs.onRequest) |cb| cb(obs.ctx, method, url, headers.items, payload);
    }

    const uri = try std.Uri.parse(url);
    var response_body: std.Io.Writer.Allocating = .init(allocator);
    defer response_body.deinit();

    const start = std.Io.Clock.awake.now(client.io);
    const result = client.http.fetch(.{
        .location = .{ .uri = uri },
        .method = method,
        .extra_headers = headers.items,
        .payload = payload,
        .response_writer = &response_body.writer,
    }) catch |err| {
        if (client.http_observer) |obs| {
            if (obs.onError) |cb| cb(obs.ctx, method, url, @errorName(err));
        }
        return err;
    };
    const elapsed_ns = @as(u64, @intCast(start.untilNow(client.io, .awake).nanoseconds));

    const body = try response_body.toOwnedSlice();

    if (client.http_observer) |obs| {
        if (obs.onResponse) |cb| cb(obs.ctx, method, url, result.status, &.{}, body, elapsed_ns);
    }

    return .{
        .allocator = allocator,
        .status = result.status,
        .body = body,
    };
}

pub fn getRaw(client: *Client, path: []const u8) !RawResponse {
    const url = try std.fmt.allocPrint(client.allocator, "{s}{s}", .{ client.base_url, path });
    defer client.allocator.free(url);
    return requestRaw(client, .GET, url, null);
}

pub fn postJsonRaw(client: *Client, path: []const u8, payload: anytype) !RawResponse {
    const allocator = client.allocator;
    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(payload, .{ .emit_null_optional_fields = false }, &str.writer);

    const url = try std.fmt.allocPrint(allocator, "{s}{s}", .{ client.base_url, path });
    defer allocator.free(url);
    return requestRaw(client, .POST, url, str.written());
}

pub fn parseRawResponse(comptime T: type, raw: RawResponse) !ApiResult(T) {
    if (raw.status.class() != .success) return .{ .api_error = raw };
    const parsed = std.json.parseFromSlice(T, raw.allocator, raw.body, .{ .ignore_unknown_fields = true }) catch |err| {
        return .{ .parse_error = .{ .raw = raw, .error_name = @errorName(err) } };
    };
    return .{ .ok = .{ .allocator = raw.allocator, .body = raw.body, .parsed = parsed } };
}

pub fn getJsonResult(comptime T: type, client: *Client, path: []const u8) !ApiResult(T) {
    return parseRawResponse(T, try getRaw(client, path));
}

pub fn postJsonResult(comptime T: type, client: *Client, path: []const u8, payload: anytype) !ApiResult(T) {
    return parseRawResponse(T, try postJsonRaw(client, path, payload));
}
fn appendClientHeaders(allocator: std.mem.Allocator, headers: *std.ArrayList(std.http.Header), client: *Client, content_type: ?[]const u8, accept: []const u8) !void {
    if (content_type) |ct| {
        try headers.append(allocator, .{ .name = "Content-Type", .value = ct });
    }
    try headers.append(allocator, .{ .name = "Accept", .value = accept });
    if (client.organization) |organization| {
        try headers.append(allocator, .{ .name = "OpenAI-Organization", .value = organization });
    }
    if (client.project) |project| {
        try headers.append(allocator, .{ .name = "OpenAI-Project", .value = project });
    }
    for (client.default_headers) |header| {
        try headers.append(allocator, header);
    }
}

/////////////////
// Summary:
// Check if a pull request has been merged
//
// Description:
// Checks if a pull request has been merged into the base branch. The HTTP status of the response indicates whether or not the pull request has been merged; the response body is empty.
//
pub fn pullsCheckIfMerged(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64) !void {
    var raw = try pullsCheckIfMergedRaw(client, owner, repo, pull_number);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn pullsCheckIfMergedRaw(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pulls/{d}/merge", .{ client.base_url, owner, repo, pull_number });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Merge a pull request
//
// Description:
// Merges a pull request into the base branch.
// This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating content too quickly using this endpoint may result in secondary rate limiting. For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)" and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
//
pub fn pullsMerge(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !Owned(models.@"pull-request-merge-result") {
    var result = try pullsMergeResult(client, owner, repo, pull_number, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullsMergeRaw(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pulls/{d}/merge", .{ client.base_url, owner, repo, pull_number });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PUT, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullsMergeResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !ApiResult(models.@"pull-request-merge-result") {
    return parseRawResponse(models.@"pull-request-merge-result", try pullsMergeRaw(client, owner, repo, pull_number, requestBody));
}

/////////////////
// Summary:
// Get a deployment branch policy
//
// Description:
// Gets a deployment branch or tag policy for an environment.
//
// Anyone with read access to the repository can use this endpoint.
//
// OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
//
pub fn reposGetDeploymentBranchPolicy(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, branch_policy_id: i64) !Owned(models.@"deployment-branch-policy") {
    var result = try reposGetDeploymentBranchPolicyResult(client, owner, repo, environment_name, branch_policy_id);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetDeploymentBranchPolicyRaw(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, branch_policy_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/environments/{s}/deployment-branch-policies/{d}", .{ client.base_url, owner, repo, environment_name, branch_policy_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetDeploymentBranchPolicyResult(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, branch_policy_id: i64) !ApiResult(models.@"deployment-branch-policy") {
    return parseRawResponse(models.@"deployment-branch-policy", try reposGetDeploymentBranchPolicyRaw(client, owner, repo, environment_name, branch_policy_id));
}

/////////////////
// Summary:
// Update a deployment branch policy
//
// Description:
// Updates a deployment branch or tag policy for an environment.
//
// OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
//
pub fn reposUpdateDeploymentBranchPolicy(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, branch_policy_id: i64, requestBody: models.@"deployment-branch-policy-name-pattern") !Owned(models.@"deployment-branch-policy") {
    var result = try reposUpdateDeploymentBranchPolicyResult(client, owner, repo, environment_name, branch_policy_id, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposUpdateDeploymentBranchPolicyRaw(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, branch_policy_id: i64, requestBody: models.@"deployment-branch-policy-name-pattern") !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/environments/{s}/deployment-branch-policies/{d}", .{ client.base_url, owner, repo, environment_name, branch_policy_id });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PUT, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposUpdateDeploymentBranchPolicyResult(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, branch_policy_id: i64, requestBody: models.@"deployment-branch-policy-name-pattern") !ApiResult(models.@"deployment-branch-policy") {
    return parseRawResponse(models.@"deployment-branch-policy", try reposUpdateDeploymentBranchPolicyRaw(client, owner, repo, environment_name, branch_policy_id, requestBody));
}

/////////////////
// Summary:
// Delete a deployment branch policy
//
// Description:
// Deletes a deployment branch or tag policy for an environment.
//
// OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
//
pub fn reposDeleteDeploymentBranchPolicy(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, branch_policy_id: i64) !void {
    var raw = try reposDeleteDeploymentBranchPolicyRaw(client, owner, repo, environment_name, branch_policy_id);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposDeleteDeploymentBranchPolicyRaw(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, branch_policy_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/environments/{s}/deployment-branch-policies/{d}", .{ client.base_url, owner, repo, environment_name, branch_policy_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// List review comments in a repository
//
// Description:
// Lists review comments for all pull requests in a repository. By default,
// review comments are in ascending order by ID.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn pullsListReviewCommentsForRepo(client: *Client, owner: []const u8, repo: []const u8, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try pullsListReviewCommentsForRepoResult(client, owner, repo, sort, direction, since, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullsListReviewCommentsForRepoRaw(client: *Client, owner: []const u8, repo: []const u8, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pulls/comments", .{ client.base_url, owner, repo });
    var first_query = true;
    if (sort) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "sort", value);
    }
    if (direction) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "direction", value);
    }
    if (since) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "since", value);
    }
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullsListReviewCommentsForRepoResult(client: *Client, owner: []const u8, repo: []const u8, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try pullsListReviewCommentsForRepoRaw(client, owner, repo, sort, direction, since, per_page, page));
}

/////////////////
// Summary:
// List pull request stacks
//
// Description:
// Lists pull request stacks in a repository.
//
pub fn pullRequestStacksList(client: *Client, owner: []const u8, repo: []const u8, pull_request: ?i64, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try pullRequestStacksListResult(client, owner, repo, pull_request, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullRequestStacksListRaw(client: *Client, owner: []const u8, repo: []const u8, pull_request: ?i64, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/stacks", .{ client.base_url, owner, repo });
    var first_query = true;
    if (pull_request) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "pull_request", value);
    }
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullRequestStacksListResult(client: *Client, owner: []const u8, repo: []const u8, pull_request: ?i64, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try pullRequestStacksListRaw(client, owner, repo, pull_request, per_page, page));
}

/////////////////
// Summary:
// Create a pull request stack
//
// Description:
// Creates a stack from an ordered list of pull request numbers. Provide the pull
// request numbers from the bottom of the stack to the top. Each pull request's
// base ref must match the previous pull request's head ref.
//
pub fn pullRequestStacksCreate(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(std.json.Value) {
    var result = try pullRequestStacksCreateResult(client, owner, repo, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullRequestStacksCreateRaw(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/stacks", .{ client.base_url, owner, repo });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullRequestStacksCreateResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(std.json.Value) {
    return parseRawResponse(std.json.Value, try pullRequestStacksCreateRaw(client, owner, repo, requestBody));
}

/////////////////
// Summary:
// Get a repository rule suite
//
// Description:
// Gets information about a suite of rule evaluations from within a repository.
// For more information, see "[Managing rulesets for a repository](https://docs.github.com/repositories/configuring-branches-and-merges-in-your-repository/managing-rulesets/managing-rulesets-for-a-repository#viewing-insights-for-rulesets)."
//
pub fn reposGetRepoRuleSuite(client: *Client, owner: []const u8, repo: []const u8, rule_suite_id: i64) !Owned(models.@"rule-suite") {
    var result = try reposGetRepoRuleSuiteResult(client, owner, repo, rule_suite_id);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetRepoRuleSuiteRaw(client: *Client, owner: []const u8, repo: []const u8, rule_suite_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/rulesets/rule-suites/{d}", .{ client.base_url, owner, repo, rule_suite_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetRepoRuleSuiteResult(client: *Client, owner: []const u8, repo: []const u8, rule_suite_id: i64) !ApiResult(models.@"rule-suite") {
    return parseRawResponse(models.@"rule-suite", try reposGetRepoRuleSuiteRaw(client, owner, repo, rule_suite_id));
}

/////////////////
// Summary:
// Get pull request review protection
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
pub fn reposGetPullRequestReviewProtection(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !Owned(models.@"protected-branch-pull-request-review") {
    var result = try reposGetPullRequestReviewProtectionResult(client, owner, repo, branch);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetPullRequestReviewProtectionRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection/required_pull_request_reviews", .{ client.base_url, owner, repo, branch });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetPullRequestReviewProtectionResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !ApiResult(models.@"protected-branch-pull-request-review") {
    return parseRawResponse(models.@"protected-branch-pull-request-review", try reposGetPullRequestReviewProtectionRaw(client, owner, repo, branch));
}

/////////////////
// Summary:
// Delete pull request review protection
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
pub fn reposDeletePullRequestReviewProtection(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !void {
    var raw = try reposDeletePullRequestReviewProtectionRaw(client, owner, repo, branch);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposDeletePullRequestReviewProtectionRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection/required_pull_request_reviews", .{ client.base_url, owner, repo, branch });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Update pull request review protection
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
// Updating pull request review enforcement requires admin or owner permissions to the repository and branch protection to be enabled.
//
// > [!NOTE]
// > Passing new arrays of `users` and `teams` replaces their previous values.
//
pub fn reposUpdatePullRequestReviewProtection(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned(models.@"protected-branch-pull-request-review") {
    var result = try reposUpdatePullRequestReviewProtectionResult(client, owner, repo, branch, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposUpdatePullRequestReviewProtectionRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection/required_pull_request_reviews", .{ client.base_url, owner, repo, branch });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PATCH, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposUpdatePullRequestReviewProtectionResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult(models.@"protected-branch-pull-request-review") {
    return parseRawResponse(models.@"protected-branch-pull-request-review", try reposUpdatePullRequestReviewProtectionRaw(client, owner, repo, branch, requestBody));
}

/////////////////
// Summary:
// Dismiss a review for a pull request
//
// Description:
// Dismisses a specified review on a pull request.
//
// > [!NOTE]
// > To dismiss a pull request review on a [protected branch](https://docs.github.com/rest/branches/branch-protection), you must be a repository administrator or be included in the list of people or teams who can dismiss pull request reviews.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn pullsDismissReview(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, review_id: i64, requestBody: std.json.Value) !Owned(models.@"pull-request-review") {
    var result = try pullsDismissReviewResult(client, owner, repo, pull_number, review_id, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullsDismissReviewRaw(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, review_id: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pulls/{d}/reviews/{d}/dismissals", .{ client.base_url, owner, repo, pull_number, review_id });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PUT, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullsDismissReviewResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, review_id: i64, requestBody: std.json.Value) !ApiResult(models.@"pull-request-review") {
    return parseRawResponse(models.@"pull-request-review", try pullsDismissReviewRaw(client, owner, repo, pull_number, review_id, requestBody));
}

/////////////////
// Summary:
// Get admin branch protection
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
pub fn reposGetAdminBranchProtection(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !Owned(models.@"protected-branch-admin-enforced") {
    var result = try reposGetAdminBranchProtectionResult(client, owner, repo, branch);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetAdminBranchProtectionRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection/enforce_admins", .{ client.base_url, owner, repo, branch });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetAdminBranchProtectionResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !ApiResult(models.@"protected-branch-admin-enforced") {
    return parseRawResponse(models.@"protected-branch-admin-enforced", try reposGetAdminBranchProtectionRaw(client, owner, repo, branch));
}

/////////////////
// Summary:
// Set admin branch protection
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
// Adding admin enforcement requires admin or owner permissions to the repository and branch protection to be enabled.
//
pub fn reposSetAdminBranchProtection(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !Owned(models.@"protected-branch-admin-enforced") {
    var result = try reposSetAdminBranchProtectionResult(client, owner, repo, branch);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposSetAdminBranchProtectionRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection/enforce_admins", .{ client.base_url, owner, repo, branch });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposSetAdminBranchProtectionResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !ApiResult(models.@"protected-branch-admin-enforced") {
    return parseRawResponse(models.@"protected-branch-admin-enforced", try reposSetAdminBranchProtectionRaw(client, owner, repo, branch));
}

/////////////////
// Summary:
// Delete admin branch protection
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
// Removing admin enforcement requires admin or owner permissions to the repository and branch protection to be enabled.
//
pub fn reposDeleteAdminBranchProtection(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !void {
    var raw = try reposDeleteAdminBranchProtectionRaw(client, owner, repo, branch);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposDeleteAdminBranchProtectionRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection/enforce_admins", .{ client.base_url, owner, repo, branch });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Get top referral paths
//
// Description:
// Get the top 10 popular contents over the last 14 days.
//
pub fn reposGetTopPaths(client: *Client, owner: []const u8, repo: []const u8) !Owned([]const std.json.Value) {
    var result = try reposGetTopPathsResult(client, owner, repo);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetTopPathsRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/traffic/popular/paths", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetTopPathsResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposGetTopPathsRaw(client, owner, repo));
}

/////////////////
// Summary:
// Get page views
//
// Description:
// Get the total number of views and breakdown per day or week for the last 14 days. Timestamps are aligned to UTC midnight of the beginning of the day or week. Week begins on Monday.
//
pub fn reposGetViews(client: *Client, owner: []const u8, repo: []const u8, per: ?[]const u8) !Owned(models.@"view-traffic") {
    var result = try reposGetViewsResult(client, owner, repo, per);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetViewsRaw(client: *Client, owner: []const u8, repo: []const u8, per: ?[]const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/traffic/views", .{ client.base_url, owner, repo });
    var first_query = true;
    if (per) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetViewsResult(client: *Client, owner: []const u8, repo: []const u8, per: ?[]const u8) !ApiResult(models.@"view-traffic") {
    return parseRawResponse(models.@"view-traffic", try reposGetViewsRaw(client, owner, repo, per));
}

/////////////////
// Summary:
// List user account issues assigned to the authenticated user
//
// Description:
// List issues across owned and member repositories assigned to the authenticated user.
//
// > [!NOTE]
// > GitHub's REST API considers every pull request an issue, but not every issue is a pull request. For this reason, "Issues" endpoints may return both issues and pull requests in the response. You can identify pull requests by the `pull_request` key. Be aware that the `id` of a pull request returned from "Issues" endpoints will be an _issue id_. To find out the pull request id, use the "[List pull requests](https://docs.github.com/rest/pulls/pulls#list-pull-requests)" endpoint.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn issuesListForAuthenticatedUser(client: *Client, filter: ?[]const u8, state: ?[]const u8, labels: ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try issuesListForAuthenticatedUserResult(client, filter, state, labels, sort, direction, since, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesListForAuthenticatedUserRaw(client: *Client, filter: ?[]const u8, state: ?[]const u8, labels: ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/user/issues", .{client.base_url});
    var first_query = true;
    if (filter) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "filter", value);
    }
    if (state) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "state", value);
    }
    if (labels) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "labels", value);
    }
    if (sort) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "sort", value);
    }
    if (direction) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "direction", value);
    }
    if (since) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "since", value);
    }
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesListForAuthenticatedUserResult(client: *Client, filter: ?[]const u8, state: ?[]const u8, labels: ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try issuesListForAuthenticatedUserRaw(client, filter, state, labels, sort, direction, since, per_page, page));
}

/////////////////
// Summary:
// Get teams with access to the protected branch
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
// Lists the teams who have push access to this branch. The list includes child teams.
//
pub fn reposGetTeamsWithAccessToProtectedBranch(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !Owned([]const std.json.Value) {
    var result = try reposGetTeamsWithAccessToProtectedBranchResult(client, owner, repo, branch);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetTeamsWithAccessToProtectedBranchRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection/restrictions/teams", .{ client.base_url, owner, repo, branch });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetTeamsWithAccessToProtectedBranchResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposGetTeamsWithAccessToProtectedBranchRaw(client, owner, repo, branch));
}

/////////////////
// Summary:
// Add team access restrictions
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
// Grants the specified teams push access for this branch. You can also give push access to child teams.
//
pub fn reposAddTeamAccessRestrictions(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned([]const std.json.Value) {
    var result = try reposAddTeamAccessRestrictionsResult(client, owner, repo, branch, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposAddTeamAccessRestrictionsRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection/restrictions/teams", .{ client.base_url, owner, repo, branch });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposAddTeamAccessRestrictionsResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposAddTeamAccessRestrictionsRaw(client, owner, repo, branch, requestBody));
}

/////////////////
// Summary:
// Set team access restrictions
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
// Replaces the list of teams that have push access to this branch. This removes all teams that previously had push access and grants push access to the new list of teams. Team restrictions include child teams.
//
pub fn reposSetTeamAccessRestrictions(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned([]const std.json.Value) {
    var result = try reposSetTeamAccessRestrictionsResult(client, owner, repo, branch, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposSetTeamAccessRestrictionsRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection/restrictions/teams", .{ client.base_url, owner, repo, branch });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PUT, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposSetTeamAccessRestrictionsResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposSetTeamAccessRestrictionsRaw(client, owner, repo, branch, requestBody));
}

/////////////////
// Summary:
// Remove team access restrictions
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
// Removes the ability of a team to push to this branch. You can also remove push access for child teams.
//
pub fn reposRemoveTeamAccessRestrictions(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned([]const std.json.Value) {
    var result = try reposRemoveTeamAccessRestrictionsResult(client, owner, repo, branch, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposRemoveTeamAccessRestrictionsRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection/restrictions/teams", .{ client.base_url, owner, repo, branch });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposRemoveTeamAccessRestrictionsResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposRemoveTeamAccessRestrictionsRaw(client, owner, repo, branch, requestBody));
}

/////////////////
// Summary:
// List repository tags
//
// Description:
//
//
pub fn reposListTags(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try reposListTagsResult(client, owner, repo, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListTagsRaw(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/tags", .{ client.base_url, owner, repo });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListTagsResult(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposListTagsRaw(client, owner, repo, per_page, page));
}

/////////////////
// Summary:
// Get a deployment
//
// Description:
//
//
pub fn reposGetDeployment(client: *Client, owner: []const u8, repo: []const u8, deployment_id: i64) !Owned(models.deployment) {
    var result = try reposGetDeploymentResult(client, owner, repo, deployment_id);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetDeploymentRaw(client: *Client, owner: []const u8, repo: []const u8, deployment_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/deployments/{d}", .{ client.base_url, owner, repo, deployment_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetDeploymentResult(client: *Client, owner: []const u8, repo: []const u8, deployment_id: i64) !ApiResult(models.deployment) {
    return parseRawResponse(models.deployment, try reposGetDeploymentRaw(client, owner, repo, deployment_id));
}

/////////////////
// Summary:
// Delete a deployment
//
// Description:
// If the repository only has one deployment, you can delete the deployment regardless of its status. If the repository has more than one deployment, you can only delete inactive deployments. This ensures that repositories with multiple deployments will always have an active deployment.
//
// To set a deployment as inactive, you must:
//
// *   Create a new deployment that is active so that the system has a record of the current state, then delete the previously active deployment.
// *   Mark the active deployment as inactive by adding any non-successful deployment status.
//
// For more information, see "[Create a deployment](https://docs.github.com/rest/deployments/deployments/#create-a-deployment)" and "[Create a deployment status](https://docs.github.com/rest/deployments/statuses#create-a-deployment-status)."
//
// OAuth app tokens and personal access tokens (classic) need the `repo` or `repo_deployment` scope to use this endpoint.
//
pub fn reposDeleteDeployment(client: *Client, owner: []const u8, repo: []const u8, deployment_id: i64) !void {
    var raw = try reposDeleteDeploymentRaw(client, owner, repo, deployment_id);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposDeleteDeploymentRaw(client: *Client, owner: []const u8, repo: []const u8, deployment_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/deployments/{d}", .{ client.base_url, owner, repo, deployment_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Get a repository
//
// Description:
// The `parent` and `source` objects are present when the repository is a fork. `parent` is the repository this repository was forked from, `source` is the ultimate source for the network.
//
// > [!NOTE]
// > - In order to see the `security_and_analysis` block for a repository you must have admin permissions for the repository or be an owner or security manager for the organization that owns the repository. For more information, see "[Managing security managers in your organization](https://docs.github.com/organizations/managing-peoples-access-to-your-organization-with-roles/managing-security-managers-in-your-organization)."
// > - To view merge-related settings, you must have the `contents:read` and `contents:write` permissions.
//
pub fn reposGet(client: *Client, owner: []const u8, repo: []const u8) !Owned(models.@"full-repository") {
    var result = try reposGetResult(client, owner, repo);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult(models.@"full-repository") {
    return parseRawResponse(models.@"full-repository", try reposGetRaw(client, owner, repo));
}

/////////////////
// Summary:
// Delete a repository
//
// Description:
// Deleting a repository requires admin access.
//
// If an organization owner has configured the organization to prevent members from deleting organization-owned
// repositories, you will get a `403 Forbidden` response.
//
// OAuth app tokens and personal access tokens (classic) need the `delete_repo` scope to use this endpoint.
//
pub fn reposDelete(client: *Client, owner: []const u8, repo: []const u8) !void {
    var raw = try reposDeleteRaw(client, owner, repo);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposDeleteRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Update a repository
//
// Description:
// **Note**: To edit a repository's topics, use the [Replace all repository topics](https://docs.github.com/rest/repos/repos#replace-all-repository-topics) endpoint.
//
pub fn reposUpdate(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.@"full-repository") {
    var result = try reposUpdateResult(client, owner, repo, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposUpdateRaw(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}", .{ client.base_url, owner, repo });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PATCH, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposUpdateResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.@"full-repository") {
    return parseRawResponse(models.@"full-repository", try reposUpdateRaw(client, owner, repo, requestBody));
}

/////////////////
// Summary:
// List repository teams
//
// Description:
// Lists the teams that have access to the specified repository and that are also visible to the authenticated user.
//
// For a public repository, a team is listed only if that team added the public repository explicitly.
//
// OAuth app tokens and personal access tokens (classic) need the `public_repo` or `repo` scope to use this endpoint with a public repository, and `repo` scope to use this endpoint with a private repository.
//
pub fn reposListTeams(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try reposListTeamsResult(client, owner, repo, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListTeamsRaw(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/teams", .{ client.base_url, owner, repo });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListTeamsResult(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposListTeamsRaw(client, owner, repo, per_page, page));
}

/////////////////
// Summary:
// List repository activities
//
// Description:
// Lists a detailed history of changes to a repository, such as pushes, merges, force pushes, and branch changes, and associates these changes with commits and users.
//
// For more information about viewing repository activity,
// see "[Viewing activity and data for your repository](https://docs.github.com/repositories/viewing-activity-and-data-for-your-repository)."
//
pub fn reposListActivities(client: *Client, owner: []const u8, repo: []const u8, direction: ?[]const u8, per_page: ?i64, before: ?[]const u8, after: ?[]const u8, ref: ?[]const u8, actor: ?[]const u8, time_period: ?[]const u8, activity_type: ?[]const u8) !Owned([]const std.json.Value) {
    var result = try reposListActivitiesResult(client, owner, repo, direction, per_page, before, after, ref, actor, time_period, activity_type);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListActivitiesRaw(client: *Client, owner: []const u8, repo: []const u8, direction: ?[]const u8, per_page: ?i64, before: ?[]const u8, after: ?[]const u8, ref: ?[]const u8, actor: ?[]const u8, time_period: ?[]const u8, activity_type: ?[]const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/activity", .{ client.base_url, owner, repo });
    var first_query = true;
    if (direction) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "direction", value);
    }
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (before) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "before", value);
    }
    if (after) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "after", value);
    }
    if (ref) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "ref", value);
    }
    if (actor) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "actor", value);
    }
    if (time_period) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "time_period", value);
    }
    if (activity_type) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "activity_type", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListActivitiesResult(client: *Client, owner: []const u8, repo: []const u8, direction: ?[]const u8, per_page: ?i64, before: ?[]const u8, after: ?[]const u8, ref: ?[]const u8, actor: ?[]const u8, time_period: ?[]const u8, activity_type: ?[]const u8) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposListActivitiesRaw(client, owner, repo, direction, per_page, before, after, ref, actor, time_period, activity_type));
}

/////////////////
// Summary:
// List branches for HEAD commit
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
// Returns all branches where the given commit SHA is the HEAD, or latest commit for the branch.
//
pub fn reposListBranchesForHeadCommit(client: *Client, owner: []const u8, repo: []const u8, commit_sha: []const u8) !Owned([]const std.json.Value) {
    var result = try reposListBranchesForHeadCommitResult(client, owner, repo, commit_sha);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListBranchesForHeadCommitRaw(client: *Client, owner: []const u8, repo: []const u8, commit_sha: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/commits/{s}/branches-where-head", .{ client.base_url, owner, repo, commit_sha });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListBranchesForHeadCommitResult(client: *Client, owner: []const u8, repo: []const u8, commit_sha: []const u8) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposListBranchesForHeadCommitRaw(client, owner, repo, commit_sha));
}

/////////////////
// Summary:
// List deployments
//
// Description:
// Simple filtering of deployments is available via query parameters:
//
pub fn reposListDeployments(client: *Client, owner: []const u8, repo: []const u8, sha: ?[]const u8, ref: ?[]const u8, task: ?[]const u8, environment: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try reposListDeploymentsResult(client, owner, repo, sha, ref, task, environment, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListDeploymentsRaw(client: *Client, owner: []const u8, repo: []const u8, sha: ?[]const u8, ref: ?[]const u8, task: ?[]const u8, environment: ?[]const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/deployments", .{ client.base_url, owner, repo });
    var first_query = true;
    if (sha) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "sha", value);
    }
    if (ref) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "ref", value);
    }
    if (task) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "task", value);
    }
    if (environment) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "environment", value);
    }
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListDeploymentsResult(client: *Client, owner: []const u8, repo: []const u8, sha: ?[]const u8, ref: ?[]const u8, task: ?[]const u8, environment: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposListDeploymentsRaw(client, owner, repo, sha, ref, task, environment, per_page, page));
}

/////////////////
// Summary:
// Create a deployment
//
// Description:
// Deployments offer a few configurable parameters with certain defaults.
//
// The `ref` parameter can be any named branch, tag, or SHA. At GitHub we often deploy branches and verify them
// before we merge a pull request.
//
// The `environment` parameter allows deployments to be issued to different runtime environments. Teams often have
// multiple environments for verifying their applications, such as `production`, `staging`, and `qa`. This parameter
// makes it easier to track which environments have requested deployments. The default environment is `production`.
//
// The `auto_merge` parameter is used to ensure that the requested ref is not behind the repository's default branch. If
// the ref _is_ behind the default branch for the repository, we will attempt to merge it for you. If the merge succeeds,
// the API will return a successful merge commit. If merge conflicts prevent the merge from succeeding, the API will
// return a failure response.
//
// By default, [commit statuses](https://docs.github.com/rest/commits/statuses) for every submitted context must be in a `success`
// state. The `required_contexts` parameter allows you to specify a subset of contexts that must be `success`, or to
// specify contexts that have not yet been submitted. You are not required to use commit statuses to deploy. If you do
// not require any contexts or create any commit statuses, the deployment will always succeed.
//
// The `payload` parameter is available for any extra information that a deployment system might need. It is a JSON text
// field that will be passed on when a deployment event is dispatched.
//
// The `task` parameter is used by the deployment system to allow different execution paths. In the web world this might
// be `deploy:migrations` to run schema changes on the system. In the compiled world this could be a flag to compile an
// application with debugging enabled.
//
// Merged branch response:
//
// You will see this response when GitHub automatically merges the base branch into the topic branch instead of creating
// a deployment. This auto-merge happens when:
// *   Auto-merge option is enabled in the repository
// *   Topic branch does not include the latest changes on the base branch, which is `master` in the response example
// *   There are no merge conflicts
//
// If there are no new commits in the base branch, a new request to create a deployment should give a successful
// response.
//
// Merge conflict response:
//
// This error happens when the `auto_merge` option is enabled and when the default branch (in this case `master`), can't
// be merged into the branch that's being deployed (in this case `topic-branch`), due to merge conflicts.
//
// Failed commit status checks:
//
// This error happens when the `required_contexts` parameter indicates that one or more contexts need to have a `success`
// status for the commit to be deployed, but one or more of the required contexts do not have a state of `success`.
//
// OAuth app tokens and personal access tokens (classic) need the `repo` or `repo_deployment` scope to use this endpoint.
//
pub fn reposCreateDeployment(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.deployment) {
    var result = try reposCreateDeploymentResult(client, owner, repo, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposCreateDeploymentRaw(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/deployments", .{ client.base_url, owner, repo });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposCreateDeploymentResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.deployment) {
    return parseRawResponse(models.deployment, try reposCreateDeploymentRaw(client, owner, repo, requestBody));
}

/////////////////
// Summary:
// List sub-issues
//
// Description:
// You can use the REST API to list the sub-issues on an issue.
//
// This endpoint supports the following custom media types. For more information, see [Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types).
//
// - **`application/vnd.github.raw+json`**: Returns the raw Markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github.text+json`**: Returns a text only representation of the Markdown body. Response will include `body_text`.
// - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's Markdown. Response will include `body_html`.
// - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn issuesListSubIssues(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try issuesListSubIssuesResult(client, owner, repo, issue_number, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesListSubIssuesRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}/sub_issues", .{ client.base_url, owner, repo, issue_number });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesListSubIssuesResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try issuesListSubIssuesRaw(client, owner, repo, issue_number, per_page, page));
}

/////////////////
// Summary:
// Add sub-issue
//
// Description:
// You can use the REST API to add sub-issues to issues.
//
// Creating content too quickly using this endpoint may result in secondary rate limiting.
// For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)"
// and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn issuesAddSubIssue(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !Owned(models.issue) {
    var result = try issuesAddSubIssueResult(client, owner, repo, issue_number, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesAddSubIssueRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}/sub_issues", .{ client.base_url, owner, repo, issue_number });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesAddSubIssueResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !ApiResult(models.issue) {
    return parseRawResponse(models.issue, try issuesAddSubIssueRaw(client, owner, repo, issue_number, requestBody));
}

/////////////////
// Summary:
// Get the hourly commit count for each day
//
// Description:
// Each array contains the day number, hour number, and number of commits:
//
// *   `0-6`: Sunday - Saturday
// *   `0-23`: Hour of day
// *   Number of commits
//
// For example, `[2, 14, 25]` indicates that there were 25 total commits, during the 2:00pm hour on Tuesdays. All times are based on the time zone of individual commits.
//
pub fn reposGetPunchCardStats(client: *Client, owner: []const u8, repo: []const u8) !Owned([]const std.json.Value) {
    var result = try reposGetPunchCardStatsResult(client, owner, repo);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetPunchCardStatsRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/stats/punch_card", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetPunchCardStatsResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposGetPunchCardStatsRaw(client, owner, repo));
}

/////////////////
// Summary:
// List dependencies an issue is blocking
//
// Description:
// You can use the REST API to list the dependencies an issue is blocking.
//
// This endpoint supports the following custom media types. For more information, see [Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types).
//
// - **`application/vnd.github.raw+json`**: Returns the raw Markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github.text+json`**: Returns a text only representation of the Markdown body. Response will include `body_text`.
// - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's Markdown. Response will include `body_html`.
// - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn issuesListDependenciesBlocking(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try issuesListDependenciesBlockingResult(client, owner, repo, issue_number, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesListDependenciesBlockingRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}/dependencies/blocking", .{ client.base_url, owner, repo, issue_number });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesListDependenciesBlockingResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try issuesListDependenciesBlockingRaw(client, owner, repo, issue_number, per_page, page));
}

/////////////////
// Summary:
// Approve an issue suggestion
//
// Description:
// Approves a pending suggestion on an issue. Applies the proposed change (creating the corresponding timeline event), transitions the suggestion to `approved`, and dismisses any competing pending suggestions for the same change.
//
// Requires triage access to the repository. Approving a suggestion also requires permission to perform the change it applies (for example, setting the issue type, adding a label or assignee, or closing the issue); this only affects fine-grained access tokens and GitHub Apps whose permissions are narrower than the triage role. This endpoint only supports issues, not pull requests.
//
pub fn issuesApproveSuggestion(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, suggestion_id: i64) !Owned(models.@"issue-suggestion") {
    var result = try issuesApproveSuggestionResult(client, owner, repo, issue_number, suggestion_id);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesApproveSuggestionRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, suggestion_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}/suggestions/{d}/approve", .{ client.base_url, owner, repo, issue_number, suggestion_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesApproveSuggestionResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, suggestion_id: i64) !ApiResult(models.@"issue-suggestion") {
    return parseRawResponse(models.@"issue-suggestion", try issuesApproveSuggestionRaw(client, owner, repo, issue_number, suggestion_id));
}

/////////////////
// Summary:
// Get repository permissions for a user
//
// Description:
// Checks the repository permission and role of a collaborator.
//
// The `permission` attribute provides the legacy base roles of `admin`, `write`, `read`, and `none`, where the
// `maintain` role is mapped to `write` and the `triage` role is mapped to `read`.
// The `role_name` attribute provides the name of the assigned role, including custom roles. The
// `permission` can also be used to determine which base level of access the collaborator has to the repository.
//
// The calculated permissions are the highest role assigned to the collaborator after considering all sources of grants, including: repo, teams, organization, and enterprise.
// There is presently not a way to differentiate between an organization level grant and a repository level grant from this endpoint response.
//
pub fn reposGetCollaboratorPermissionLevel(client: *Client, owner: []const u8, repo: []const u8, username: []const u8) !Owned(models.@"repository-collaborator-permission") {
    var result = try reposGetCollaboratorPermissionLevelResult(client, owner, repo, username);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetCollaboratorPermissionLevelRaw(client: *Client, owner: []const u8, repo: []const u8, username: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/collaborators/{s}/permission", .{ client.base_url, owner, repo, username });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetCollaboratorPermissionLevelResult(client: *Client, owner: []const u8, repo: []const u8, username: []const u8) !ApiResult(models.@"repository-collaborator-permission") {
    return parseRawResponse(models.@"repository-collaborator-permission", try reposGetCollaboratorPermissionLevelRaw(client, owner, repo, username));
}

/////////////////
// Summary:
// Lock an issue
//
// Description:
// Users with push access can lock an issue or pull request's conversation.
//
// Note that, if you choose not to pass any parameters, you'll need to set `Content-Length` to zero when calling out to this endpoint. For more information, see "[HTTP method](https://docs.github.com/rest/guides/getting-started-with-the-rest-api#http-method)."
//
pub fn issuesLock(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !void {
    var raw = try issuesLockRaw(client, owner, repo, issue_number, requestBody);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn issuesLockRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}/lock", .{ client.base_url, owner, repo, issue_number });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PUT, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Unlock an issue
//
// Description:
// Users with push access can unlock an issue's conversation.
//
pub fn issuesUnlock(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64) !void {
    var raw = try issuesUnlockRaw(client, owner, repo, issue_number);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn issuesUnlockRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}/lock", .{ client.base_url, owner, repo, issue_number });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// List CODEOWNERS errors
//
// Description:
// List any syntax errors that are detected in the CODEOWNERS
// file.
//
// For more information about the correct CODEOWNERS syntax,
// see "[About code owners](https://docs.github.com/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/about-code-owners)."
//
pub fn reposCodeownersErrors(client: *Client, owner: []const u8, repo: []const u8, ref: ?[]const u8) !Owned(models.@"codeowners-errors") {
    var result = try reposCodeownersErrorsResult(client, owner, repo, ref);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposCodeownersErrorsRaw(client: *Client, owner: []const u8, repo: []const u8, ref: ?[]const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/codeowners/errors", .{ client.base_url, owner, repo });
    var first_query = true;
    if (ref) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "ref", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposCodeownersErrorsResult(client: *Client, owner: []const u8, repo: []const u8, ref: ?[]const u8) !ApiResult(models.@"codeowners-errors") {
    return parseRawResponse(models.@"codeowners-errors", try reposCodeownersErrorsRaw(client, owner, repo, ref));
}

/////////////////
// Summary:
// Delete a repository invitation
//
// Description:
//
//
pub fn reposDeleteInvitation(client: *Client, owner: []const u8, repo: []const u8, invitation_id: i64) !void {
    var raw = try reposDeleteInvitationRaw(client, owner, repo, invitation_id);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposDeleteInvitationRaw(client: *Client, owner: []const u8, repo: []const u8, invitation_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/invitations/{d}", .{ client.base_url, owner, repo, invitation_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Update a repository invitation
//
// Description:
//
//
pub fn reposUpdateInvitation(client: *Client, owner: []const u8, repo: []const u8, invitation_id: i64, requestBody: std.json.Value) !Owned(models.@"repository-invitation") {
    var result = try reposUpdateInvitationResult(client, owner, repo, invitation_id, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposUpdateInvitationRaw(client: *Client, owner: []const u8, repo: []const u8, invitation_id: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/invitations/{d}", .{ client.base_url, owner, repo, invitation_id });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PATCH, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposUpdateInvitationResult(client: *Client, owner: []const u8, repo: []const u8, invitation_id: i64, requestBody: std.json.Value) !ApiResult(models.@"repository-invitation") {
    return parseRawResponse(models.@"repository-invitation", try reposUpdateInvitationRaw(client, owner, repo, invitation_id, requestBody));
}

/////////////////
// Summary:
// List deploy keys
//
// Description:
//
//
pub fn reposListDeployKeys(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try reposListDeployKeysResult(client, owner, repo, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListDeployKeysRaw(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/keys", .{ client.base_url, owner, repo });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListDeployKeysResult(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposListDeployKeysRaw(client, owner, repo, per_page, page));
}

/////////////////
// Summary:
// Create a deploy key
//
// Description:
// You can create a read-only deploy key.
//
pub fn reposCreateDeployKey(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.@"deploy-key") {
    var result = try reposCreateDeployKeyResult(client, owner, repo, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposCreateDeployKeyRaw(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/keys", .{ client.base_url, owner, repo });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposCreateDeployKeyResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.@"deploy-key") {
    return parseRawResponse(models.@"deploy-key", try reposCreateDeployKeyRaw(client, owner, repo, requestBody));
}

/////////////////
// Summary:
// Get a GitHub Pages site
//
// Description:
// Gets information about a GitHub Pages site.
//
// OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
//
pub fn reposGetPages(client: *Client, owner: []const u8, repo: []const u8) !Owned(models.page) {
    var result = try reposGetPagesResult(client, owner, repo);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetPagesRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pages", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetPagesResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult(models.page) {
    return parseRawResponse(models.page, try reposGetPagesRaw(client, owner, repo));
}

/////////////////
// Summary:
// Create a GitHub Pages site
//
// Description:
// Configures a GitHub Pages site. For more information, see "[About GitHub Pages](/github/working-with-github-pages/about-github-pages)."
//
// The authenticated user must be a repository administrator, maintainer, or have the 'manage GitHub Pages settings' permission.
//
// OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
//
pub fn reposCreatePagesSite(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.page) {
    var result = try reposCreatePagesSiteResult(client, owner, repo, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposCreatePagesSiteRaw(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pages", .{ client.base_url, owner, repo });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposCreatePagesSiteResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.page) {
    return parseRawResponse(models.page, try reposCreatePagesSiteRaw(client, owner, repo, requestBody));
}

/////////////////
// Summary:
// Update information about a GitHub Pages site
//
// Description:
// Updates information for a GitHub Pages site. For more information, see "[About GitHub Pages](/github/working-with-github-pages/about-github-pages).
//
// The authenticated user must be a repository administrator, maintainer, or have the 'manage GitHub Pages settings' permission.
//
// OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
//
pub fn reposUpdateInformationAboutPagesSite(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !void {
    var raw = try reposUpdateInformationAboutPagesSiteRaw(client, owner, repo, requestBody);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposUpdateInformationAboutPagesSiteRaw(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pages", .{ client.base_url, owner, repo });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PUT, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Delete a GitHub Pages site
//
// Description:
// Deletes a GitHub Pages site. For more information, see "[About GitHub Pages](/github/working-with-github-pages/about-github-pages).
//
// The authenticated user must be a repository administrator, maintainer, or have the 'manage GitHub Pages settings' permission.
//
// OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
//
pub fn reposDeletePagesSite(client: *Client, owner: []const u8, repo: []const u8) !void {
    var raw = try reposDeletePagesSiteRaw(client, owner, repo);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposDeletePagesSiteRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pages", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Get repository clones
//
// Description:
// Get the total number of clones and breakdown per day or week for the last 14 days. Timestamps are aligned to UTC midnight of the beginning of the day or week. Week begins on Monday.
//
pub fn reposGetClones(client: *Client, owner: []const u8, repo: []const u8, per: ?[]const u8) !Owned(models.@"clone-traffic") {
    var result = try reposGetClonesResult(client, owner, repo, per);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetClonesRaw(client: *Client, owner: []const u8, repo: []const u8, per: ?[]const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/traffic/clones", .{ client.base_url, owner, repo });
    var first_query = true;
    if (per) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetClonesResult(client: *Client, owner: []const u8, repo: []const u8, per: ?[]const u8) !ApiResult(models.@"clone-traffic") {
    return parseRawResponse(models.@"clone-traffic", try reposGetClonesRaw(client, owner, repo, per));
}

/////////////////
// Summary:
// Create a commit status
//
// Description:
// Users with push access in a repository can create commit statuses for a given SHA.
//
// Note: there is a limit of 1000 statuses per `sha` and `context` within a repository. Attempts to create more than 1000 statuses will result in a validation error.
//
pub fn reposCreateCommitStatus(client: *Client, owner: []const u8, repo: []const u8, sha: []const u8, requestBody: std.json.Value) !Owned(models.status) {
    var result = try reposCreateCommitStatusResult(client, owner, repo, sha, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposCreateCommitStatusRaw(client: *Client, owner: []const u8, repo: []const u8, sha: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/statuses/{s}", .{ client.base_url, owner, repo, sha });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposCreateCommitStatusResult(client: *Client, owner: []const u8, repo: []const u8, sha: []const u8, requestBody: std.json.Value) !ApiResult(models.status) {
    return parseRawResponse(models.status, try reposCreateCommitStatusRaw(client, owner, repo, sha, requestBody));
}

/////////////////
// Summary:
// Get a deploy key
//
// Description:
//
//
pub fn reposGetDeployKey(client: *Client, owner: []const u8, repo: []const u8, key_id: i64) !Owned(models.@"deploy-key") {
    var result = try reposGetDeployKeyResult(client, owner, repo, key_id);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetDeployKeyRaw(client: *Client, owner: []const u8, repo: []const u8, key_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/keys/{d}", .{ client.base_url, owner, repo, key_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetDeployKeyResult(client: *Client, owner: []const u8, repo: []const u8, key_id: i64) !ApiResult(models.@"deploy-key") {
    return parseRawResponse(models.@"deploy-key", try reposGetDeployKeyRaw(client, owner, repo, key_id));
}

/////////////////
// Summary:
// Delete a deploy key
//
// Description:
// Deploy keys are immutable. If you need to update a key, remove the key and create a new one instead.
//
pub fn reposDeleteDeployKey(client: *Client, owner: []const u8, repo: []const u8, key_id: i64) !void {
    var raw = try reposDeleteDeployKeyRaw(client, owner, repo, key_id);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposDeleteDeployKeyRaw(client: *Client, owner: []const u8, repo: []const u8, key_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/keys/{d}", .{ client.base_url, owner, repo, key_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// List comments for a pull request review
//
// Description:
// Lists comments for a specific pull request review.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn pullsListCommentsForReview(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, review_id: i64, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try pullsListCommentsForReviewResult(client, owner, repo, pull_number, review_id, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullsListCommentsForReviewRaw(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, review_id: i64, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pulls/{d}/reviews/{d}/comments", .{ client.base_url, owner, repo, pull_number, review_id });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullsListCommentsForReviewResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, review_id: i64, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try pullsListCommentsForReviewRaw(client, owner, repo, pull_number, review_id, per_page, page));
}

/////////////////
// Summary:
// Get all requested reviewers for a pull request
//
// Description:
// Gets the users or teams whose review is requested for a pull request. Once a requested reviewer submits a review, they are no longer considered a requested reviewer. Their review will instead be returned by the [List reviews for a pull request](https://docs.github.com/rest/pulls/reviews#list-reviews-for-a-pull-request) operation.
//
pub fn pullsListRequestedReviewers(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64) !Owned(models.@"pull-request-review-request") {
    var result = try pullsListRequestedReviewersResult(client, owner, repo, pull_number);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullsListRequestedReviewersRaw(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pulls/{d}/requested_reviewers", .{ client.base_url, owner, repo, pull_number });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullsListRequestedReviewersResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64) !ApiResult(models.@"pull-request-review-request") {
    return parseRawResponse(models.@"pull-request-review-request", try pullsListRequestedReviewersRaw(client, owner, repo, pull_number));
}

/////////////////
// Summary:
// Request reviewers for a pull request
//
// Description:
// Requests reviews for a pull request from a given set of users and/or teams.
// This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating content too quickly using this endpoint may result in secondary rate limiting. For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)" and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
//
pub fn pullsRequestReviewers(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !Owned(models.@"pull-request-simple") {
    var result = try pullsRequestReviewersResult(client, owner, repo, pull_number, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullsRequestReviewersRaw(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pulls/{d}/requested_reviewers", .{ client.base_url, owner, repo, pull_number });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullsRequestReviewersResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !ApiResult(models.@"pull-request-simple") {
    return parseRawResponse(models.@"pull-request-simple", try pullsRequestReviewersRaw(client, owner, repo, pull_number, requestBody));
}

/////////////////
// Summary:
// Remove requested reviewers from a pull request
//
// Description:
// Removes review requests from a pull request for a given set of users and/or teams.
//
pub fn pullsRemoveRequestedReviewers(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !Owned(models.@"pull-request-simple") {
    var result = try pullsRemoveRequestedReviewersResult(client, owner, repo, pull_number, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullsRemoveRequestedReviewersRaw(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pulls/{d}/requested_reviewers", .{ client.base_url, owner, repo, pull_number });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullsRemoveRequestedReviewersResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !ApiResult(models.@"pull-request-simple") {
    return parseRawResponse(models.@"pull-request-simple", try pullsRemoveRequestedReviewersRaw(client, owner, repo, pull_number, requestBody));
}

/////////////////
// Summary:
// List issue events
//
// Description:
// Lists all events for an issue.
//
pub fn issuesListEvents(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try issuesListEventsResult(client, owner, repo, issue_number, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesListEventsRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}/events", .{ client.base_url, owner, repo, issue_number });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesListEventsResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try issuesListEventsRaw(client, owner, repo, issue_number, per_page, page));
}

/////////////////
// Summary:
// Get users with access to the protected branch
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
// Lists the people who have push access to this branch.
//
pub fn reposGetUsersWithAccessToProtectedBranch(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !Owned([]const std.json.Value) {
    var result = try reposGetUsersWithAccessToProtectedBranchResult(client, owner, repo, branch);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetUsersWithAccessToProtectedBranchRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection/restrictions/users", .{ client.base_url, owner, repo, branch });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetUsersWithAccessToProtectedBranchResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposGetUsersWithAccessToProtectedBranchRaw(client, owner, repo, branch));
}

/////////////////
// Summary:
// Add user access restrictions
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
// Grants the specified people push access for this branch.
//
// | Type    | Description                                                                                                                   |
// | ------- | ----------------------------------------------------------------------------------------------------------------------------- |
// | `array` | Usernames for people who can have push access. **Note**: The list of users, apps, and teams in total is limited to 100 items. |
//
pub fn reposAddUserAccessRestrictions(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned([]const std.json.Value) {
    var result = try reposAddUserAccessRestrictionsResult(client, owner, repo, branch, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposAddUserAccessRestrictionsRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection/restrictions/users", .{ client.base_url, owner, repo, branch });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposAddUserAccessRestrictionsResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposAddUserAccessRestrictionsRaw(client, owner, repo, branch, requestBody));
}

/////////////////
// Summary:
// Set user access restrictions
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
// Replaces the list of people that have push access to this branch. This removes all people that previously had push access and grants push access to the new list of people.
//
// | Type    | Description                                                                                                                   |
// | ------- | ----------------------------------------------------------------------------------------------------------------------------- |
// | `array` | Usernames for people who can have push access. **Note**: The list of users, apps, and teams in total is limited to 100 items. |
//
pub fn reposSetUserAccessRestrictions(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned([]const std.json.Value) {
    var result = try reposSetUserAccessRestrictionsResult(client, owner, repo, branch, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposSetUserAccessRestrictionsRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection/restrictions/users", .{ client.base_url, owner, repo, branch });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PUT, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposSetUserAccessRestrictionsResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposSetUserAccessRestrictionsRaw(client, owner, repo, branch, requestBody));
}

/////////////////
// Summary:
// Remove user access restrictions
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
// Removes the ability of a user to push to this branch.
//
// | Type    | Description                                                                                                                                   |
// | ------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
// | `array` | Usernames of the people who should no longer have push access. **Note**: The list of users, apps, and teams in total is limited to 100 items. |
//
pub fn reposRemoveUserAccessRestrictions(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned([]const std.json.Value) {
    var result = try reposRemoveUserAccessRestrictionsResult(client, owner, repo, branch, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposRemoveUserAccessRestrictionsRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection/restrictions/users", .{ client.base_url, owner, repo, branch });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposRemoveUserAccessRestrictionsResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposRemoveUserAccessRestrictionsRaw(client, owner, repo, branch, requestBody));
}

/////////////////
// Summary:
// Get latest Pages build
//
// Description:
// Gets information about the single most recent build of a GitHub Pages site.
//
// OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
//
pub fn reposGetLatestPagesBuild(client: *Client, owner: []const u8, repo: []const u8) !Owned(models.@"page-build") {
    var result = try reposGetLatestPagesBuildResult(client, owner, repo);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetLatestPagesBuildRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pages/builds/latest", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetLatestPagesBuildResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult(models.@"page-build") {
    return parseRawResponse(models.@"page-build", try reposGetLatestPagesBuildRaw(client, owner, repo));
}

/////////////////
// Summary:
// List issue types for a repository
//
// Description:
// Lists issue types available for a repository (inherited from its organization owner, with any per-repository overrides applied).
// OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
// Fine-grained access tokens require the "Metadata" repository permission (read).
//
pub fn reposListIssueTypes(client: *Client, owner: []const u8, repo: []const u8) !Owned([]const std.json.Value) {
    var result = try reposListIssueTypesResult(client, owner, repo);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListIssueTypesRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issue-types", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListIssueTypesResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposListIssueTypesRaw(client, owner, repo));
}

/////////////////
// Summary:
// Get all custom property values for a repository
//
// Description:
// Gets all custom property values that are set for a repository.
// Users with read access to the repository can use this endpoint.
//
pub fn reposCustomPropertiesForReposGetRepositoryValues(client: *Client, owner: []const u8, repo: []const u8) !Owned([]const std.json.Value) {
    var result = try reposCustomPropertiesForReposGetRepositoryValuesResult(client, owner, repo);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposCustomPropertiesForReposGetRepositoryValuesRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/properties/values", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposCustomPropertiesForReposGetRepositoryValuesResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposCustomPropertiesForReposGetRepositoryValuesRaw(client, owner, repo));
}

/////////////////
// Summary:
// Create or update custom property values for a repository
//
// Description:
// Create new or update existing custom property values for a repository.
// Using a value of `null` for a custom property will remove or 'unset' the property value from the repository.
//
// Repository admins and other users with the repository-level "edit custom property values" fine-grained permission can use this endpoint.
//
pub fn reposCustomPropertiesForReposCreateOrUpdateRepositoryValues(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !void {
    var raw = try reposCustomPropertiesForReposCreateOrUpdateRepositoryValuesRaw(client, owner, repo, requestBody);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposCustomPropertiesForReposCreateOrUpdateRepositoryValuesRaw(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/properties/values", .{ client.base_url, owner, repo });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PATCH, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Get a release by tag name
//
// Description:
// Get a published release with the specified tag.
//
pub fn reposGetReleaseByTag(client: *Client, owner: []const u8, repo: []const u8, tag: []const u8) !Owned(models.release) {
    var result = try reposGetReleaseByTagResult(client, owner, repo, tag);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetReleaseByTagRaw(client: *Client, owner: []const u8, repo: []const u8, tag: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/releases/tags/{s}", .{ client.base_url, owner, repo, tag });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetReleaseByTagResult(client: *Client, owner: []const u8, repo: []const u8, tag: []const u8) !ApiResult(models.release) {
    return parseRawResponse(models.release, try reposGetReleaseByTagRaw(client, owner, repo, tag));
}

/////////////////
// Summary:
// Sync a fork branch with the upstream repository
//
// Description:
// Sync a branch of a forked repository to keep it up-to-date with the upstream repository.
//
pub fn reposMergeUpstream(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.@"merged-upstream") {
    var result = try reposMergeUpstreamResult(client, owner, repo, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposMergeUpstreamRaw(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/merge-upstream", .{ client.base_url, owner, repo });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposMergeUpstreamResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.@"merged-upstream") {
    return parseRawResponse(models.@"merged-upstream", try reposMergeUpstreamRaw(client, owner, repo, requestBody));
}

/////////////////
// Summary:
// List environments
//
// Description:
// Lists the environments for a repository.
//
// Anyone with read access to the repository can use this endpoint.
//
// OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
//
pub fn reposGetAllEnvironments(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !Owned(std.json.Value) {
    var result = try reposGetAllEnvironmentsResult(client, owner, repo, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetAllEnvironmentsRaw(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/environments", .{ client.base_url, owner, repo });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetAllEnvironmentsResult(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !ApiResult(std.json.Value) {
    return parseRawResponse(std.json.Value, try reposGetAllEnvironmentsRaw(client, owner, repo, per_page, page));
}

/////////////////
// Summary:
// Rename a branch
//
// Description:
// Renames a branch in a repository.
//
// > [!NOTE]
// > Although the API responds immediately, the branch rename process might take some extra time to complete in the background. You won't be able to push to the old branch name while the rename process is in progress. For more information, see "[Renaming a branch](https://docs.github.com/github/administering-a-repository/renaming-a-branch)".
//
// The authenticated user must have push access to the branch. If the branch is the default branch, the authenticated user must also have admin or owner permissions.
//
// In order to rename the default branch, fine-grained access tokens also need the `administration:write` repository permission.
//
pub fn reposRenameBranch(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned(models.@"branch-with-protection") {
    var result = try reposRenameBranchResult(client, owner, repo, branch, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposRenameBranchRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/rename", .{ client.base_url, owner, repo, branch });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposRenameBranchResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult(models.@"branch-with-protection") {
    return parseRawResponse(models.@"branch-with-protection", try reposRenameBranchRaw(client, owner, repo, branch, requestBody));
}

/////////////////
// Summary:
// Get all status check contexts
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
pub fn reposGetAllStatusCheckContexts(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !Owned([]const std.json.Value) {
    var result = try reposGetAllStatusCheckContextsResult(client, owner, repo, branch);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetAllStatusCheckContextsRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection/required_status_checks/contexts", .{ client.base_url, owner, repo, branch });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetAllStatusCheckContextsResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposGetAllStatusCheckContextsRaw(client, owner, repo, branch));
}

/////////////////
// Summary:
// Add status check contexts
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
pub fn reposAddStatusCheckContexts(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned([]const std.json.Value) {
    var result = try reposAddStatusCheckContextsResult(client, owner, repo, branch, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposAddStatusCheckContextsRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection/required_status_checks/contexts", .{ client.base_url, owner, repo, branch });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposAddStatusCheckContextsResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposAddStatusCheckContextsRaw(client, owner, repo, branch, requestBody));
}

/////////////////
// Summary:
// Set status check contexts
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
pub fn reposSetStatusCheckContexts(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned([]const std.json.Value) {
    var result = try reposSetStatusCheckContextsResult(client, owner, repo, branch, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposSetStatusCheckContextsRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection/required_status_checks/contexts", .{ client.base_url, owner, repo, branch });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PUT, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposSetStatusCheckContextsResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposSetStatusCheckContextsRaw(client, owner, repo, branch, requestBody));
}

/////////////////
// Summary:
// Remove status check contexts
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
pub fn reposRemoveStatusCheckContexts(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned([]const std.json.Value) {
    var result = try reposRemoveStatusCheckContextsResult(client, owner, repo, branch, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposRemoveStatusCheckContextsRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection/required_status_checks/contexts", .{ client.base_url, owner, repo, branch });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposRemoveStatusCheckContextsResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposRemoveStatusCheckContextsRaw(client, owner, repo, branch, requestBody));
}

/////////////////
// Summary:
// Remove pull requests from a pull request stack
//
// Description:
// Removes the unmerged pull requests from a stack. Pull requests that cannot be
// unstacked (for example, those that are queued for merge) are left in place. When pull requests remain in the stack, the updated
// stack is returned with a `200`. When no pull requests remain, the stack is
// dissolved and a `204` is returned.
//
pub fn pullRequestStacksUnstack(client: *Client, owner: []const u8, repo: []const u8, stack_number: i64) !Owned(std.json.Value) {
    var result = try pullRequestStacksUnstackResult(client, owner, repo, stack_number);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullRequestStacksUnstackRaw(client: *Client, owner: []const u8, repo: []const u8, stack_number: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/stacks/{d}/unstack", .{ client.base_url, owner, repo, stack_number });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullRequestStacksUnstackResult(client: *Client, owner: []const u8, repo: []const u8, stack_number: i64) !ApiResult(std.json.Value) {
    return parseRawResponse(std.json.Value, try pullRequestStacksUnstackRaw(client, owner, repo, stack_number));
}

/////////////////
// Summary:
// Create a repository dispatch event
//
// Description:
// You can use this endpoint to trigger a webhook event called `repository_dispatch` when you want activity that happens outside of GitHub to trigger a GitHub Actions workflow or GitHub App webhook. You must configure your GitHub Actions workflow or GitHub App to run when the `repository_dispatch` event occurs. For an example `repository_dispatch` webhook payload, see "[RepositoryDispatchEvent](https://docs.github.com/webhooks/event-payloads/#repository_dispatch)."
//
// The `client_payload` parameter is available for any extra information that your workflow might need. This parameter is a JSON payload that will be passed on when the webhook event is dispatched. For example, the `client_payload` can include a message that a user would like to send using a GitHub Actions workflow. Or the `client_payload` can be used as a test to debug your workflow.
//
// This input example shows how you can use the `client_payload` as a test to debug your workflow.
//
// OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
//
pub fn reposCreateDispatchEvent(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !void {
    var raw = try reposCreateDispatchEventRaw(client, owner, repo, requestBody);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposCreateDispatchEventRaw(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/dispatches", .{ client.base_url, owner, repo });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Get commit signature protection
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
// When authenticated with admin or owner permissions to the repository, you can use this endpoint to check whether a branch requires signed commits. An enabled status of `true` indicates you must sign commits on this branch. For more information, see [Signing commits with GPG](https://docs.github.com/articles/signing-commits-with-gpg) in GitHub Help.
//
// > [!NOTE]
// > You must enable branch protection to require signed commits.
//
pub fn reposGetCommitSignatureProtection(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !Owned(models.@"protected-branch-admin-enforced") {
    var result = try reposGetCommitSignatureProtectionResult(client, owner, repo, branch);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetCommitSignatureProtectionRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection/required_signatures", .{ client.base_url, owner, repo, branch });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetCommitSignatureProtectionResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !ApiResult(models.@"protected-branch-admin-enforced") {
    return parseRawResponse(models.@"protected-branch-admin-enforced", try reposGetCommitSignatureProtectionRaw(client, owner, repo, branch));
}

/////////////////
// Summary:
// Create commit signature protection
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
// When authenticated with admin or owner permissions to the repository, you can use this endpoint to require signed commits on a branch. You must enable branch protection to require signed commits.
//
pub fn reposCreateCommitSignatureProtection(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !Owned(models.@"protected-branch-admin-enforced") {
    var result = try reposCreateCommitSignatureProtectionResult(client, owner, repo, branch);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposCreateCommitSignatureProtectionRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection/required_signatures", .{ client.base_url, owner, repo, branch });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposCreateCommitSignatureProtectionResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !ApiResult(models.@"protected-branch-admin-enforced") {
    return parseRawResponse(models.@"protected-branch-admin-enforced", try reposCreateCommitSignatureProtectionRaw(client, owner, repo, branch));
}

/////////////////
// Summary:
// Delete commit signature protection
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
// When authenticated with admin or owner permissions to the repository, you can use this endpoint to disable required signed commits on a branch. You must enable branch protection to require signed commits.
//
pub fn reposDeleteCommitSignatureProtection(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !void {
    var raw = try reposDeleteCommitSignatureProtectionRaw(client, owner, repo, branch);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposDeleteCommitSignatureProtectionRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection/required_signatures", .{ client.base_url, owner, repo, branch });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Get a custom deployment protection rule
//
// Description:
// Gets an enabled custom deployment protection rule for an environment. Anyone with read access to the repository can use this endpoint. For more information about environments, see "[Using environments for deployment](https://docs.github.com/actions/deployment/targeting-different-environments/using-environments-for-deployment)."
//
// For more information about the app that is providing this custom deployment rule, see [`GET /apps/{app_slug}`](https://docs.github.com/rest/apps/apps#get-an-app).
//
// OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
//
pub fn reposGetCustomDeploymentProtectionRule(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, protection_rule_id: i64) !Owned(models.@"deployment-protection-rule") {
    var result = try reposGetCustomDeploymentProtectionRuleResult(client, owner, repo, environment_name, protection_rule_id);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetCustomDeploymentProtectionRuleRaw(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, protection_rule_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/environments/{s}/deployment_protection_rules/{d}", .{ client.base_url, owner, repo, environment_name, protection_rule_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetCustomDeploymentProtectionRuleResult(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, protection_rule_id: i64) !ApiResult(models.@"deployment-protection-rule") {
    return parseRawResponse(models.@"deployment-protection-rule", try reposGetCustomDeploymentProtectionRuleRaw(client, owner, repo, environment_name, protection_rule_id));
}

/////////////////
// Summary:
// Disable a custom protection rule for an environment
//
// Description:
// Disables a custom deployment protection rule for an environment.
//
// The authenticated user must have admin or owner permissions to the repository to use this endpoint.
//
// OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
//
pub fn reposDisableDeploymentProtectionRule(client: *Client, environment_name: []const u8, repo: []const u8, owner: []const u8, protection_rule_id: i64) !void {
    var raw = try reposDisableDeploymentProtectionRuleRaw(client, environment_name, repo, owner, protection_rule_id);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposDisableDeploymentProtectionRuleRaw(client: *Client, environment_name: []const u8, repo: []const u8, owner: []const u8, protection_rule_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/environments/{s}/deployment_protection_rules/{d}", .{ client.base_url, environment_name, repo, owner, protection_rule_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Get a commit comment
//
// Description:
// Gets a specified commit comment.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn reposGetCommitComment(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !Owned(models.@"commit-comment") {
    var result = try reposGetCommitCommentResult(client, owner, repo, comment_id);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetCommitCommentRaw(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/comments/{d}", .{ client.base_url, owner, repo, comment_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetCommitCommentResult(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !ApiResult(models.@"commit-comment") {
    return parseRawResponse(models.@"commit-comment", try reposGetCommitCommentRaw(client, owner, repo, comment_id));
}

/////////////////
// Summary:
// Delete a commit comment
//
// Description:
//
//
pub fn reposDeleteCommitComment(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !void {
    var raw = try reposDeleteCommitCommentRaw(client, owner, repo, comment_id);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposDeleteCommitCommentRaw(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/comments/{d}", .{ client.base_url, owner, repo, comment_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Update a commit comment
//
// Description:
// Updates the contents of a specified commit comment.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn reposUpdateCommitComment(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64, requestBody: std.json.Value) !Owned(models.@"commit-comment") {
    var result = try reposUpdateCommitCommentResult(client, owner, repo, comment_id, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposUpdateCommitCommentRaw(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/comments/{d}", .{ client.base_url, owner, repo, comment_id });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PATCH, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposUpdateCommitCommentResult(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64, requestBody: std.json.Value) !ApiResult(models.@"commit-comment") {
    return parseRawResponse(models.@"commit-comment", try reposUpdateCommitCommentRaw(client, owner, repo, comment_id, requestBody));
}

/////////////////
// Summary:
// Get the weekly commit activity
//
// Description:
// Returns a weekly aggregate of the number of additions and deletions pushed to a repository.
//
// > [!NOTE]
// > This endpoint can only be used for repositories with fewer than 10,000 commits. If the repository contains 10,000 or more commits, a 422 status code will be returned.
//
pub fn reposGetCodeFrequencyStats(client: *Client, owner: []const u8, repo: []const u8) !Owned([]const std.json.Value) {
    var result = try reposGetCodeFrequencyStatsResult(client, owner, repo);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetCodeFrequencyStatsRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/stats/code_frequency", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetCodeFrequencyStatsResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposGetCodeFrequencyStatsRaw(client, owner, repo));
}

/////////////////
// Summary:
// Transfer a repository
//
// Description:
// A transfer request will need to be accepted by the new owner when transferring a personal repository to another user. The response will contain the original `owner`, and the transfer will continue asynchronously. For more details on the requirements to transfer personal and organization-owned repositories, see [about repository transfers](https://docs.github.com/articles/about-repository-transfers/).
//
pub fn reposTransfer(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.@"minimal-repository") {
    var result = try reposTransferResult(client, owner, repo, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposTransferRaw(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/transfer", .{ client.base_url, owner, repo });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposTransferResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.@"minimal-repository") {
    return parseRawResponse(models.@"minimal-repository", try reposTransferRaw(client, owner, repo, requestBody));
}

/////////////////
// Summary:
// List commits on a pull request
//
// Description:
// Lists a maximum of 250 commits for a pull request. To receive a complete
// commit list for pull requests with more than 250 commits, use the [List commits](https://docs.github.com/rest/commits/commits#list-commits)
// endpoint.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn pullsListCommits(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try pullsListCommitsResult(client, owner, repo, pull_number, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullsListCommitsRaw(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pulls/{d}/commits", .{ client.base_url, owner, repo, pull_number });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullsListCommitsResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try pullsListCommitsRaw(client, owner, repo, pull_number, per_page, page));
}

/////////////////
// Summary:
// Check if vulnerability alerts are enabled for a repository
//
// Description:
// Shows whether dependency alerts are enabled or disabled for a repository. The authenticated user must have admin read access to the repository. For more information, see "[About security alerts for vulnerable dependencies](https://docs.github.com/articles/about-security-alerts-for-vulnerable-dependencies)".
//
pub fn reposCheckVulnerabilityAlerts(client: *Client, owner: []const u8, repo: []const u8) !void {
    var raw = try reposCheckVulnerabilityAlertsRaw(client, owner, repo);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposCheckVulnerabilityAlertsRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/vulnerability-alerts", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Enable vulnerability alerts
//
// Description:
// Enables dependency alerts and the dependency graph for a repository. The authenticated user must have admin access to the repository. For more information, see "[About security alerts for vulnerable dependencies](https://docs.github.com/articles/about-security-alerts-for-vulnerable-dependencies)".
//
pub fn reposEnableVulnerabilityAlerts(client: *Client, owner: []const u8, repo: []const u8) !void {
    var raw = try reposEnableVulnerabilityAlertsRaw(client, owner, repo);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposEnableVulnerabilityAlertsRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/vulnerability-alerts", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PUT, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Disable vulnerability alerts
//
// Description:
// Disables dependency alerts and the dependency graph for a repository.
// The authenticated user must have admin access to the repository. For more information,
// see "[About security alerts for vulnerable dependencies](https://docs.github.com/articles/about-security-alerts-for-vulnerable-dependencies)".
//
pub fn reposDisableVulnerabilityAlerts(client: *Client, owner: []const u8, repo: []const u8) !void {
    var raw = try reposDisableVulnerabilityAlertsRaw(client, owner, repo);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposDisableVulnerabilityAlertsRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/vulnerability-alerts", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Get status checks protection
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
pub fn reposGetStatusChecksProtection(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !Owned(models.@"status-check-policy") {
    var result = try reposGetStatusChecksProtectionResult(client, owner, repo, branch);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetStatusChecksProtectionRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection/required_status_checks", .{ client.base_url, owner, repo, branch });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetStatusChecksProtectionResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !ApiResult(models.@"status-check-policy") {
    return parseRawResponse(models.@"status-check-policy", try reposGetStatusChecksProtectionRaw(client, owner, repo, branch));
}

/////////////////
// Summary:
// Remove status check protection
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
pub fn reposRemoveStatusCheckProtection(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !void {
    var raw = try reposRemoveStatusCheckProtectionRaw(client, owner, repo, branch);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposRemoveStatusCheckProtectionRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection/required_status_checks", .{ client.base_url, owner, repo, branch });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Update status check protection
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
// Updating required status checks requires admin or owner permissions to the repository and branch protection to be enabled.
//
pub fn reposUpdateStatusCheckProtection(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned(models.@"status-check-policy") {
    var result = try reposUpdateStatusCheckProtectionResult(client, owner, repo, branch, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposUpdateStatusCheckProtectionRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection/required_status_checks", .{ client.base_url, owner, repo, branch });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PATCH, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposUpdateStatusCheckProtectionResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult(models.@"status-check-policy") {
    return parseRawResponse(models.@"status-check-policy", try reposUpdateStatusCheckProtectionRaw(client, owner, repo, branch, requestBody));
}

/////////////////
// Summary:
// List deliveries for a repository webhook
//
// Description:
// Returns a list of webhook deliveries for a webhook configured in a repository.
//
pub fn reposListWebhookDeliveries(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64, per_page: ?i64, cursor: ?[]const u8, status: ?[]const u8) !Owned([]const std.json.Value) {
    var result = try reposListWebhookDeliveriesResult(client, owner, repo, hook_id, per_page, cursor, status);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListWebhookDeliveriesRaw(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64, per_page: ?i64, cursor: ?[]const u8, status: ?[]const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/hooks/{d}/deliveries", .{ client.base_url, owner, repo, hook_id });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (cursor) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "cursor", value);
    }
    if (status) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "status", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListWebhookDeliveriesResult(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64, per_page: ?i64, cursor: ?[]const u8, status: ?[]const u8) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposListWebhookDeliveriesRaw(client, owner, repo, hook_id, per_page, cursor, status));
}

/////////////////
// Summary:
// Get a DNS health check for GitHub Pages
//
// Description:
// Gets a health check of the DNS settings for the `CNAME` record configured for a repository's GitHub Pages.
//
// The first request to this endpoint returns a `202 Accepted` status and starts an asynchronous background task to get the results for the domain. After the background task completes, subsequent requests to this endpoint return a `200 OK` status with the health check results in the response.
//
// The authenticated user must be a repository administrator, maintainer, or have the 'manage GitHub Pages settings' permission to use this endpoint.
//
// OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
//
pub fn reposGetPagesHealthCheck(client: *Client, owner: []const u8, repo: []const u8) !Owned(models.@"pages-health-check") {
    var result = try reposGetPagesHealthCheckResult(client, owner, repo);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetPagesHealthCheckRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pages/health", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetPagesHealthCheckResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult(models.@"pages-health-check") {
    return parseRawResponse(models.@"pages-health-check", try reposGetPagesHealthCheckRaw(client, owner, repo));
}

/////////////////
// Summary:
// List repository invitations
//
// Description:
// When authenticating as a user with admin rights to a repository, this endpoint will list all currently open repository invitations.
//
pub fn reposListInvitations(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try reposListInvitationsResult(client, owner, repo, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListInvitationsRaw(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/invitations", .{ client.base_url, owner, repo });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListInvitationsResult(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposListInvitationsRaw(client, owner, repo, per_page, page));
}

/////////////////
// Summary:
// Update a pull request branch
//
// Description:
// Updates the pull request branch with the latest upstream changes by merging HEAD from the base branch into the pull request branch.
// Note: If making a request on behalf of a GitHub App you must also have permissions to write the contents of the head repository.
//
pub fn pullsUpdateBranch(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !Owned(std.json.Value) {
    var result = try pullsUpdateBranchResult(client, owner, repo, pull_number, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullsUpdateBranchRaw(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pulls/{d}/update-branch", .{ client.base_url, owner, repo, pull_number });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PUT, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullsUpdateBranchResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !ApiResult(std.json.Value) {
    return parseRawResponse(std.json.Value, try pullsUpdateBranchRaw(client, owner, repo, pull_number, requestBody));
}

/////////////////
// Summary:
// Submit a review for a pull request
//
// Description:
// Submits a pending review for a pull request. For more information about creating a pending review for a pull request, see "[Create a review for a pull request](https://docs.github.com/rest/pulls/reviews#create-a-review-for-a-pull-request)."
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn pullsSubmitReview(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, review_id: i64, requestBody: std.json.Value) !Owned(models.@"pull-request-review") {
    var result = try pullsSubmitReviewResult(client, owner, repo, pull_number, review_id, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullsSubmitReviewRaw(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, review_id: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pulls/{d}/reviews/{d}/events", .{ client.base_url, owner, repo, pull_number, review_id });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullsSubmitReviewResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, review_id: i64, requestBody: std.json.Value) !ApiResult(models.@"pull-request-review") {
    return parseRawResponse(models.@"pull-request-review", try pullsSubmitReviewRaw(client, owner, repo, pull_number, review_id, requestBody));
}

/////////////////
// Summary:
// Get a repository webhook
//
// Description:
// Returns a webhook configured in a repository. To get only the webhook `config` properties, see "[Get a webhook configuration for a repository](/rest/webhooks/repo-config#get-a-webhook-configuration-for-a-repository)."
//
pub fn reposGetWebhook(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64) !Owned(models.hook) {
    var result = try reposGetWebhookResult(client, owner, repo, hook_id);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetWebhookRaw(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/hooks/{d}", .{ client.base_url, owner, repo, hook_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetWebhookResult(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64) !ApiResult(models.hook) {
    return parseRawResponse(models.hook, try reposGetWebhookRaw(client, owner, repo, hook_id));
}

/////////////////
// Summary:
// Delete a repository webhook
//
// Description:
// Delete a webhook for an organization.
//
// The authenticated user must be a repository owner, or have admin access in the repository, to delete the webhook.
//
pub fn reposDeleteWebhook(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64) !void {
    var raw = try reposDeleteWebhookRaw(client, owner, repo, hook_id);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposDeleteWebhookRaw(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/hooks/{d}", .{ client.base_url, owner, repo, hook_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Update a repository webhook
//
// Description:
// Updates a webhook configured in a repository. If you previously had a `secret` set, you must provide the same `secret` or set a new `secret` or the secret will be removed. If you are only updating individual webhook `config` properties, use "[Update a webhook configuration for a repository](/rest/webhooks/repo-config#update-a-webhook-configuration-for-a-repository)."
//
pub fn reposUpdateWebhook(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64, requestBody: std.json.Value) !Owned(models.hook) {
    var result = try reposUpdateWebhookResult(client, owner, repo, hook_id, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposUpdateWebhookRaw(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/hooks/{d}", .{ client.base_url, owner, repo, hook_id });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PATCH, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposUpdateWebhookResult(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64, requestBody: std.json.Value) !ApiResult(models.hook) {
    return parseRawResponse(models.hook, try reposUpdateWebhookRaw(client, owner, repo, hook_id, requestBody));
}

/////////////////
// Summary:
// Get a release asset
//
// Description:
// To download the asset's binary content:
//
// - If within a browser, fetch the location specified in the `browser_download_url` key provided in the response.
// - Alternatively, set the `Accept` header of the request to
//   [`application/octet-stream`](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types).
//   The API will either redirect the client to the location, or stream it directly if possible.
//   API clients should handle both a `200` or `302` response.
//
pub fn reposGetReleaseAsset(client: *Client, owner: []const u8, repo: []const u8, asset_id: i64) !Owned(models.@"release-asset") {
    var result = try reposGetReleaseAssetResult(client, owner, repo, asset_id);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetReleaseAssetRaw(client: *Client, owner: []const u8, repo: []const u8, asset_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/releases/assets/{d}", .{ client.base_url, owner, repo, asset_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetReleaseAssetResult(client: *Client, owner: []const u8, repo: []const u8, asset_id: i64) !ApiResult(models.@"release-asset") {
    return parseRawResponse(models.@"release-asset", try reposGetReleaseAssetRaw(client, owner, repo, asset_id));
}

/////////////////
// Summary:
// Delete a release asset
//
// Description:
//
//
pub fn reposDeleteReleaseAsset(client: *Client, owner: []const u8, repo: []const u8, asset_id: i64) !void {
    var raw = try reposDeleteReleaseAssetRaw(client, owner, repo, asset_id);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposDeleteReleaseAssetRaw(client: *Client, owner: []const u8, repo: []const u8, asset_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/releases/assets/{d}", .{ client.base_url, owner, repo, asset_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Update a release asset
//
// Description:
// Users with push access to the repository can edit a release asset.
//
pub fn reposUpdateReleaseAsset(client: *Client, owner: []const u8, repo: []const u8, asset_id: i64, requestBody: std.json.Value) !Owned(models.@"release-asset") {
    var result = try reposUpdateReleaseAssetResult(client, owner, repo, asset_id, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposUpdateReleaseAssetRaw(client: *Client, owner: []const u8, repo: []const u8, asset_id: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/releases/assets/{d}", .{ client.base_url, owner, repo, asset_id });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PATCH, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposUpdateReleaseAssetResult(client: *Client, owner: []const u8, repo: []const u8, asset_id: i64, requestBody: std.json.Value) !ApiResult(models.@"release-asset") {
    return parseRawResponse(models.@"release-asset", try reposUpdateReleaseAssetRaw(client, owner, repo, asset_id, requestBody));
}

/////////////////
// Summary:
// Get repository content
//
// Description:
// Gets the contents of a file or directory in a repository. Specify the file path or directory with the `path` parameter. If you omit the `path` parameter, you will receive the contents of the repository's root directory.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github.raw+json`**: Returns the raw file contents for files and symlinks.
// - **`application/vnd.github.html+json`**: Returns the file contents in HTML. Markup languages are rendered to HTML using GitHub's open-source [Markup library](https://github.com/github/markup).
// - **`application/vnd.github.object+json`**: Returns the contents in a consistent object format regardless of the content type. For example, instead of an array of objects for a directory, the response will be an object with an `entries` attribute containing the array of objects.
//
// If the content is a directory, the response will be an array of objects, one object for each item in the directory. When listing the contents of a directory, submodules have their "type" specified as "file". Logically, the value _should_ be "submodule". This behavior exists [for backwards compatibility purposes](https://git.io/v1YCW). In the next major version of the API, the type will be returned as "submodule".
//
// If the content is a symlink and the symlink's target is a normal file in the repository, then the API responds with the content of the file. Otherwise, the API responds with an object describing the symlink itself.
//
// If the content is a submodule, the `submodule_git_url` field identifies the location of the submodule repository, and the `sha` identifies a specific commit within the submodule repository. Git uses the given URL when cloning the submodule repository, and checks out the submodule at that specific commit. If the submodule repository is not hosted on github.com, the Git URLs (`git_url` and `_links["git"]`) and the github.com URLs (`html_url` and `_links["html"]`) will have null values.
//
// **Notes**:
//
// - To get a repository's contents recursively, you can [recursively get the tree](https://docs.github.com/rest/git/trees#get-a-tree).
// - This API has an upper limit of 1,000 files for a directory. If you need to retrieve
// more files, use the [Git Trees API](https://docs.github.com/rest/git/trees#get-a-tree).
// - Download URLs expire and are meant to be used just once. To ensure the download URL does not expire, please use the contents API to obtain a fresh download URL for each download.
// - If the requested file's size is:
//   - 1 MB or smaller: All features of this endpoint are supported.
//   - Between 1-100 MB: Only the `raw` or `object` custom media types are supported. Both will work as normal, except that when using the `object` media type, the `content` field will be an empty
// string and the `encoding` field will be `"none"`. To get the contents of these larger files, use the `raw` media type.
//   - Greater than 100 MB: This endpoint is not supported.
//
pub fn reposGetContent(client: *Client, owner: []const u8, repo: []const u8, path: []const u8, ref: ?[]const u8, requestBody: std.json.Value) !Owned(std.json.Value) {
    var result = try reposGetContentResult(client, owner, repo, path, ref, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetContentRaw(client: *Client, owner: []const u8, repo: []const u8, path: []const u8, ref: ?[]const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/contents/{s}", .{ client.base_url, owner, repo, path });
    var first_query = true;
    if (ref) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "ref", value);
    }

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetContentResult(client: *Client, owner: []const u8, repo: []const u8, path: []const u8, ref: ?[]const u8, requestBody: std.json.Value) !ApiResult(std.json.Value) {
    return parseRawResponse(std.json.Value, try reposGetContentRaw(client, owner, repo, path, ref, requestBody));
}

/////////////////
// Summary:
// Create or update file contents
//
// Description:
// Creates a new file or replaces an existing file in a repository.
//
// > [!NOTE]
// > If you use this endpoint and the "[Delete a file](https://docs.github.com/rest/repos/contents/#delete-a-file)" endpoint in parallel, the concurrent requests will conflict and you will receive errors. You must use these endpoints serially instead.
//
// OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint. The `workflow` scope is also required in order to modify files in the `.github/workflows` directory.
//
pub fn reposCreateOrUpdateFileContents(client: *Client, owner: []const u8, repo: []const u8, path: []const u8, requestBody: std.json.Value) !Owned(models.@"file-commit") {
    var result = try reposCreateOrUpdateFileContentsResult(client, owner, repo, path, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposCreateOrUpdateFileContentsRaw(client: *Client, owner: []const u8, repo: []const u8, path: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/contents/{s}", .{ client.base_url, owner, repo, path });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PUT, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposCreateOrUpdateFileContentsResult(client: *Client, owner: []const u8, repo: []const u8, path: []const u8, requestBody: std.json.Value) !ApiResult(models.@"file-commit") {
    return parseRawResponse(models.@"file-commit", try reposCreateOrUpdateFileContentsRaw(client, owner, repo, path, requestBody));
}

/////////////////
// Summary:
// Delete a file
//
// Description:
// Deletes a file in a repository.
//
// You can provide an additional `committer` parameter, which is an object containing information about the committer. Or, you can provide an `author` parameter, which is an object containing information about the author.
//
// The `author` section is optional and is filled in with the `committer` information if omitted. If the `committer` information is omitted, the authenticated user's information is used.
//
// You must provide values for both `name` and `email`, whether you choose to use `author` or `committer`. Otherwise, you'll receive a `422` status code.
//
// > [!NOTE]
// > If you use this endpoint and the "[Create or update file contents](https://docs.github.com/rest/repos/contents/#create-or-update-file-contents)" endpoint in parallel, the concurrent requests will conflict and you will receive errors. You must use these endpoints serially instead.
//
pub fn reposDeleteFile(client: *Client, owner: []const u8, repo: []const u8, path: []const u8, requestBody: std.json.Value) !Owned(models.@"file-commit") {
    var result = try reposDeleteFileResult(client, owner, repo, path, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposDeleteFileRaw(client: *Client, owner: []const u8, repo: []const u8, path: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/contents/{s}", .{ client.base_url, owner, repo, path });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposDeleteFileResult(client: *Client, owner: []const u8, repo: []const u8, path: []const u8, requestBody: std.json.Value) !ApiResult(models.@"file-commit") {
    return parseRawResponse(models.@"file-commit", try reposDeleteFileRaw(client, owner, repo, path, requestBody));
}

/////////////////
// Summary:
// Get an issue comment
//
// Description:
// You can use the REST API to get comments on issues and pull requests. Every pull request is an issue, but not every issue is a pull request.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn issuesGetComment(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !Owned(models.@"issue-comment") {
    var result = try issuesGetCommentResult(client, owner, repo, comment_id);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesGetCommentRaw(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/comments/{d}", .{ client.base_url, owner, repo, comment_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesGetCommentResult(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !ApiResult(models.@"issue-comment") {
    return parseRawResponse(models.@"issue-comment", try issuesGetCommentRaw(client, owner, repo, comment_id));
}

/////////////////
// Summary:
// Delete an issue comment
//
// Description:
// You can use the REST API to delete comments on issues and pull requests. Every pull request is an issue, but not every issue is a pull request.
//
pub fn issuesDeleteComment(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !void {
    var raw = try issuesDeleteCommentRaw(client, owner, repo, comment_id);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn issuesDeleteCommentRaw(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/comments/{d}", .{ client.base_url, owner, repo, comment_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Update an issue comment
//
// Description:
// You can use the REST API to update comments on issues and pull requests. Every pull request is an issue, but not every issue is a pull request.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn issuesUpdateComment(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64, requestBody: std.json.Value) !Owned(models.@"issue-comment") {
    var result = try issuesUpdateCommentResult(client, owner, repo, comment_id, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesUpdateCommentRaw(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/comments/{d}", .{ client.base_url, owner, repo, comment_id });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PATCH, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesUpdateCommentResult(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64, requestBody: std.json.Value) !ApiResult(models.@"issue-comment") {
    return parseRawResponse(models.@"issue-comment", try issuesUpdateCommentRaw(client, owner, repo, comment_id, requestBody));
}

/////////////////
// Summary:
// List labels for issues in a milestone
//
// Description:
// Lists labels for issues in a milestone.
//
pub fn issuesListLabelsForMilestone(client: *Client, owner: []const u8, repo: []const u8, milestone_number: i64, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try issuesListLabelsForMilestoneResult(client, owner, repo, milestone_number, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesListLabelsForMilestoneRaw(client: *Client, owner: []const u8, repo: []const u8, milestone_number: i64, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/milestones/{d}/labels", .{ client.base_url, owner, repo, milestone_number });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesListLabelsForMilestoneResult(client: *Client, owner: []const u8, repo: []const u8, milestone_number: i64, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try issuesListLabelsForMilestoneRaw(client, owner, repo, milestone_number, per_page, page));
}

/////////////////
// Summary:
// Get an issue
//
// Description:
// The API returns a [`301 Moved Permanently` status](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api#follow-redirects) if the issue was
// [transferred](https://docs.github.com/articles/transferring-an-issue-to-another-repository/) to another repository. If
// the issue was transferred to or deleted from a repository where the authenticated user lacks read access, the API
// returns a `404 Not Found` status. If the issue was deleted from a repository where the authenticated user has read
// access, the API returns a `410 Gone` status. To receive webhook events for transferred and deleted issues, subscribe
// to the [`issues`](https://docs.github.com/webhooks/event-payloads/#issues) webhook.
//
// > [!NOTE]
// > GitHub's REST API considers every pull request an issue, but not every issue is a pull request. For this reason, "Issues" endpoints may return both issues and pull requests in the response. You can identify pull requests by the `pull_request` key. Be aware that the `id` of a pull request returned from "Issues" endpoints will be an _issue id_. To find out the pull request id, use the "[List pull requests](https://docs.github.com/rest/pulls/pulls#list-pull-requests)" endpoint.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn issuesGet(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64) !Owned(models.issue) {
    var result = try issuesGetResult(client, owner, repo, issue_number);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesGetRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}", .{ client.base_url, owner, repo, issue_number });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesGetResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64) !ApiResult(models.issue) {
    return parseRawResponse(models.issue, try issuesGetRaw(client, owner, repo, issue_number));
}

/////////////////
// Summary:
// Update an issue
//
// Description:
// Issue owners and users with push access or Triage role can edit an issue.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn issuesUpdate(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !Owned(std.json.Value) {
    var result = try issuesUpdateResult(client, owner, repo, issue_number, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesUpdateRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}", .{ client.base_url, owner, repo, issue_number });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PATCH, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesUpdateResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !ApiResult(std.json.Value) {
    return parseRawResponse(std.json.Value, try issuesUpdateRaw(client, owner, repo, issue_number, requestBody));
}

/////////////////
// Summary:
// Reprioritize sub-issue
//
// Description:
// You can use the REST API to reprioritize a sub-issue to a different position in the parent list.
//
pub fn issuesReprioritizeSubIssue(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !Owned(models.issue) {
    var result = try issuesReprioritizeSubIssueResult(client, owner, repo, issue_number, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesReprioritizeSubIssueRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}/sub_issues/priority", .{ client.base_url, owner, repo, issue_number });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PATCH, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesReprioritizeSubIssueResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !ApiResult(models.issue) {
    return parseRawResponse(models.issue, try issuesReprioritizeSubIssueRaw(client, owner, repo, issue_number, requestBody));
}

/////////////////
// Summary:
// Add assignees to an issue
//
// Description:
// Adds up to 10 assignees to an issue. Users already assigned to an issue are not replaced.
//
pub fn issuesAddAssignees(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !Owned(models.issue) {
    var result = try issuesAddAssigneesResult(client, owner, repo, issue_number, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesAddAssigneesRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}/assignees", .{ client.base_url, owner, repo, issue_number });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesAddAssigneesResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !ApiResult(models.issue) {
    return parseRawResponse(models.issue, try issuesAddAssigneesRaw(client, owner, repo, issue_number, requestBody));
}

/////////////////
// Summary:
// Remove assignees from an issue
//
// Description:
// Removes one or more assignees from an issue.
//
pub fn issuesRemoveAssignees(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !Owned(models.issue) {
    var result = try issuesRemoveAssigneesResult(client, owner, repo, issue_number, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesRemoveAssigneesRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}/assignees", .{ client.base_url, owner, repo, issue_number });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesRemoveAssigneesResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !ApiResult(models.issue) {
    return parseRawResponse(models.issue, try issuesRemoveAssigneesRaw(client, owner, repo, issue_number, requestBody));
}

/////////////////
// Summary:
// List repository collaborators
//
// Description:
// For organization-owned repositories, the list of collaborators includes outside collaborators, organization members that are direct collaborators, organization members with access through team memberships, organization members with access through default organization permissions, and organization owners.
// The `permissions` hash returned in the response contains the base role permissions of the collaborator. The `role_name` is the highest role assigned to the collaborator after considering all sources of grants, including: repo, teams, organization, and enterprise.
// There is presently not a way to differentiate between an organization level grant and a repository level grant from this endpoint response.
//
// Team members will include the members of child teams.
//
// The authenticated user must have write, maintain, or admin privileges on the repository to use this endpoint. For organization-owned repositories, the authenticated user needs to be a member of the organization.
// OAuth app tokens and personal access tokens (classic) need the `read:org` and `repo` scopes to use this endpoint.
//
pub fn reposListCollaborators(client: *Client, owner: []const u8, repo: []const u8, affiliation: ?[]const u8, permission: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try reposListCollaboratorsResult(client, owner, repo, affiliation, permission, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListCollaboratorsRaw(client: *Client, owner: []const u8, repo: []const u8, affiliation: ?[]const u8, permission: ?[]const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/collaborators", .{ client.base_url, owner, repo });
    var first_query = true;
    if (affiliation) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "affiliation", value);
    }
    if (permission) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "permission", value);
    }
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListCollaboratorsResult(client: *Client, owner: []const u8, repo: []const u8, affiliation: ?[]const u8, permission: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposListCollaboratorsRaw(client, owner, repo, affiliation, permission, per_page, page));
}

/////////////////
// Summary:
// Get access restrictions
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
// Lists who has access to this protected branch.
//
// > [!NOTE]
// > Users, apps, and teams `restrictions` are only available for organization-owned repositories.
//
pub fn reposGetAccessRestrictions(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !Owned(models.@"branch-restriction-policy") {
    var result = try reposGetAccessRestrictionsResult(client, owner, repo, branch);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetAccessRestrictionsRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection/restrictions", .{ client.base_url, owner, repo, branch });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetAccessRestrictionsResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !ApiResult(models.@"branch-restriction-policy") {
    return parseRawResponse(models.@"branch-restriction-policy", try reposGetAccessRestrictionsRaw(client, owner, repo, branch));
}

/////////////////
// Summary:
// Delete access restrictions
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
// Disables the ability to restrict who can push to this branch.
//
pub fn reposDeleteAccessRestrictions(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !void {
    var raw = try reposDeleteAccessRestrictionsRaw(client, owner, repo, branch);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposDeleteAccessRestrictionsRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection/restrictions", .{ client.base_url, owner, repo, branch });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Get all deployment protection rules for an environment
//
// Description:
// Gets all custom deployment protection rules that are enabled for an environment. Anyone with read access to the repository can use this endpoint. For more information about environments, see "[Using environments for deployment](https://docs.github.com/actions/deployment/targeting-different-environments/using-environments-for-deployment)."
//
// For more information about the app that is providing this custom deployment rule, see the [documentation for the `GET /apps/{app_slug}` endpoint](https://docs.github.com/rest/apps/apps#get-an-app).
//
// OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
//
pub fn reposGetAllDeploymentProtectionRules(client: *Client, environment_name: []const u8, repo: []const u8, owner: []const u8) !Owned(std.json.Value) {
    var result = try reposGetAllDeploymentProtectionRulesResult(client, environment_name, repo, owner);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetAllDeploymentProtectionRulesRaw(client: *Client, environment_name: []const u8, repo: []const u8, owner: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/environments/{s}/deployment_protection_rules", .{ client.base_url, environment_name, repo, owner });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetAllDeploymentProtectionRulesResult(client: *Client, environment_name: []const u8, repo: []const u8, owner: []const u8) !ApiResult(std.json.Value) {
    return parseRawResponse(std.json.Value, try reposGetAllDeploymentProtectionRulesRaw(client, environment_name, repo, owner));
}

/////////////////
// Summary:
// Create a custom deployment protection rule on an environment
//
// Description:
// Enable a custom deployment protection rule for an environment.
//
// The authenticated user must have admin or owner permissions to the repository to use this endpoint.
//
// For more information about the app that is providing this custom deployment rule, see the [documentation for the `GET /apps/{app_slug}` endpoint](https://docs.github.com/rest/apps/apps#get-an-app), as well as the [guide to creating custom deployment protection rules](https://docs.github.com/actions/managing-workflow-runs-and-deployments/managing-deployments/creating-custom-deployment-protection-rules).
//
// OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
//
pub fn reposCreateDeploymentProtectionRule(client: *Client, environment_name: []const u8, repo: []const u8, owner: []const u8, requestBody: std.json.Value) !Owned(models.@"deployment-protection-rule") {
    var result = try reposCreateDeploymentProtectionRuleResult(client, environment_name, repo, owner, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposCreateDeploymentProtectionRuleRaw(client: *Client, environment_name: []const u8, repo: []const u8, owner: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/environments/{s}/deployment_protection_rules", .{ client.base_url, environment_name, repo, owner });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposCreateDeploymentProtectionRuleResult(client: *Client, environment_name: []const u8, repo: []const u8, owner: []const u8, requestBody: std.json.Value) !ApiResult(models.@"deployment-protection-rule") {
    return parseRawResponse(models.@"deployment-protection-rule", try reposCreateDeploymentProtectionRuleRaw(client, environment_name, repo, owner, requestBody));
}

/////////////////
// Summary:
// List repository issues
//
// Description:
// List issues in a repository. Only open issues will be listed.
//
// > [!NOTE]
// > GitHub's REST API considers every pull request an issue, but not every issue is a pull request. For this reason, "Issues" endpoints may return both issues and pull requests in the response. You can identify pull requests by the `pull_request` key. Be aware that the `id` of a pull request returned from "Issues" endpoints will be an _issue id_. To find out the pull request id, use the "[List pull requests](https://docs.github.com/rest/pulls/pulls#list-pull-requests)" endpoint.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn issuesListForRepo(client: *Client, owner: []const u8, repo: []const u8, milestone: ?[]const u8, state: ?[]const u8, assignee: ?[]const u8, @"type": ?[]const u8, creator: ?[]const u8, mentioned: ?[]const u8, issue_field_values: ?[]const u8, labels: ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try issuesListForRepoResult(client, owner, repo, milestone, state, assignee, @"type", creator, mentioned, issue_field_values, labels, sort, direction, since, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesListForRepoRaw(client: *Client, owner: []const u8, repo: []const u8, milestone: ?[]const u8, state: ?[]const u8, assignee: ?[]const u8, @"type": ?[]const u8, creator: ?[]const u8, mentioned: ?[]const u8, issue_field_values: ?[]const u8, labels: ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues", .{ client.base_url, owner, repo });
    var first_query = true;
    if (milestone) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "milestone", value);
    }
    if (state) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "state", value);
    }
    if (assignee) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "assignee", value);
    }
    if (@"type") |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "type", value);
    }
    if (creator) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "creator", value);
    }
    if (mentioned) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "mentioned", value);
    }
    if (issue_field_values) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "issue_field_values", value);
    }
    if (labels) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "labels", value);
    }
    if (sort) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "sort", value);
    }
    if (direction) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "direction", value);
    }
    if (since) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "since", value);
    }
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesListForRepoResult(client: *Client, owner: []const u8, repo: []const u8, milestone: ?[]const u8, state: ?[]const u8, assignee: ?[]const u8, @"type": ?[]const u8, creator: ?[]const u8, mentioned: ?[]const u8, issue_field_values: ?[]const u8, labels: ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try issuesListForRepoRaw(client, owner, repo, milestone, state, assignee, @"type", creator, mentioned, issue_field_values, labels, sort, direction, since, per_page, page));
}

/////////////////
// Summary:
// Create an issue
//
// Description:
// Any user with pull access to a repository can create an issue. If [issues are disabled in the repository](https://docs.github.com/articles/disabling-issues/), the API returns a `410 Gone` status.
//
// This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating content too quickly using this endpoint may result in secondary rate limiting. For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)"
// and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn issuesCreate(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.issue) {
    var result = try issuesCreateResult(client, owner, repo, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesCreateRaw(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues", .{ client.base_url, owner, repo });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesCreateResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.issue) {
    return parseRawResponse(models.issue, try issuesCreateRaw(client, owner, repo, requestBody));
}

/////////////////
// Summary:
// List release assets
//
// Description:
//
//
pub fn reposListReleaseAssets(client: *Client, owner: []const u8, repo: []const u8, release_id: i64, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try reposListReleaseAssetsResult(client, owner, repo, release_id, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListReleaseAssetsRaw(client: *Client, owner: []const u8, repo: []const u8, release_id: i64, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/releases/{d}/assets", .{ client.base_url, owner, repo, release_id });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListReleaseAssetsResult(client: *Client, owner: []const u8, repo: []const u8, release_id: i64, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposListReleaseAssetsRaw(client, owner, repo, release_id, per_page, page));
}

/////////////////
// Summary:
// Upload a release asset
//
// Description:
// This endpoint makes use of a [Hypermedia relation](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#hypermedia) to determine which URL to access. The endpoint you call to upload release assets is specific to your release. Use the `upload_url` returned in
// the response of the [Create a release endpoint](https://docs.github.com/rest/releases/releases#create-a-release) to upload a release asset.
//
// You need to use an HTTP client which supports [SNI](http://en.wikipedia.org/wiki/Server_Name_Indication) to make calls to this endpoint.
//
// Most libraries will set the required `Content-Length` header automatically. Use the required `Content-Type` header to provide the media type of the asset. For a list of media types, see [Media Types](https://www.iana.org/assignments/media-types/media-types.xhtml). For example:
//
// `application/zip`
//
// GitHub expects the asset data in its raw binary form, rather than JSON. You will send the raw binary content of the asset as the request body. Everything else about the endpoint is the same as the rest of the API. For example,
// you'll still need to pass your authentication to be able to upload an asset.
//
// When an upstream failure occurs, you will receive a `502 Bad Gateway` status. This may leave an empty asset with a state of `starter`. It can be safely deleted.
//
// **Notes:**
// *   GitHub renames asset filenames that have special characters, non-alphanumeric characters, and leading or trailing periods. The "[List release assets](https://docs.github.com/rest/releases/assets#list-release-assets)"
// endpoint lists the renamed filenames. For more information and help, contact [GitHub Support](https://support.github.com/contact?tags=dotcom-rest-api).
// *   To find the `release_id` query the [`GET /repos/{owner}/{repo}/releases/latest` endpoint](https://docs.github.com/rest/releases/releases#get-the-latest-release).
// *   If you upload an asset with the same filename as another uploaded asset, you'll receive an error and must delete the old file before you can re-upload the new asset.
//
pub fn reposUploadReleaseAsset(client: *Client, owner: []const u8, repo: []const u8, release_id: i64, name: []const u8, label: ?[]const u8, requestBody: []const u8) !Owned(models.@"release-asset") {
    var result = try reposUploadReleaseAssetResult(client, owner, repo, release_id, name, label, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposUploadReleaseAssetRaw(client: *Client, owner: []const u8, repo: []const u8, release_id: i64, name: []const u8, label: ?[]const u8, requestBody: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/releases/{d}/assets", .{ client.base_url, owner, repo, release_id });
    var first_query = true;
    try appendQueryParam(&uri_buf.writer, &first_query, "name", name);
    if (label) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "label", value);
    }
    const payload: ?[]const u8 = requestBody;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    try appendClientHeaders(allocator, &operation_headers, client, "application/octet-stream", "application/json");
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    if (client.http_observer) |obs| {
        if (obs.onRequest) |cb| cb(obs.ctx, std.http.Method.POST, uri_buf.written(), operation_headers.items, payload);
    }

    const uri = try std.Uri.parse(uri_buf.written());
    var response_body: std.Io.Writer.Allocating = .init(allocator);
    defer response_body.deinit();
    const start = std.Io.Clock.awake.now(client.io);
    const result = client.http.fetch(.{
        .location = .{ .uri = uri },
        .method = std.http.Method.POST,
        .extra_headers = operation_headers.items,
        .payload = payload,
        .response_writer = &response_body.writer,
    }) catch |err| {
        if (client.http_observer) |obs| {
            if (obs.onError) |cb| cb(obs.ctx, std.http.Method.POST, uri_buf.written(), @errorName(err));
        }
        return err;
    };
    const elapsed_ns = @as(u64, @intCast(start.untilNow(client.io, .awake).nanoseconds));

    const body = try response_body.toOwnedSlice();

    if (client.http_observer) |obs| {
        if (obs.onResponse) |cb| cb(obs.ctx, std.http.Method.POST, uri_buf.written(), result.status, &.{}, body, elapsed_ns);
    }

    return .{
        .allocator = allocator,
        .status = result.status,
        .body = body,
    };
}

pub fn reposUploadReleaseAssetResult(client: *Client, owner: []const u8, repo: []const u8, release_id: i64, name: []const u8, label: ?[]const u8, requestBody: []const u8) !ApiResult(models.@"release-asset") {
    return parseRawResponse(models.@"release-asset", try reposUploadReleaseAssetRaw(client, owner, repo, release_id, name, label, requestBody));
}

/////////////////
// Summary:
// List organization repositories
//
// Description:
// Lists repositories for the specified organization.
//
// > [!NOTE]
// > In order to see the `security_and_analysis` block for a repository you must have admin permissions for the repository or be an owner or security manager for the organization that owns the repository. For more information, see "[Managing security managers in your organization](https://docs.github.com/organizations/managing-peoples-access-to-your-organization-with-roles/managing-security-managers-in-your-organization)."
//
pub fn reposListForOrg(client: *Client, org: []const u8, @"type": ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try reposListForOrgResult(client, org, @"type", sort, direction, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListForOrgRaw(client: *Client, org: []const u8, @"type": ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/orgs/{s}/repos", .{ client.base_url, org });
    var first_query = true;
    if (@"type") |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "type", value);
    }
    if (sort) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "sort", value);
    }
    if (direction) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "direction", value);
    }
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListForOrgResult(client: *Client, org: []const u8, @"type": ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposListForOrgRaw(client, org, @"type", sort, direction, per_page, page));
}

/////////////////
// Summary:
// Create an organization repository
//
// Description:
// Creates a new repository in the specified organization. The authenticated user must be a member of the organization.
//
// OAuth app tokens and personal access tokens (classic) need the `public_repo` or `repo` scope to create a public repository, and `repo` scope to create a private repository.
//
pub fn reposCreateInOrg(client: *Client, org: []const u8, requestBody: std.json.Value) !Owned(models.@"full-repository") {
    var result = try reposCreateInOrgResult(client, org, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposCreateInOrgRaw(client: *Client, org: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/orgs/{s}/repos", .{ client.base_url, org });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposCreateInOrgResult(client: *Client, org: []const u8, requestBody: std.json.Value) !ApiResult(models.@"full-repository") {
    return parseRawResponse(models.@"full-repository", try reposCreateInOrgRaw(client, org, requestBody));
}

/////////////////
// Summary:
// Create a GitHub Pages deployment
//
// Description:
// Create a GitHub Pages deployment for a repository.
//
// The authenticated user must have write permission to the repository.
//
pub fn reposCreatePagesDeployment(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.@"page-deployment") {
    var result = try reposCreatePagesDeploymentResult(client, owner, repo, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposCreatePagesDeploymentRaw(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pages/deployments", .{ client.base_url, owner, repo });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposCreatePagesDeploymentResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.@"page-deployment") {
    return parseRawResponse(models.@"page-deployment", try reposCreatePagesDeploymentRaw(client, owner, repo, requestBody));
}

/////////////////
// Summary:
// List repositories for the authenticated user
//
// Description:
// Lists repositories that the authenticated user has explicit permission (`:read`, `:write`, or `:admin`) to access.
//
// The authenticated user has explicit permission to access repositories they own, repositories where they are a collaborator, and repositories that they can access through an organization membership.
//
pub fn reposListForAuthenticatedUser(client: *Client, visibility: ?[]const u8, affiliation: ?[]const u8, @"type": ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, per_page: ?i64, page: ?i64, since: ?[]const u8, before: ?[]const u8) !Owned([]const std.json.Value) {
    var result = try reposListForAuthenticatedUserResult(client, visibility, affiliation, @"type", sort, direction, per_page, page, since, before);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListForAuthenticatedUserRaw(client: *Client, visibility: ?[]const u8, affiliation: ?[]const u8, @"type": ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, per_page: ?i64, page: ?i64, since: ?[]const u8, before: ?[]const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/user/repos", .{client.base_url});
    var first_query = true;
    if (visibility) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "visibility", value);
    }
    if (affiliation) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "affiliation", value);
    }
    if (@"type") |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "type", value);
    }
    if (sort) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "sort", value);
    }
    if (direction) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "direction", value);
    }
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    if (since) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "since", value);
    }
    if (before) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "before", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListForAuthenticatedUserResult(client: *Client, visibility: ?[]const u8, affiliation: ?[]const u8, @"type": ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, per_page: ?i64, page: ?i64, since: ?[]const u8, before: ?[]const u8) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposListForAuthenticatedUserRaw(client, visibility, affiliation, @"type", sort, direction, per_page, page, since, before));
}

/////////////////
// Summary:
// Create a repository for the authenticated user
//
// Description:
// Creates a new repository for the authenticated user.
//
// OAuth app tokens and personal access tokens (classic) need the `public_repo` or `repo` scope to create a public repository, and `repo` scope to create a private repository.
//
pub fn reposCreateForAuthenticatedUser(client: *Client, requestBody: std.json.Value) !Owned(models.@"full-repository") {
    var result = try reposCreateForAuthenticatedUserResult(client, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposCreateForAuthenticatedUserRaw(client: *Client, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/user/repos", .{client.base_url});

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposCreateForAuthenticatedUserResult(client: *Client, requestBody: std.json.Value) !ApiResult(models.@"full-repository") {
    return parseRawResponse(models.@"full-repository", try reposCreateForAuthenticatedUserRaw(client, requestBody));
}

/////////////////
// Summary:
// List commit statuses for a reference
//
// Description:
// Users with pull access in a repository can view commit statuses for a given ref. The ref can be a SHA, a branch name, or a tag name. Statuses are returned in reverse chronological order. The first status in the list will be the latest one.
//
// This resource is also available via a legacy route: `GET /repos/:owner/:repo/statuses/:ref`.
//
pub fn reposListCommitStatusesForRef(client: *Client, owner: []const u8, repo: []const u8, ref: []const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try reposListCommitStatusesForRefResult(client, owner, repo, ref, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListCommitStatusesForRefRaw(client: *Client, owner: []const u8, repo: []const u8, ref: []const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/commits/{s}/statuses", .{ client.base_url, owner, repo, ref });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListCommitStatusesForRefResult(client: *Client, owner: []const u8, repo: []const u8, ref: []const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposListCommitStatusesForRefRaw(client, owner, repo, ref, per_page, page));
}

/////////////////
// Summary:
// Get a branch
//
// Description:
//
//
pub fn reposGetBranch(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !Owned(models.@"branch-with-protection") {
    var result = try reposGetBranchResult(client, owner, repo, branch);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetBranchRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}", .{ client.base_url, owner, repo, branch });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetBranchResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !ApiResult(models.@"branch-with-protection") {
    return parseRawResponse(models.@"branch-with-protection", try reposGetBranchRaw(client, owner, repo, branch));
}

/////////////////
// Summary:
// Get a deployment status
//
// Description:
// Users with pull access can view a deployment status for a deployment:
//
pub fn reposGetDeploymentStatus(client: *Client, owner: []const u8, repo: []const u8, deployment_id: i64, status_id: i64) !Owned(models.@"deployment-status") {
    var result = try reposGetDeploymentStatusResult(client, owner, repo, deployment_id, status_id);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetDeploymentStatusRaw(client: *Client, owner: []const u8, repo: []const u8, deployment_id: i64, status_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/deployments/{d}/statuses/{d}", .{ client.base_url, owner, repo, deployment_id, status_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetDeploymentStatusResult(client: *Client, owner: []const u8, repo: []const u8, deployment_id: i64, status_id: i64) !ApiResult(models.@"deployment-status") {
    return parseRawResponse(models.@"deployment-status", try reposGetDeploymentStatusRaw(client, owner, repo, deployment_id, status_id));
}

/////////////////
// Summary:
// Check if a user can be assigned to a issue
//
// Description:
// Checks if a user has permission to be assigned to a specific issue.
//
// If the `assignee` can be assigned to this issue, a `204` status code with no content is returned.
//
// Otherwise a `404` status code is returned.
//
pub fn issuesCheckUserCanBeAssignedToIssue(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, assignee: []const u8) !void {
    var raw = try issuesCheckUserCanBeAssignedToIssueRaw(client, owner, repo, issue_number, assignee);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn issuesCheckUserCanBeAssignedToIssueRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, assignee: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}/assignees/{s}", .{ client.base_url, owner, repo, issue_number, assignee });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// List custom deployment rule integrations available for an environment
//
// Description:
// Gets all custom deployment protection rule integrations that are available for an environment.
//
// The authenticated user must have admin or owner permissions to the repository to use this endpoint.
//
// For more information about environments, see "[Using environments for deployment](https://docs.github.com/actions/deployment/targeting-different-environments/using-environments-for-deployment)."
//
// For more information about the app that is providing this custom deployment rule, see "[GET an app](https://docs.github.com/rest/apps/apps#get-an-app)".
//
// OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
//
pub fn reposListCustomDeploymentRuleIntegrations(client: *Client, environment_name: []const u8, repo: []const u8, owner: []const u8, page: ?i64, per_page: ?i64) !Owned(std.json.Value) {
    var result = try reposListCustomDeploymentRuleIntegrationsResult(client, environment_name, repo, owner, page, per_page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListCustomDeploymentRuleIntegrationsRaw(client: *Client, environment_name: []const u8, repo: []const u8, owner: []const u8, page: ?i64, per_page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/environments/{s}/deployment_protection_rules/apps", .{ client.base_url, environment_name, repo, owner });
    var first_query = true;
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListCustomDeploymentRuleIntegrationsResult(client: *Client, environment_name: []const u8, repo: []const u8, owner: []const u8, page: ?i64, per_page: ?i64) !ApiResult(std.json.Value) {
    return parseRawResponse(std.json.Value, try reposListCustomDeploymentRuleIntegrationsRaw(client, environment_name, repo, owner, page, per_page));
}

/////////////////
// Summary:
// Remove sub-issue
//
// Description:
// You can use the REST API to remove a sub-issue from an issue.
// Removing content too quickly using this endpoint may result in secondary rate limiting.
// For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)"
// and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
// - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass a specific media type.
// - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn issuesRemoveSubIssue(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !Owned(models.issue) {
    var result = try issuesRemoveSubIssueResult(client, owner, repo, issue_number, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesRemoveSubIssueRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}/sub_issue", .{ client.base_url, owner, repo, issue_number });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesRemoveSubIssueResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !ApiResult(models.issue) {
    return parseRawResponse(models.issue, try issuesRemoveSubIssueRaw(client, owner, repo, issue_number, requestBody));
}

/////////////////
// Summary:
// Decline a repository invitation
//
// Description:
//
//
pub fn reposDeclineInvitationForAuthenticatedUser(client: *Client, invitation_id: i64) !void {
    var raw = try reposDeclineInvitationForAuthenticatedUserRaw(client, invitation_id);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposDeclineInvitationForAuthenticatedUserRaw(client: *Client, invitation_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/user/repository_invitations/{d}", .{ client.base_url, invitation_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Accept a repository invitation
//
// Description:
//
//
pub fn reposAcceptInvitationForAuthenticatedUser(client: *Client, invitation_id: i64) !void {
    var raw = try reposAcceptInvitationForAuthenticatedUserRaw(client, invitation_id);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposAcceptInvitationForAuthenticatedUserRaw(client: *Client, invitation_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/user/repository_invitations/{d}", .{ client.base_url, invitation_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PATCH, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Get a commit
//
// Description:
// Returns the contents of a single commit reference. You must have `read` access for the repository to use this endpoint.
//
// > [!NOTE]
// > If there are more than 300 files in the commit diff and the default JSON media type is requested, the response will include pagination link headers for the remaining files, up to a limit of 3000 files. Each page contains the static commit information, and the only changes are to the file listing.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)." Pagination query parameters are not supported for these media types.
//
// - **`application/vnd.github.diff`**: Returns the diff of the commit. Larger diffs may time out and return a 5xx status code.
// - **`application/vnd.github.patch`**: Returns the patch of the commit. Diffs with binary data will have no `patch` property. Larger diffs may time out and return a 5xx status code.
// - **`application/vnd.github.sha`**: Returns the commit's SHA-1 hash. You can use this endpoint to check if a remote reference's SHA-1 hash is the same as your local reference's SHA-1 hash by providing the local SHA-1 reference as the ETag.
//
// **Signature verification object**
//
// The response will include a `verification` object that describes the result of verifying the commit's signature. The following fields are included in the `verification` object:
//
// | Name | Type | Description |
// | ---- | ---- | ----------- |
// | `verified` | `boolean` | Indicates whether GitHub considers the signature in this commit to be verified. |
// | `reason` | `string` | The reason for verified value. Possible values and their meanings are enumerated in table below. |
// | `signature` | `string` | The signature that was extracted from the commit. |
// | `payload` | `string` | The value that was signed. |
// | `verified_at` | `string` | The date the signature was verified by GitHub. |
//
// These are the possible values for `reason` in the `verification` object:
//
// | Value | Description |
// | ----- | ----------- |
// | `expired_key` | The key that made the signature is expired. |
// | `not_signing_key` | The "signing" flag is not among the usage flags in the GPG key that made the signature. |
// | `gpgverify_error` | There was an error communicating with the signature verification service. |
// | `gpgverify_unavailable` | The signature verification service is currently unavailable. |
// | `unsigned` | The object does not include a signature. |
// | `unknown_signature_type` | A non-PGP signature was found in the commit. |
// | `no_user` | No user was associated with the `committer` email address in the commit. |
// | `unverified_email` | The `committer` email address in the commit was associated with a user, but the email address is not verified on their account. |
// | `bad_email` | The `committer` email address in the commit is not included in the identities of the PGP key that made the signature. |
// | `unknown_key` | The key that made the signature has not been registered with any user's account. |
// | `malformed_signature` | There was an error parsing the signature. |
// | `invalid` | The signature could not be cryptographically verified using the key whose key-id was found in the signature. |
// | `valid` | None of the above errors applied, so the signature is considered to be verified. |
//
pub fn reposGetCommit(client: *Client, owner: []const u8, repo: []const u8, page: ?i64, per_page: ?i64, ref: []const u8) !Owned(models.commit) {
    var result = try reposGetCommitResult(client, owner, repo, page, per_page, ref);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetCommitRaw(client: *Client, owner: []const u8, repo: []const u8, page: ?i64, per_page: ?i64, ref: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/commits/{s}", .{ client.base_url, owner, repo, ref });
    var first_query = true;
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetCommitResult(client: *Client, owner: []const u8, repo: []const u8, page: ?i64, per_page: ?i64, ref: []const u8) !ApiResult(models.commit) {
    return parseRawResponse(models.commit, try reposGetCommitRaw(client, owner, repo, page, per_page, ref));
}

/////////////////
// Summary:
// Get an autolink reference of a repository
//
// Description:
// This returns a single autolink reference by ID that was configured for the given repository.
//
// Information about autolinks are only available to repository administrators.
//
pub fn reposGetAutolink(client: *Client, owner: []const u8, repo: []const u8, autolink_id: i64) !Owned(models.autolink) {
    var result = try reposGetAutolinkResult(client, owner, repo, autolink_id);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetAutolinkRaw(client: *Client, owner: []const u8, repo: []const u8, autolink_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/autolinks/{d}", .{ client.base_url, owner, repo, autolink_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetAutolinkResult(client: *Client, owner: []const u8, repo: []const u8, autolink_id: i64) !ApiResult(models.autolink) {
    return parseRawResponse(models.autolink, try reposGetAutolinkRaw(client, owner, repo, autolink_id));
}

/////////////////
// Summary:
// Delete an autolink reference from a repository
//
// Description:
// This deletes a single autolink reference by ID that was configured for the given repository.
//
// Information about autolinks are only available to repository administrators.
//
pub fn reposDeleteAutolink(client: *Client, owner: []const u8, repo: []const u8, autolink_id: i64) !void {
    var raw = try reposDeleteAutolinkRaw(client, owner, repo, autolink_id);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposDeleteAutolinkRaw(client: *Client, owner: []const u8, repo: []const u8, autolink_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/autolinks/{d}", .{ client.base_url, owner, repo, autolink_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Get parent issue
//
// Description:
// You can use the REST API to get the parent issue of a sub-issue.
//
// This endpoint supports the following custom media types. For more information, see [Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types).
//
// - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn issuesGetParent(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64) !Owned(models.issue) {
    var result = try issuesGetParentResult(client, owner, repo, issue_number);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesGetParentRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}/parent", .{ client.base_url, owner, repo, issue_number });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesGetParentResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64) !ApiResult(models.issue) {
    return parseRawResponse(models.issue, try issuesGetParentRaw(client, owner, repo, issue_number));
}

/////////////////
// Summary:
// List review comments on a pull request
//
// Description:
// Lists all review comments for a specified pull request. By default, review comments
// are in ascending order by ID.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn pullsListReviewComments(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try pullsListReviewCommentsResult(client, owner, repo, pull_number, sort, direction, since, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullsListReviewCommentsRaw(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pulls/{d}/comments", .{ client.base_url, owner, repo, pull_number });
    var first_query = true;
    if (sort) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "sort", value);
    }
    if (direction) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "direction", value);
    }
    if (since) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "since", value);
    }
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullsListReviewCommentsResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try pullsListReviewCommentsRaw(client, owner, repo, pull_number, sort, direction, since, per_page, page));
}

/////////////////
// Summary:
// Create a review comment for a pull request
//
// Description:
// Creates a review comment on the diff of a specified pull request. To add a regular comment to a pull request timeline, see "[Create an issue comment](https://docs.github.com/rest/issues/comments#create-an-issue-comment)."
//
// If your comment applies to more than one line in the pull request diff, you should use the parameters `line`, `side`, and optionally `start_line` and `start_side` in your request.
//
// The `position` parameter is closing down. If you use `position`, the `line`, `side`, `start_line`, and `start_side` parameters are not required.
//
// This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating content too quickly using this endpoint may result in secondary rate limiting. For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)"
// and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn pullsCreateReviewComment(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !Owned(models.@"pull-request-review-comment") {
    var result = try pullsCreateReviewCommentResult(client, owner, repo, pull_number, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullsCreateReviewCommentRaw(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pulls/{d}/comments", .{ client.base_url, owner, repo, pull_number });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullsCreateReviewCommentResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !ApiResult(models.@"pull-request-review-comment") {
    return parseRawResponse(models.@"pull-request-review-comment", try pullsCreateReviewCommentRaw(client, owner, repo, pull_number, requestBody));
}

/////////////////
// Summary:
// List releases
//
// Description:
// This returns a list of releases, which does not include regular Git tags that have not been associated with a release. To get a list of Git tags, use the [Repository Tags API](https://docs.github.com/rest/repos/repos#list-repository-tags).
//
// Information about published releases are available to everyone. Only users with push access will receive listings for draft releases.
//
pub fn reposListReleases(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try reposListReleasesResult(client, owner, repo, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListReleasesRaw(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/releases", .{ client.base_url, owner, repo });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListReleasesResult(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposListReleasesRaw(client, owner, repo, per_page, page));
}

/////////////////
// Summary:
// Create a release
//
// Description:
// Users with push access to the repository can create a release.
//
// > [!NOTE]
// > If the commit identified by `target_commitish` (or, when `target_commitish` is omitted, the latest commit on the default branch) adds or modifies any file under `.github/workflows/` relative to the repository's default branch, the authenticating token must be authorized to modify workflows. Otherwise, this endpoint returns `404 Not Found`; some authentication paths surface `403 Resource not accessible by integration` instead.
//
// OAuth app tokens and personal access tokens (classic) need the `workflow` scope when the resolved target commit modifies workflow files. Fine-grained access tokens and GitHub App installation tokens also need the "Workflows" repository permission (write). The `GITHUB_TOKEN` available to GitHub Actions cannot be authorized for this; for more information, see "[Automatic token authentication](https://docs.github.com/actions/security-guides/automatic-token-authentication#permissions-for-the-github_token)".
//
// This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating content too quickly using this endpoint may result in secondary rate limiting. For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)" and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
//
pub fn reposCreateRelease(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.release) {
    var result = try reposCreateReleaseResult(client, owner, repo, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposCreateReleaseRaw(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/releases", .{ client.base_url, owner, repo });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposCreateReleaseResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.release) {
    return parseRawResponse(models.release, try reposCreateReleaseRaw(client, owner, repo, requestBody));
}

/////////////////
// Summary:
// Get all contributor commit activity
//
// Description:
//
// Returns the `total` number of commits authored by the contributor. In addition, the response includes a Weekly Hash (`weeks` array) with the following information:
//
// *   `w` - Start of the week, given as a [Unix timestamp](https://en.wikipedia.org/wiki/Unix_time).
// *   `a` - Number of additions
// *   `d` - Number of deletions
// *   `c` - Number of commits
//
// > [!NOTE]
// > This endpoint will return `0` values for all addition and deletion counts in repositories with 10,000 or more commits.
//
pub fn reposGetContributorsStats(client: *Client, owner: []const u8, repo: []const u8) !Owned([]const std.json.Value) {
    var result = try reposGetContributorsStatsResult(client, owner, repo);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetContributorsStatsRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/stats/contributors", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetContributorsStatsResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposGetContributorsStatsRaw(client, owner, repo));
}

/////////////////
// Summary:
// Get an organization rule suite
//
// Description:
// Gets information about a suite of rule evaluations from within an organization.
// For more information, see "[Managing rulesets for repositories in your organization](https://docs.github.com/organizations/managing-organization-settings/managing-rulesets-for-repositories-in-your-organization#viewing-insights-for-rulesets)."
//
pub fn reposGetOrgRuleSuite(client: *Client, org: []const u8, rule_suite_id: i64) !Owned(models.@"rule-suite") {
    var result = try reposGetOrgRuleSuiteResult(client, org, rule_suite_id);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetOrgRuleSuiteRaw(client: *Client, org: []const u8, rule_suite_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/orgs/{s}/rulesets/rule-suites/{d}", .{ client.base_url, org, rule_suite_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetOrgRuleSuiteResult(client: *Client, org: []const u8, rule_suite_id: i64) !ApiResult(models.@"rule-suite") {
    return parseRawResponse(models.@"rule-suite", try reposGetOrgRuleSuiteRaw(client, org, rule_suite_id));
}

/////////////////
// Summary:
// Get all repository topics
//
// Description:
//
//
pub fn reposGetAllTopics(client: *Client, owner: []const u8, repo: []const u8, page: ?i64, per_page: ?i64) !Owned(models.topic) {
    var result = try reposGetAllTopicsResult(client, owner, repo, page, per_page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetAllTopicsRaw(client: *Client, owner: []const u8, repo: []const u8, page: ?i64, per_page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/topics", .{ client.base_url, owner, repo });
    var first_query = true;
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetAllTopicsResult(client: *Client, owner: []const u8, repo: []const u8, page: ?i64, per_page: ?i64) !ApiResult(models.topic) {
    return parseRawResponse(models.topic, try reposGetAllTopicsRaw(client, owner, repo, page, per_page));
}

/////////////////
// Summary:
// Replace all repository topics
//
// Description:
//
//
pub fn reposReplaceAllTopics(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.topic) {
    var result = try reposReplaceAllTopicsResult(client, owner, repo, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposReplaceAllTopicsRaw(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/topics", .{ client.base_url, owner, repo });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PUT, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposReplaceAllTopicsResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.topic) {
    return parseRawResponse(models.topic, try reposReplaceAllTopicsRaw(client, owner, repo, requestBody));
}

/////////////////
// Summary:
// Check if private vulnerability reporting is enabled for a repository
//
// Description:
// Returns a boolean indicating whether or not private vulnerability reporting is enabled for the repository. For more information, see "[Evaluating the security settings of a repository](https://docs.github.com/code-security/security-advisories/working-with-repository-security-advisories/evaluating-the-security-settings-of-a-repository)".
//
pub fn reposCheckPrivateVulnerabilityReporting(client: *Client, owner: []const u8, repo: []const u8) !Owned(std.json.Value) {
    var result = try reposCheckPrivateVulnerabilityReportingResult(client, owner, repo);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposCheckPrivateVulnerabilityReportingRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/private-vulnerability-reporting", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposCheckPrivateVulnerabilityReportingResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult(std.json.Value) {
    return parseRawResponse(std.json.Value, try reposCheckPrivateVulnerabilityReportingRaw(client, owner, repo));
}

/////////////////
// Summary:
// Enable private vulnerability reporting for a repository
//
// Description:
// Enables private vulnerability reporting for a repository. The authenticated user must have admin access to the repository. For more information, see "[Privately reporting a security vulnerability](https://docs.github.com/code-security/security-advisories/guidance-on-reporting-and-writing/privately-reporting-a-security-vulnerability)."
//
pub fn reposEnablePrivateVulnerabilityReporting(client: *Client, owner: []const u8, repo: []const u8) !void {
    var raw = try reposEnablePrivateVulnerabilityReportingRaw(client, owner, repo);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposEnablePrivateVulnerabilityReportingRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/private-vulnerability-reporting", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PUT, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Disable private vulnerability reporting for a repository
//
// Description:
// Disables private vulnerability reporting for a repository. The authenticated user must have admin access to the repository. For more information, see "[Privately reporting a security vulnerability](https://docs.github.com/code-security/security-advisories/guidance-on-reporting-and-writing/privately-reporting-a-security-vulnerability)".
//
pub fn reposDisablePrivateVulnerabilityReporting(client: *Client, owner: []const u8, repo: []const u8) !void {
    var raw = try reposDisablePrivateVulnerabilityReportingRaw(client, owner, repo);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposDisablePrivateVulnerabilityReportingRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/private-vulnerability-reporting", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// List pull requests associated with a commit
//
// Description:
// Lists the merged pull request that introduced the commit to the repository. If the commit is not present in the default branch, it will return merged and open pull requests associated with the commit.
//
// To list the open or merged pull requests associated with a branch, you can set the `commit_sha` parameter to the branch name.
//
pub fn reposListPullRequestsAssociatedWithCommit(client: *Client, owner: []const u8, repo: []const u8, commit_sha: []const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try reposListPullRequestsAssociatedWithCommitResult(client, owner, repo, commit_sha, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListPullRequestsAssociatedWithCommitRaw(client: *Client, owner: []const u8, repo: []const u8, commit_sha: []const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/commits/{s}/pulls", .{ client.base_url, owner, repo, commit_sha });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListPullRequestsAssociatedWithCommitResult(client: *Client, owner: []const u8, repo: []const u8, commit_sha: []const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposListPullRequestsAssociatedWithCommitRaw(client, owner, repo, commit_sha, per_page, page));
}

/////////////////
// Summary:
// Check if a user is a repository collaborator
//
// Description:
// For organization-owned repositories, the list of collaborators includes outside collaborators, organization members that are direct collaborators, organization members with access through team memberships, organization members with access through default organization permissions, and organization owners.
//
// Team members will include the members of child teams.
//
// The authenticated user must have push access to the repository to use this endpoint.
//
// OAuth app tokens and personal access tokens (classic) need the `read:org` and `repo` scopes to use this endpoint.
//
pub fn reposCheckCollaborator(client: *Client, owner: []const u8, repo: []const u8, username: []const u8) !void {
    var raw = try reposCheckCollaboratorRaw(client, owner, repo, username);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposCheckCollaboratorRaw(client: *Client, owner: []const u8, repo: []const u8, username: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/collaborators/{s}", .{ client.base_url, owner, repo, username });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Add a repository collaborator
//
// Description:
// Add a user to a repository with a specified level of access. If the repository is owned by an organization, this API does not add the user to the organization - a user that has repository access without being an organization member is called an "outside collaborator" (if they are not an Enterprise Managed User) or a "repository collaborator" if they are an Enterprise Managed User. These users are exempt from some organization policies - see "[Adding outside collaborators to repositories](https://docs.github.com/organizations/managing-user-access-to-your-organizations-repositories/managing-outside-collaborators/adding-outside-collaborators-to-repositories-in-your-organization)" to learn more about these collaborator types.
//
// This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications).
//
// Adding an outside collaborator may be restricted by enterprise and organization administrators. For more information, see "[Enforcing repository management policies in your enterprise](https://docs.github.com/admin/policies/enforcing-policies-for-your-enterprise/enforcing-repository-management-policies-in-your-enterprise#enforcing-a-policy-for-inviting-outside-collaborators-to-repositories)" and "[Setting permissions for adding outside collaborators](https://docs.github.com/organizations/managing-organization-settings/setting-permissions-for-adding-outside-collaborators)" for organization settings.
//
// For more information on permission levels, see "[Repository permission levels for an organization](https://docs.github.com/github/setting-up-and-managing-organizations-and-teams/repository-permission-levels-for-an-organization#permission-levels-for-repositories-owned-by-an-organization)". There are restrictions on which permissions can be granted to organization members when an organization base role is in place. In this case, the role being given must be equal to or higher than the org base permission. Otherwise, the request will fail with:
//
// ```
// Cannot assign {member} permission of {role name}
// ```
//
// Note that, if you choose not to pass any parameters, you'll need to set `Content-Length` to zero when calling out to this endpoint. For more information, see "[HTTP method](https://docs.github.com/rest/guides/getting-started-with-the-rest-api#http-method)."
//
// The invitee will receive a notification that they have been invited to the repository, which they must accept or decline. They may do this via the notifications page, the email they receive, or by using the [API](https://docs.github.com/rest/collaborators/invitations).
//
// For Enterprise Managed Users, this endpoint does not send invitations - these users are automatically added to organizations and repositories. Enterprise Managed Users can only be added to organizations and repositories within their enterprise.
//
// **Updating an existing collaborator's permission level**
//
// The endpoint can also be used to change the permissions of an existing collaborator without first removing and re-adding the collaborator. To change the permissions, use the same endpoint and pass a different `permission` parameter. The response will be a `204`, with no other indication that the permission level changed.
//
// **Rate limits**
//
// You are limited to sending 50 invitations to a repository per 24 hour period. Note there is no limit if you are inviting organization members to an organization repository.
//
pub fn reposAddCollaborator(client: *Client, owner: []const u8, repo: []const u8, username: []const u8, requestBody: std.json.Value) !Owned(models.@"repository-invitation") {
    var result = try reposAddCollaboratorResult(client, owner, repo, username, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposAddCollaboratorRaw(client: *Client, owner: []const u8, repo: []const u8, username: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/collaborators/{s}", .{ client.base_url, owner, repo, username });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PUT, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposAddCollaboratorResult(client: *Client, owner: []const u8, repo: []const u8, username: []const u8, requestBody: std.json.Value) !ApiResult(models.@"repository-invitation") {
    return parseRawResponse(models.@"repository-invitation", try reposAddCollaboratorRaw(client, owner, repo, username, requestBody));
}

/////////////////
// Summary:
// Remove a repository collaborator
//
// Description:
// Removes a collaborator from a repository.
//
// To use this endpoint, the authenticated user must either be an administrator of the repository or target themselves for removal.
//
// This endpoint also:
// - Cancels any outstanding invitations sent by the collaborator
// - Unassigns the user from any issues
// - Removes access to organization projects if the user is not an organization member and is not a collaborator on any other organization repositories.
// - Unstars the repository
// - Updates access permissions to packages
//
// Removing a user as a collaborator has the following effects on forks:
//  - If the user had access to a fork through their membership to this repository, the user will also be removed from the fork.
//  - If the user had their own fork of the repository, the fork will be deleted.
//  - If the user still has read access to the repository, open pull requests by this user from a fork will be denied.
//
// > [!NOTE]
// > A user can still have access to the repository through organization permissions like base repository permissions.
//
// Although the API responds immediately, the additional permission updates might take some extra time to complete in the background.
//
// For more information on fork permissions, see "[About permissions and visibility of forks](https://docs.github.com/pull-requests/collaborating-with-pull-requests/working-with-forks/about-permissions-and-visibility-of-forks)".
//
pub fn reposRemoveCollaborator(client: *Client, owner: []const u8, repo: []const u8, username: []const u8) !void {
    var raw = try reposRemoveCollaboratorRaw(client, owner, repo, username);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposRemoveCollaboratorRaw(client: *Client, owner: []const u8, repo: []const u8, username: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/collaborators/{s}", .{ client.base_url, owner, repo, username });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// List repository contributors
//
// Description:
// Lists contributors to the specified repository and sorts them by the number of commits per contributor in descending order. This endpoint may return information that is a few hours old because the GitHub REST API caches contributor data to improve performance.
//
// GitHub identifies contributors by author email address. This endpoint groups contribution counts by GitHub user, which includes all associated email addresses. To improve performance, only the first 500 author email addresses in the repository link to GitHub users. The rest will appear as anonymous contributors without associated GitHub user information.
//
pub fn reposListContributors(client: *Client, owner: []const u8, repo: []const u8, anon: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try reposListContributorsResult(client, owner, repo, anon, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListContributorsRaw(client: *Client, owner: []const u8, repo: []const u8, anon: ?[]const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/contributors", .{ client.base_url, owner, repo });
    var first_query = true;
    if (anon) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "anon", value);
    }
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListContributorsResult(client: *Client, owner: []const u8, repo: []const u8, anon: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposListContributorsRaw(client, owner, repo, anon, per_page, page));
}

/////////////////
// Summary:
// List labels for an issue
//
// Description:
// Lists all labels for an issue.
//
pub fn issuesListLabelsOnIssue(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try issuesListLabelsOnIssueResult(client, owner, repo, issue_number, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesListLabelsOnIssueRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}/labels", .{ client.base_url, owner, repo, issue_number });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesListLabelsOnIssueResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try issuesListLabelsOnIssueRaw(client, owner, repo, issue_number, per_page, page));
}

/////////////////
// Summary:
// Add labels to an issue
//
// Description:
// Adds labels to an issue.
//
pub fn issuesAddLabels(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !Owned([]const std.json.Value) {
    var result = try issuesAddLabelsResult(client, owner, repo, issue_number, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesAddLabelsRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}/labels", .{ client.base_url, owner, repo, issue_number });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesAddLabelsResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try issuesAddLabelsRaw(client, owner, repo, issue_number, requestBody));
}

/////////////////
// Summary:
// Set labels for an issue
//
// Description:
// Removes any previous labels and sets the new labels for an issue.
//
pub fn issuesSetLabels(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !Owned([]const std.json.Value) {
    var result = try issuesSetLabelsResult(client, owner, repo, issue_number, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesSetLabelsRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}/labels", .{ client.base_url, owner, repo, issue_number });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PUT, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesSetLabelsResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try issuesSetLabelsRaw(client, owner, repo, issue_number, requestBody));
}

/////////////////
// Summary:
// Remove all labels from an issue
//
// Description:
// Removes all labels from an issue.
//
pub fn issuesRemoveAllLabels(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64) !void {
    var raw = try issuesRemoveAllLabelsRaw(client, owner, repo, issue_number);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn issuesRemoveAllLabelsRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}/labels", .{ client.base_url, owner, repo, issue_number });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Remove a label from an issue
//
// Description:
// Removes the specified label from the issue, and returns the remaining labels on the issue. This endpoint returns a `404 Not Found` status if the label does not exist.
//
pub fn issuesRemoveLabel(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, name: []const u8) !Owned([]const std.json.Value) {
    var result = try issuesRemoveLabelResult(client, owner, repo, issue_number, name);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesRemoveLabelRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, name: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}/labels/{s}", .{ client.base_url, owner, repo, issue_number, name });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesRemoveLabelResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, name: []const u8) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try issuesRemoveLabelRaw(client, owner, repo, issue_number, name));
}

/////////////////
// Summary:
// Merge a branch
//
// Description:
//
//
pub fn reposMerge(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.commit) {
    var result = try reposMergeResult(client, owner, repo, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposMergeRaw(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/merges", .{ client.base_url, owner, repo });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposMergeResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.commit) {
    return parseRawResponse(models.commit, try reposMergeRaw(client, owner, repo, requestBody));
}

/////////////////
// Summary:
// Get the latest release
//
// Description:
// View the latest published full release for the repository.
//
// The latest release is the most recent non-prerelease, non-draft release, sorted by the `created_at` attribute. The `created_at` attribute is the date of the commit used for the release, and not the date when the release was drafted or published.
//
pub fn reposGetLatestRelease(client: *Client, owner: []const u8, repo: []const u8) !Owned(models.release) {
    var result = try reposGetLatestReleaseResult(client, owner, repo);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetLatestReleaseRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/releases/latest", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetLatestReleaseResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult(models.release) {
    return parseRawResponse(models.release, try reposGetLatestReleaseRaw(client, owner, repo));
}

/////////////////
// Summary:
// List GitHub Pages builds
//
// Description:
// Lists builts of a GitHub Pages site.
//
// OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
//
pub fn reposListPagesBuilds(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try reposListPagesBuildsResult(client, owner, repo, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListPagesBuildsRaw(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pages/builds", .{ client.base_url, owner, repo });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListPagesBuildsResult(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposListPagesBuildsRaw(client, owner, repo, per_page, page));
}

/////////////////
// Summary:
// Request a GitHub Pages build
//
// Description:
// You can request that your site be built from the latest revision on the default branch. This has the same effect as pushing a commit to your default branch, but does not require an additional commit. Manually triggering page builds can be helpful when diagnosing build warnings and failures.
//
// Build requests are limited to one concurrent build per repository and one concurrent build per requester. If you request a build while another is still in progress, the second request will be queued until the first completes.
//
pub fn reposRequestPagesBuild(client: *Client, owner: []const u8, repo: []const u8) !Owned(models.@"page-build-status") {
    var result = try reposRequestPagesBuildResult(client, owner, repo);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposRequestPagesBuildRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pages/builds", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposRequestPagesBuildResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult(models.@"page-build-status") {
    return parseRawResponse(models.@"page-build-status", try reposRequestPagesBuildRaw(client, owner, repo));
}

/////////////////
// Summary:
// Get an environment
//
// Description:
// > [!NOTE]
// > To get information about name patterns that branches must match in order to deploy to this environment, see "[Get a deployment branch policy](/rest/deployments/branch-policies#get-a-deployment-branch-policy)."
//
// Anyone with read access to the repository can use this endpoint.
//
// OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
//
pub fn reposGetEnvironment(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8) !Owned(models.environment) {
    var result = try reposGetEnvironmentResult(client, owner, repo, environment_name);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetEnvironmentRaw(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/environments/{s}", .{ client.base_url, owner, repo, environment_name });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetEnvironmentResult(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8) !ApiResult(models.environment) {
    return parseRawResponse(models.environment, try reposGetEnvironmentRaw(client, owner, repo, environment_name));
}

/////////////////
// Summary:
// Create or update an environment
//
// Description:
// Create or update an environment with protection rules, such as required reviewers. For more information about environment protection rules, see "[Environments](/actions/reference/environments#environment-protection-rules)."
//
// > [!NOTE]
// > To create or update name patterns that branches must match in order to deploy to this environment, see "[Deployment branch policies](/rest/deployments/branch-policies)."
//
// > [!NOTE]
// > To create or update secrets for an environment, see "[GitHub Actions secrets](/rest/actions/secrets)."
//
// OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
//
pub fn reposCreateOrUpdateEnvironment(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, requestBody: std.json.Value) !Owned(models.environment) {
    var result = try reposCreateOrUpdateEnvironmentResult(client, owner, repo, environment_name, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposCreateOrUpdateEnvironmentRaw(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/environments/{s}", .{ client.base_url, owner, repo, environment_name });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PUT, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposCreateOrUpdateEnvironmentResult(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, requestBody: std.json.Value) !ApiResult(models.environment) {
    return parseRawResponse(models.environment, try reposCreateOrUpdateEnvironmentRaw(client, owner, repo, environment_name, requestBody));
}

/////////////////
// Summary:
// Delete an environment
//
// Description:
// OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
//
pub fn reposDeleteAnEnvironment(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8) !void {
    var raw = try reposDeleteAnEnvironmentRaw(client, owner, repo, environment_name);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposDeleteAnEnvironmentRaw(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/environments/{s}", .{ client.base_url, owner, repo, environment_name });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// List forks
//
// Description:
//
//
pub fn reposListForks(client: *Client, owner: []const u8, repo: []const u8, sort: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try reposListForksResult(client, owner, repo, sort, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListForksRaw(client: *Client, owner: []const u8, repo: []const u8, sort: ?[]const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/forks", .{ client.base_url, owner, repo });
    var first_query = true;
    if (sort) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "sort", value);
    }
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListForksResult(client: *Client, owner: []const u8, repo: []const u8, sort: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposListForksRaw(client, owner, repo, sort, per_page, page));
}

/////////////////
// Summary:
// Create a fork
//
// Description:
// Create a fork for the authenticated user.
//
// > [!NOTE]
// > Forking a Repository happens asynchronously. You may have to wait a short period of time before you can access the git objects. If this takes longer than 5 minutes, be sure to contact [GitHub Support](https://support.github.com/contact?tags=dotcom-rest-api).
//
// > [!NOTE]
// > Although this endpoint works with GitHub Apps, the GitHub App must be installed on the destination account with access to all repositories and on the source account with access to the source repository.
//
pub fn reposCreateFork(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.@"full-repository") {
    var result = try reposCreateForkResult(client, owner, repo, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposCreateForkRaw(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/forks", .{ client.base_url, owner, repo });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposCreateForkResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.@"full-repository") {
    return parseRawResponse(models.@"full-repository", try reposCreateForkRaw(client, owner, repo, requestBody));
}

/////////////////
// Summary:
// Get top referral sources
//
// Description:
// Get the top 10 referrers over the last 14 days.
//
pub fn reposGetTopReferrers(client: *Client, owner: []const u8, repo: []const u8) !Owned([]const std.json.Value) {
    var result = try reposGetTopReferrersResult(client, owner, repo);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetTopReferrersRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/traffic/popular/referrers", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetTopReferrersResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposGetTopReferrersRaw(client, owner, repo));
}

/////////////////
// Summary:
// List repository languages
//
// Description:
// Lists languages for the specified repository. The value shown for each language is the number of bytes of code written in that language.
//
pub fn reposListLanguages(client: *Client, owner: []const u8, repo: []const u8) !Owned(models.language) {
    var result = try reposListLanguagesResult(client, owner, repo);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListLanguagesRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/languages", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListLanguagesResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult(models.language) {
    return parseRawResponse(models.language, try reposListLanguagesRaw(client, owner, repo));
}

/////////////////
// Summary:
// Generate release notes content for a release
//
// Description:
// Generate a name and body describing a [release](https://docs.github.com/rest/releases/releases#get-a-release). The body content will be markdown formatted and contain information like the changes since last release and users who contributed. The generated release notes are not saved anywhere. They are intended to be generated and used when creating a new release.
//
pub fn reposGenerateReleaseNotes(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.@"release-notes-content") {
    var result = try reposGenerateReleaseNotesResult(client, owner, repo, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGenerateReleaseNotesRaw(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/releases/generate-notes", .{ client.base_url, owner, repo });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGenerateReleaseNotesResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.@"release-notes-content") {
    return parseRawResponse(models.@"release-notes-content", try reposGenerateReleaseNotesRaw(client, owner, repo, requestBody));
}

/////////////////
// Summary:
// Create an attestation
//
// Description:
// Store an artifact attestation and associate it with a repository.
//
// The authenticated user must have write permission to the repository and, if using a fine-grained access token, the `attestations:write` permission is required.
//
// Artifact attestations are meant to be created using the [attest action](https://github.com/actions/attest). For more information, see our guide on [using artifact attestations to establish a build's provenance](https://docs.github.com/actions/security-guides/using-artifact-attestations-to-establish-provenance-for-builds).
//
pub fn reposCreateAttestation(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(std.json.Value) {
    var result = try reposCreateAttestationResult(client, owner, repo, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposCreateAttestationRaw(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/attestations", .{ client.base_url, owner, repo });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposCreateAttestationResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(std.json.Value) {
    return parseRawResponse(std.json.Value, try reposCreateAttestationRaw(client, owner, repo, requestBody));
}

/////////////////
// Summary:
// List organization rule suites
//
// Description:
// Lists suites of rule evaluations at the organization level.
// For more information, see "[Managing rulesets for repositories in your organization](https://docs.github.com/organizations/managing-organization-settings/managing-rulesets-for-repositories-in-your-organization#viewing-insights-for-rulesets)."
//
pub fn reposGetOrgRuleSuites(client: *Client, org: []const u8, ref: ?[]const u8, repository_name: ?[]const u8, time_period: ?[]const u8, actor_name: ?[]const u8, rule_suite_result: ?[]const u8, evaluate_status: ?[]const u8, per_page: ?i64, page: ?i64) !Owned(models.@"rule-suites") {
    var result = try reposGetOrgRuleSuitesResult(client, org, ref, repository_name, time_period, actor_name, rule_suite_result, evaluate_status, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetOrgRuleSuitesRaw(client: *Client, org: []const u8, ref: ?[]const u8, repository_name: ?[]const u8, time_period: ?[]const u8, actor_name: ?[]const u8, rule_suite_result: ?[]const u8, evaluate_status: ?[]const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/orgs/{s}/rulesets/rule-suites", .{ client.base_url, org });
    var first_query = true;
    if (ref) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "ref", value);
    }
    if (repository_name) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "repository_name", value);
    }
    if (time_period) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "time_period", value);
    }
    if (actor_name) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "actor_name", value);
    }
    if (rule_suite_result) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "rule_suite_result", value);
    }
    if (evaluate_status) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "evaluate_status", value);
    }
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetOrgRuleSuitesResult(client: *Client, org: []const u8, ref: ?[]const u8, repository_name: ?[]const u8, time_period: ?[]const u8, actor_name: ?[]const u8, rule_suite_result: ?[]const u8, evaluate_status: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult(models.@"rule-suites") {
    return parseRawResponse(models.@"rule-suites", try reposGetOrgRuleSuitesRaw(client, org, ref, repository_name, time_period, actor_name, rule_suite_result, evaluate_status, per_page, page));
}

/////////////////
// Summary:
// List repository webhooks
//
// Description:
// Lists webhooks for a repository. `last response` may return null if there have not been any deliveries within 30 days.
//
pub fn reposListWebhooks(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try reposListWebhooksResult(client, owner, repo, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListWebhooksRaw(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/hooks", .{ client.base_url, owner, repo });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListWebhooksResult(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposListWebhooksRaw(client, owner, repo, per_page, page));
}

/////////////////
// Summary:
// Create a repository webhook
//
// Description:
// Repositories can have multiple webhooks installed. Each webhook should have a unique `config`. Multiple webhooks can
// share the same `config` as long as those webhooks do not have any `events` that overlap.
//
pub fn reposCreateWebhook(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.hook) {
    var result = try reposCreateWebhookResult(client, owner, repo, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposCreateWebhookRaw(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/hooks", .{ client.base_url, owner, repo });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposCreateWebhookResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.hook) {
    return parseRawResponse(models.hook, try reposCreateWebhookRaw(client, owner, repo, requestBody));
}

/////////////////
// Summary:
// Get all autolinks of a repository
//
// Description:
// Gets all autolinks that are configured for a repository.
//
// Information about autolinks are only available to repository administrators.
//
pub fn reposListAutolinks(client: *Client, owner: []const u8, repo: []const u8) !Owned([]const std.json.Value) {
    var result = try reposListAutolinksResult(client, owner, repo);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListAutolinksRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/autolinks", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListAutolinksResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposListAutolinksRaw(client, owner, repo));
}

/////////////////
// Summary:
// Create an autolink reference for a repository
//
// Description:
// Users with admin access to the repository can create an autolink.
//
pub fn reposCreateAutolink(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.autolink) {
    var result = try reposCreateAutolinkResult(client, owner, repo, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposCreateAutolinkRaw(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/autolinks", .{ client.base_url, owner, repo });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposCreateAutolinkResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.autolink) {
    return parseRawResponse(models.autolink, try reposCreateAutolinkRaw(client, owner, repo, requestBody));
}

/////////////////
// Summary:
// Get a delivery for a repository webhook
//
// Description:
// Returns a delivery for a webhook configured in a repository.
//
pub fn reposGetWebhookDelivery(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64, delivery_id: i64) !Owned(models.@"hook-delivery") {
    var result = try reposGetWebhookDeliveryResult(client, owner, repo, hook_id, delivery_id);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetWebhookDeliveryRaw(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64, delivery_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/hooks/{d}/deliveries/{d}", .{ client.base_url, owner, repo, hook_id, delivery_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetWebhookDeliveryResult(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64, delivery_id: i64) !ApiResult(models.@"hook-delivery") {
    return parseRawResponse(models.@"hook-delivery", try reposGetWebhookDeliveryRaw(client, owner, repo, hook_id, delivery_id));
}

/////////////////
// Summary:
// List pull requests
//
// Description:
// Lists pull requests in a specified repository.
//
// Draft pull requests are available in public repositories with GitHub
// Free and GitHub Free for organizations, GitHub Pro, and legacy per-repository billing
// plans, and in public and private repositories with GitHub Team and GitHub Enterprise
// Cloud. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products)
// in the GitHub Help documentation.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn pullsList(client: *Client, owner: []const u8, repo: []const u8, state: ?[]const u8, head: ?[]const u8, base: ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try pullsListResult(client, owner, repo, state, head, base, sort, direction, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullsListRaw(client: *Client, owner: []const u8, repo: []const u8, state: ?[]const u8, head: ?[]const u8, base: ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pulls", .{ client.base_url, owner, repo });
    var first_query = true;
    if (state) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "state", value);
    }
    if (head) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "head", value);
    }
    if (base) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "base", value);
    }
    if (sort) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "sort", value);
    }
    if (direction) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "direction", value);
    }
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullsListResult(client: *Client, owner: []const u8, repo: []const u8, state: ?[]const u8, head: ?[]const u8, base: ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try pullsListRaw(client, owner, repo, state, head, base, sort, direction, per_page, page));
}

/////////////////
// Summary:
// Create a pull request
//
// Description:
// Draft pull requests are available in public repositories with GitHub Free and GitHub Free for organizations, GitHub Pro, and legacy per-repository billing plans, and in public and private repositories with GitHub Team and GitHub Enterprise Cloud. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
// To open or update a pull request in a public repository, you must have write access to the head or the source branch. For organization-owned repositories, you must be a member of the organization that owns the repository to open or update a pull request.
//
// This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating content too quickly using this endpoint may result in secondary rate limiting. For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)" and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn pullsCreate(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.@"pull-request") {
    var result = try pullsCreateResult(client, owner, repo, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullsCreateRaw(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pulls", .{ client.base_url, owner, repo });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullsCreateResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.@"pull-request") {
    return parseRawResponse(models.@"pull-request", try pullsCreateRaw(client, owner, repo, requestBody));
}

/////////////////
// Summary:
// List commit comments for a repository
//
// Description:
// Lists the commit comments for a specified repository. Comments are ordered by ascending ID.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn reposListCommitCommentsForRepo(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try reposListCommitCommentsForRepoResult(client, owner, repo, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListCommitCommentsForRepoRaw(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/comments", .{ client.base_url, owner, repo });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListCommitCommentsForRepoResult(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposListCommitCommentsForRepoRaw(client, owner, repo, per_page, page));
}

/////////////////
// Summary:
// Remove dependency an issue is blocked by
//
// Description:
// You can use the REST API to remove a dependency that an issue is blocked by.
//
// Removing content too quickly using this endpoint may result in secondary rate limiting.
// For more information, see [Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)
// and [Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api).
//
// This endpoint supports the following custom media types. For more information, see [Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types).
// - **`application/vnd.github.raw+json`**: Returns the raw Markdown body. Response will include `body`. This is the default if you do not pass a specific media type.
// - **`application/vnd.github.text+json`**: Returns a text only representation of the Markdown body. Response will include `body_text`.
// - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's Markdown. Response will include `body_html`.
// - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn issuesRemoveDependencyBlockedBy(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, issue_id: i64) !Owned(models.issue) {
    var result = try issuesRemoveDependencyBlockedByResult(client, owner, repo, issue_number, issue_id);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesRemoveDependencyBlockedByRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, issue_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}/dependencies/blocked_by/{d}", .{ client.base_url, owner, repo, issue_number, issue_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesRemoveDependencyBlockedByResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, issue_id: i64) !ApiResult(models.issue) {
    return parseRawResponse(models.issue, try issuesRemoveDependencyBlockedByRaw(client, owner, repo, issue_number, issue_id));
}

/////////////////
// Summary:
// Get repository ruleset history
//
// Description:
// Get the history of a repository ruleset.
//
pub fn reposGetRepoRulesetHistory(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64, ruleset_id: i64) !Owned([]const std.json.Value) {
    var result = try reposGetRepoRulesetHistoryResult(client, owner, repo, per_page, page, ruleset_id);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetRepoRulesetHistoryRaw(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64, ruleset_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/rulesets/{d}/history", .{ client.base_url, owner, repo, ruleset_id });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetRepoRulesetHistoryResult(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64, ruleset_id: i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposGetRepoRulesetHistoryRaw(client, owner, repo, per_page, page, ruleset_id));
}

/////////////////
// Summary:
// Get a review for a pull request
//
// Description:
// Retrieves a pull request review by its ID.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn pullsGetReview(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, review_id: i64) !Owned(models.@"pull-request-review") {
    var result = try pullsGetReviewResult(client, owner, repo, pull_number, review_id);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullsGetReviewRaw(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, review_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pulls/{d}/reviews/{d}", .{ client.base_url, owner, repo, pull_number, review_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullsGetReviewResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, review_id: i64) !ApiResult(models.@"pull-request-review") {
    return parseRawResponse(models.@"pull-request-review", try pullsGetReviewRaw(client, owner, repo, pull_number, review_id));
}

/////////////////
// Summary:
// Update a review for a pull request
//
// Description:
// Updates the contents of a specified review summary comment.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn pullsUpdateReview(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, review_id: i64, requestBody: std.json.Value) !Owned(models.@"pull-request-review") {
    var result = try pullsUpdateReviewResult(client, owner, repo, pull_number, review_id, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullsUpdateReviewRaw(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, review_id: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pulls/{d}/reviews/{d}", .{ client.base_url, owner, repo, pull_number, review_id });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PUT, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullsUpdateReviewResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, review_id: i64, requestBody: std.json.Value) !ApiResult(models.@"pull-request-review") {
    return parseRawResponse(models.@"pull-request-review", try pullsUpdateReviewRaw(client, owner, repo, pull_number, review_id, requestBody));
}

/////////////////
// Summary:
// Delete a pending review for a pull request
//
// Description:
// Deletes a pull request review that has not been submitted. Submitted reviews cannot be deleted.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn pullsDeletePendingReview(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, review_id: i64) !Owned(models.@"pull-request-review") {
    var result = try pullsDeletePendingReviewResult(client, owner, repo, pull_number, review_id);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullsDeletePendingReviewRaw(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, review_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pulls/{d}/reviews/{d}", .{ client.base_url, owner, repo, pull_number, review_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullsDeletePendingReviewResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, review_id: i64) !ApiResult(models.@"pull-request-review") {
    return parseRawResponse(models.@"pull-request-review", try pullsDeletePendingReviewRaw(client, owner, repo, pull_number, review_id));
}

/////////////////
// Summary:
// Get a repository README
//
// Description:
// Gets the preferred README for a repository.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github.raw+json`**: Returns the raw file contents. This is the default if you do not specify a media type.
// - **`application/vnd.github.html+json`**: Returns the README in HTML. Markup languages are rendered to HTML using GitHub's open-source [Markup library](https://github.com/github/markup).
//
pub fn reposGetReadme(client: *Client, owner: []const u8, repo: []const u8, ref: ?[]const u8) !Owned(models.@"content-file") {
    var result = try reposGetReadmeResult(client, owner, repo, ref);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetReadmeRaw(client: *Client, owner: []const u8, repo: []const u8, ref: ?[]const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/readme", .{ client.base_url, owner, repo });
    var first_query = true;
    if (ref) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "ref", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetReadmeResult(client: *Client, owner: []const u8, repo: []const u8, ref: ?[]const u8) !ApiResult(models.@"content-file") {
    return parseRawResponse(models.@"content-file", try reposGetReadmeRaw(client, owner, repo, ref));
}

/////////////////
// Summary:
// Get branch protection
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
pub fn reposGetBranchProtection(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !Owned(models.@"branch-protection") {
    var result = try reposGetBranchProtectionResult(client, owner, repo, branch);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetBranchProtectionRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection", .{ client.base_url, owner, repo, branch });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetBranchProtectionResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !ApiResult(models.@"branch-protection") {
    return parseRawResponse(models.@"branch-protection", try reposGetBranchProtectionRaw(client, owner, repo, branch));
}

/////////////////
// Summary:
// Update branch protection
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
// Protecting a branch requires admin or owner permissions to the repository.
//
// > [!NOTE]
// > Passing new arrays of `users` and `teams` replaces their previous values.
//
// > [!NOTE]
// > The list of users, apps, and teams in total is limited to 100 items.
//
pub fn reposUpdateBranchProtection(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned(models.@"protected-branch") {
    var result = try reposUpdateBranchProtectionResult(client, owner, repo, branch, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposUpdateBranchProtectionRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection", .{ client.base_url, owner, repo, branch });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PUT, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposUpdateBranchProtectionResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult(models.@"protected-branch") {
    return parseRawResponse(models.@"protected-branch", try reposUpdateBranchProtectionRaw(client, owner, repo, branch, requestBody));
}

/////////////////
// Summary:
// Delete branch protection
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
pub fn reposDeleteBranchProtection(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !void {
    var raw = try reposDeleteBranchProtectionRaw(client, owner, repo, branch);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposDeleteBranchProtectionRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection", .{ client.base_url, owner, repo, branch });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Redeliver a delivery for a repository webhook
//
// Description:
// Redeliver a webhook delivery for a webhook configured in a repository.
//
pub fn reposRedeliverWebhookDelivery(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64, delivery_id: i64) !void {
    var raw = try reposRedeliverWebhookDeliveryRaw(client, owner, repo, hook_id, delivery_id);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposRedeliverWebhookDeliveryRaw(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64, delivery_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/hooks/{d}/deliveries/{d}/attempts", .{ client.base_url, owner, repo, hook_id, delivery_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Get a pull request
//
// Description:
// Draft pull requests are available in public repositories with GitHub Free and GitHub Free for organizations, GitHub Pro, and legacy per-repository billing plans, and in public and private repositories with GitHub Team and GitHub Enterprise Cloud. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
// Lists details of a pull request by providing its number.
//
// When you get, [create](https://docs.github.com/rest/pulls/pulls/#create-a-pull-request), or [edit](https://docs.github.com/rest/pulls/pulls#update-a-pull-request) a pull request, GitHub creates a merge commit to test whether the pull request can be automatically merged into the base branch. This test commit is not added to the base branch or the head branch. You can review the status of the test commit using the `mergeable` key. For more information, see "[Checking mergeability of pull requests](https://docs.github.com/rest/guides/getting-started-with-the-git-database-api#checking-mergeability-of-pull-requests)".
//
// The value of the `mergeable` attribute can be `true`, `false`, or `null`. If the value is `null`, then GitHub has started a background job to compute the mergeability. After giving the job time to complete, resubmit the request. When the job finishes, you will see a non-`null` value for the `mergeable` attribute in the response. If `mergeable` is `true`, then `merge_commit_sha` will be the SHA of the _test_ merge commit.
//
// The value of the `merge_commit_sha` attribute changes depending on the state of the pull request. Before merging a pull request, the `merge_commit_sha` attribute holds the SHA of the _test_ merge commit. After merging a pull request, the `merge_commit_sha` attribute changes depending on how you merged the pull request:
//
// *   If merged as a [merge commit](https://docs.github.com/articles/about-merge-methods-on-github/), `merge_commit_sha` represents the SHA of the merge commit.
// *   If merged via a [squash](https://docs.github.com/articles/about-merge-methods-on-github/#squashing-your-merge-commits), `merge_commit_sha` represents the SHA of the squashed commit on the base branch.
// *   If [rebased](https://docs.github.com/articles/about-merge-methods-on-github/#rebasing-and-merging-your-commits), `merge_commit_sha` represents the commit that the base branch was updated to.
//
// Pass the appropriate [media type](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types) to fetch diff and patch formats.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
// - **`application/vnd.github.diff`**: For more information, see "[git-diff](https://git-scm.com/docs/git-diff)" in the Git documentation. If a diff is corrupt, contact us through the [GitHub Support portal](https://support.github.com/). Include the repository name and pull request ID in your message.
//
pub fn pullsGet(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64) !Owned(models.@"pull-request") {
    var result = try pullsGetResult(client, owner, repo, pull_number);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullsGetRaw(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pulls/{d}", .{ client.base_url, owner, repo, pull_number });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullsGetResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64) !ApiResult(models.@"pull-request") {
    return parseRawResponse(models.@"pull-request", try pullsGetRaw(client, owner, repo, pull_number));
}

/////////////////
// Summary:
// Update a pull request
//
// Description:
// Draft pull requests are available in public repositories with GitHub Free and GitHub Free for organizations, GitHub Pro, and legacy per-repository billing plans, and in public and private repositories with GitHub Team and GitHub Enterprise Cloud. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
// To open or update a pull request in a public repository, you must have write access to the head or the source branch. For organization-owned repositories, you must be a member of the organization that owns the repository to open or update a pull request.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn pullsUpdate(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !Owned(models.@"pull-request") {
    var result = try pullsUpdateResult(client, owner, repo, pull_number, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullsUpdateRaw(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pulls/{d}", .{ client.base_url, owner, repo, pull_number });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PATCH, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullsUpdateResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !ApiResult(models.@"pull-request") {
    return parseRawResponse(models.@"pull-request", try pullsUpdateRaw(client, owner, repo, pull_number, requestBody));
}

/////////////////
// Summary:
// Get all repository rulesets
//
// Description:
// Get all the rulesets for a repository.
//
pub fn reposGetRepoRulesets(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64, includes_parents: ?bool, targets: ?[]const u8) !Owned([]const std.json.Value) {
    var result = try reposGetRepoRulesetsResult(client, owner, repo, per_page, page, includes_parents, targets);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetRepoRulesetsRaw(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64, includes_parents: ?bool, targets: ?[]const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/rulesets", .{ client.base_url, owner, repo });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    if (includes_parents) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "includes_parents", value);
    }
    if (targets) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "targets", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetRepoRulesetsResult(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64, includes_parents: ?bool, targets: ?[]const u8) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposGetRepoRulesetsRaw(client, owner, repo, per_page, page, includes_parents, targets));
}

/////////////////
// Summary:
// Create a repository ruleset
//
// Description:
// Create a ruleset for a repository.
//
pub fn reposCreateRepoRuleset(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.@"repository-ruleset") {
    var result = try reposCreateRepoRulesetResult(client, owner, repo, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposCreateRepoRulesetRaw(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/rulesets", .{ client.base_url, owner, repo });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposCreateRepoRulesetResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.@"repository-ruleset") {
    return parseRawResponse(models.@"repository-ruleset", try reposCreateRepoRulesetRaw(client, owner, repo, requestBody));
}

/////////////////
// Summary:
// Get apps with access to the protected branch
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
// Lists the GitHub Apps that have push access to this branch. Only GitHub Apps that are installed on the repository and that have been granted write access to the repository contents can be added as authorized actors on a protected branch.
//
pub fn reposGetAppsWithAccessToProtectedBranch(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !Owned([]const std.json.Value) {
    var result = try reposGetAppsWithAccessToProtectedBranchResult(client, owner, repo, branch);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetAppsWithAccessToProtectedBranchRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection/restrictions/apps", .{ client.base_url, owner, repo, branch });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetAppsWithAccessToProtectedBranchResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposGetAppsWithAccessToProtectedBranchRaw(client, owner, repo, branch));
}

/////////////////
// Summary:
// Add app access restrictions
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
// Grants the specified apps push access for this branch. Only GitHub Apps that are installed on the repository and that have been granted write access to the repository contents can be added as authorized actors on a protected branch.
//
pub fn reposAddAppAccessRestrictions(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned([]const std.json.Value) {
    var result = try reposAddAppAccessRestrictionsResult(client, owner, repo, branch, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposAddAppAccessRestrictionsRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection/restrictions/apps", .{ client.base_url, owner, repo, branch });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposAddAppAccessRestrictionsResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposAddAppAccessRestrictionsRaw(client, owner, repo, branch, requestBody));
}

/////////////////
// Summary:
// Set app access restrictions
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
// Replaces the list of apps that have push access to this branch. This removes all apps that previously had push access and grants push access to the new list of apps. Only GitHub Apps that are installed on the repository and that have been granted write access to the repository contents can be added as authorized actors on a protected branch.
//
pub fn reposSetAppAccessRestrictions(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned([]const std.json.Value) {
    var result = try reposSetAppAccessRestrictionsResult(client, owner, repo, branch, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposSetAppAccessRestrictionsRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection/restrictions/apps", .{ client.base_url, owner, repo, branch });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PUT, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposSetAppAccessRestrictionsResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposSetAppAccessRestrictionsRaw(client, owner, repo, branch, requestBody));
}

/////////////////
// Summary:
// Remove app access restrictions
//
// Description:
// Protected branches are available in public repositories with GitHub Free and GitHub Free for organizations, and in public and private repositories with GitHub Pro, GitHub Team, GitHub Enterprise Cloud, and GitHub Enterprise Server. For more information, see [GitHub's products](https://docs.github.com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
//
// Removes the ability of an app to push to this branch. Only GitHub Apps that are installed on the repository and that have been granted write access to the repository contents can be added as authorized actors on a protected branch.
//
pub fn reposRemoveAppAccessRestrictions(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned([]const std.json.Value) {
    var result = try reposRemoveAppAccessRestrictionsResult(client, owner, repo, branch, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposRemoveAppAccessRestrictionsRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches/{s}/protection/restrictions/apps", .{ client.base_url, owner, repo, branch });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposRemoveAppAccessRestrictionsResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposRemoveAppAccessRestrictionsRaw(client, owner, repo, branch, requestBody));
}

/////////////////
// Summary:
// List commits
//
// Description:
// **Signature verification object**
//
// The response will include a `verification` object that describes the result of verifying the commit's signature. The following fields are included in the `verification` object:
//
// | Name | Type | Description |
// | ---- | ---- | ----------- |
// | `verified` | `boolean` | Indicates whether GitHub considers the signature in this commit to be verified. |
// | `reason` | `string` | The reason for verified value. Possible values and their meanings are enumerated in table below. |
// | `signature` | `string` | The signature that was extracted from the commit. |
// | `payload` | `string` | The value that was signed. |
// | `verified_at` | `string` | The date the signature was verified by GitHub. |
//
// These are the possible values for `reason` in the `verification` object:
//
// | Value | Description |
// | ----- | ----------- |
// | `expired_key` | The key that made the signature is expired. |
// | `not_signing_key` | The "signing" flag is not among the usage flags in the GPG key that made the signature. |
// | `gpgverify_error` | There was an error communicating with the signature verification service. |
// | `gpgverify_unavailable` | The signature verification service is currently unavailable. |
// | `unsigned` | The object does not include a signature. |
// | `unknown_signature_type` | A non-PGP signature was found in the commit. |
// | `no_user` | No user was associated with the `committer` email address in the commit. |
// | `unverified_email` | The `committer` email address in the commit was associated with a user, but the email address is not verified on their account. |
// | `bad_email` | The `committer` email address in the commit is not included in the identities of the PGP key that made the signature. |
// | `unknown_key` | The key that made the signature has not been registered with any user's account. |
// | `malformed_signature` | There was an error parsing the signature. |
// | `invalid` | The signature could not be cryptographically verified using the key whose key-id was found in the signature. |
// | `valid` | None of the above errors applied, so the signature is considered to be verified. |
//
pub fn reposListCommits(client: *Client, owner: []const u8, repo: []const u8, sha: ?[]const u8, path: ?[]const u8, author: ?[]const u8, committer: ?[]const u8, since: ?[]const u8, until: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try reposListCommitsResult(client, owner, repo, sha, path, author, committer, since, until, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListCommitsRaw(client: *Client, owner: []const u8, repo: []const u8, sha: ?[]const u8, path: ?[]const u8, author: ?[]const u8, committer: ?[]const u8, since: ?[]const u8, until: ?[]const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/commits", .{ client.base_url, owner, repo });
    var first_query = true;
    if (sha) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "sha", value);
    }
    if (path) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "path", value);
    }
    if (author) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "author", value);
    }
    if (committer) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "committer", value);
    }
    if (since) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "since", value);
    }
    if (until) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "until", value);
    }
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListCommitsResult(client: *Client, owner: []const u8, repo: []const u8, sha: ?[]const u8, path: ?[]const u8, author: ?[]const u8, committer: ?[]const u8, since: ?[]const u8, until: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposListCommitsRaw(client, owner, repo, sha, path, author, committer, since, until, per_page, page));
}

/////////////////
// Summary:
// Pin an issue comment
//
// Description:
// You can use the REST API to pin comments on issues.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn issuesPinComment(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !Owned(models.@"issue-comment") {
    var result = try issuesPinCommentResult(client, owner, repo, comment_id);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesPinCommentRaw(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/comments/{d}/pin", .{ client.base_url, owner, repo, comment_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PUT, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesPinCommentResult(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !ApiResult(models.@"issue-comment") {
    return parseRawResponse(models.@"issue-comment", try issuesPinCommentRaw(client, owner, repo, comment_id));
}

/////////////////
// Summary:
// Unpin an issue comment
//
// Description:
// You can use the REST API to unpin comments on issues.
//
pub fn issuesUnpinComment(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !void {
    var raw = try issuesUnpinCommentRaw(client, owner, repo, comment_id);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn issuesUnpinCommentRaw(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/comments/{d}/pin", .{ client.base_url, owner, repo, comment_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Ping a repository webhook
//
// Description:
// This will trigger a [ping event](https://docs.github.com/webhooks/#ping-event) to be sent to the hook.
//
pub fn reposPingWebhook(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64) !void {
    var raw = try reposPingWebhookRaw(client, owner, repo, hook_id);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposPingWebhookRaw(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/hooks/{d}/pings", .{ client.base_url, owner, repo, hook_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Get an issue event
//
// Description:
// Gets a single event by the event id.
//
pub fn issuesGetEvent(client: *Client, owner: []const u8, repo: []const u8, event_id: i64) !Owned(models.@"issue-event") {
    var result = try issuesGetEventResult(client, owner, repo, event_id);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesGetEventRaw(client: *Client, owner: []const u8, repo: []const u8, event_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/events/{d}", .{ client.base_url, owner, repo, event_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesGetEventResult(client: *Client, owner: []const u8, repo: []const u8, event_id: i64) !ApiResult(models.@"issue-event") {
    return parseRawResponse(models.@"issue-event", try issuesGetEventRaw(client, owner, repo, event_id));
}

/////////////////
// Summary:
// Compare two commits
//
// Description:
// Compares two commits against one another. You can compare refs (branches or tags) and commit SHAs in the same repository, or you can compare refs and commit SHAs that exist in different repositories within the same repository network, including fork branches. For more information about how to view a repository's network, see "[Understanding connections between repositories](https://docs.github.com/repositories/viewing-activity-and-data-for-your-repository/understanding-connections-between-repositories)."
//
// This endpoint is equivalent to running the `git log BASE..HEAD` command, but it returns commits in a different order. The `git log BASE..HEAD` command returns commits in reverse chronological order, whereas the API returns commits in chronological order.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github.diff`**: Returns the diff of the commit.
// - **`application/vnd.github.patch`**: Returns the patch of the commit. Diffs with binary data will have no `patch` property.
//
// The API response includes details about the files that were changed between the two commits. This includes the status of the change (if a file was added, removed, modified, or renamed), and details of the change itself. For example, files with a `renamed` status have a `previous_filename` field showing the previous filename of the file, and files with a `modified` status have a `patch` field showing the changes made to the file.
//
// When calling this endpoint without any paging parameter (`per_page` or `page`), the returned list is limited to 250 commits, and the last commit in the list is the most recent of the entire comparison.
//
// **Working with large comparisons**
//
// To process a response with a large number of commits, use a query parameter (`per_page` or `page`) to paginate the results. When using pagination:
//
// - The list of changed files is only shown on the first page of results, and it includes up to 300 changed files for the entire comparison.
// - The results are returned in chronological order, but the last commit in the returned list may not be the most recent one in the entire set if there are more pages of results.
//
// For more information on working with pagination, see "[Using pagination in the REST API](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api)."
//
// **Signature verification object**
//
// The response will include a `verification` object that describes the result of verifying the commit's signature. The `verification` object includes the following fields:
//
// | Name | Type | Description |
// | ---- | ---- | ----------- |
// | `verified` | `boolean` | Indicates whether GitHub considers the signature in this commit to be verified. |
// | `reason` | `string` | The reason for verified value. Possible values and their meanings are enumerated in table below. |
// | `signature` | `string` | The signature that was extracted from the commit. |
// | `payload` | `string` | The value that was signed. |
// | `verified_at` | `string` | The date the signature was verified by GitHub. |
//
// These are the possible values for `reason` in the `verification` object:
//
// | Value | Description |
// | ----- | ----------- |
// | `expired_key` | The key that made the signature is expired. |
// | `not_signing_key` | The "signing" flag is not among the usage flags in the GPG key that made the signature. |
// | `gpgverify_error` | There was an error communicating with the signature verification service. |
// | `gpgverify_unavailable` | The signature verification service is currently unavailable. |
// | `unsigned` | The object does not include a signature. |
// | `unknown_signature_type` | A non-PGP signature was found in the commit. |
// | `no_user` | No user was associated with the `committer` email address in the commit. |
// | `unverified_email` | The `committer` email address in the commit was associated with a user, but the email address is not verified on their account. |
// | `bad_email` | The `committer` email address in the commit is not included in the identities of the PGP key that made the signature. |
// | `unknown_key` | The key that made the signature has not been registered with any user's account. |
// | `malformed_signature` | There was an error parsing the signature. |
// | `invalid` | The signature could not be cryptographically verified using the key whose key-id was found in the signature. |
// | `valid` | None of the above errors applied, so the signature is considered to be verified. |
//
pub fn reposCompareCommits(client: *Client, owner: []const u8, repo: []const u8, page: ?i64, per_page: ?i64, basehead: []const u8) !Owned(models.@"commit-comparison") {
    var result = try reposCompareCommitsResult(client, owner, repo, page, per_page, basehead);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposCompareCommitsRaw(client: *Client, owner: []const u8, repo: []const u8, page: ?i64, per_page: ?i64, basehead: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/compare/{s}", .{ client.base_url, owner, repo, basehead });
    var first_query = true;
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposCompareCommitsResult(client: *Client, owner: []const u8, repo: []const u8, page: ?i64, per_page: ?i64, basehead: []const u8) !ApiResult(models.@"commit-comparison") {
    return parseRawResponse(models.@"commit-comparison", try reposCompareCommitsRaw(client, owner, repo, page, per_page, basehead));
}

/////////////////
// Summary:
// List organization issues assigned to the authenticated user
//
// Description:
// List issues in an organization assigned to the authenticated user.
//
// > [!NOTE]
// > GitHub's REST API considers every pull request an issue, but not every issue is a pull request. For this reason, "Issues" endpoints may return both issues and pull requests in the response. You can identify pull requests by the `pull_request` key. Be aware that the `id` of a pull request returned from "Issues" endpoints will be an _issue id_. To find out the pull request id, use the "[List pull requests](https://docs.github.com/rest/pulls/pulls#list-pull-requests)" endpoint.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn issuesListForOrg(client: *Client, org: []const u8, filter: ?[]const u8, state: ?[]const u8, labels: ?[]const u8, @"type": ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try issuesListForOrgResult(client, org, filter, state, labels, @"type", sort, direction, since, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesListForOrgRaw(client: *Client, org: []const u8, filter: ?[]const u8, state: ?[]const u8, labels: ?[]const u8, @"type": ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/orgs/{s}/issues", .{ client.base_url, org });
    var first_query = true;
    if (filter) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "filter", value);
    }
    if (state) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "state", value);
    }
    if (labels) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "labels", value);
    }
    if (@"type") |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "type", value);
    }
    if (sort) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "sort", value);
    }
    if (direction) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "direction", value);
    }
    if (since) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "since", value);
    }
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesListForOrgResult(client: *Client, org: []const u8, filter: ?[]const u8, state: ?[]const u8, labels: ?[]const u8, @"type": ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try issuesListForOrgRaw(client, org, filter, state, labels, @"type", sort, direction, since, per_page, page));
}

/////////////////
// Summary:
// Get a milestone
//
// Description:
// Gets a milestone using the given milestone number.
//
pub fn issuesGetMilestone(client: *Client, owner: []const u8, repo: []const u8, milestone_number: i64) !Owned(models.milestone) {
    var result = try issuesGetMilestoneResult(client, owner, repo, milestone_number);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesGetMilestoneRaw(client: *Client, owner: []const u8, repo: []const u8, milestone_number: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/milestones/{d}", .{ client.base_url, owner, repo, milestone_number });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesGetMilestoneResult(client: *Client, owner: []const u8, repo: []const u8, milestone_number: i64) !ApiResult(models.milestone) {
    return parseRawResponse(models.milestone, try issuesGetMilestoneRaw(client, owner, repo, milestone_number));
}

/////////////////
// Summary:
// Delete a milestone
//
// Description:
// Deletes a milestone using the given milestone number.
//
pub fn issuesDeleteMilestone(client: *Client, owner: []const u8, repo: []const u8, milestone_number: i64) !void {
    var raw = try issuesDeleteMilestoneRaw(client, owner, repo, milestone_number);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn issuesDeleteMilestoneRaw(client: *Client, owner: []const u8, repo: []const u8, milestone_number: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/milestones/{d}", .{ client.base_url, owner, repo, milestone_number });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Update a milestone
//
// Description:
//
//
pub fn issuesUpdateMilestone(client: *Client, owner: []const u8, repo: []const u8, milestone_number: i64, requestBody: std.json.Value) !Owned(models.milestone) {
    var result = try issuesUpdateMilestoneResult(client, owner, repo, milestone_number, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesUpdateMilestoneRaw(client: *Client, owner: []const u8, repo: []const u8, milestone_number: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/milestones/{d}", .{ client.base_url, owner, repo, milestone_number });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PATCH, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesUpdateMilestoneResult(client: *Client, owner: []const u8, repo: []const u8, milestone_number: i64, requestBody: std.json.Value) !ApiResult(models.milestone) {
    return parseRawResponse(models.milestone, try issuesUpdateMilestoneRaw(client, owner, repo, milestone_number, requestBody));
}

/////////////////
// Summary:
// List issues assigned to the authenticated user
//
// Description:
// List issues assigned to the authenticated user across all visible repositories including owned repositories, member
// repositories, and organization repositories. You can use the `filter` query parameter to fetch issues that are not
// necessarily assigned to you.
//
// > [!NOTE]
// > GitHub's REST API considers every pull request an issue, but not every issue is a pull request. For this reason, "Issues" endpoints may return both issues and pull requests in the response. You can identify pull requests by the `pull_request` key. Be aware that the `id` of a pull request returned from "Issues" endpoints will be an _issue id_. To find out the pull request id, use the "[List pull requests](https://docs.github.com/rest/pulls/pulls#list-pull-requests)" endpoint.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn issuesList(client: *Client, filter: ?[]const u8, state: ?[]const u8, labels: ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, collab: ?bool, orgs: ?bool, owned: ?bool, pulls: ?bool, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try issuesListResult(client, filter, state, labels, sort, direction, since, collab, orgs, owned, pulls, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesListRaw(client: *Client, filter: ?[]const u8, state: ?[]const u8, labels: ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, collab: ?bool, orgs: ?bool, owned: ?bool, pulls: ?bool, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/issues", .{client.base_url});
    var first_query = true;
    if (filter) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "filter", value);
    }
    if (state) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "state", value);
    }
    if (labels) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "labels", value);
    }
    if (sort) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "sort", value);
    }
    if (direction) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "direction", value);
    }
    if (since) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "since", value);
    }
    if (collab) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "collab", value);
    }
    if (orgs) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "orgs", value);
    }
    if (owned) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "owned", value);
    }
    if (pulls) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "pulls", value);
    }
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesListResult(client: *Client, filter: ?[]const u8, state: ?[]const u8, labels: ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, collab: ?bool, orgs: ?bool, owned: ?bool, pulls: ?bool, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try issuesListRaw(client, filter, state, labels, sort, direction, since, collab, orgs, owned, pulls, per_page, page));
}

/////////////////
// Summary:
// Merge a pull request asynchronously
//
// Description:
// Merges a pull request into the base branch in the background. Merging in this way allows certain types of errors to be retried, and avoids the risk of timeouts for particularly complex merges.
//
// This is the required method for merging stacked PRs, but also supports unstacked PRs. When using this endpoint to merge a stacked pull request, all pull requests in the stack up to and including the requested PR will be merged into the base branch.
//
// The response includes a UUID that can be used to fetch the result of the merge. If another asynchronous merge request has already been made for this pull request, the UUID of that request will be returned instead with a 409 response status to indicate that the merge options may be different from those that were requested. If there isn't an existing asynchronous merge request, a 202 response status is used.
//
// If the pull request is already merged, the merge commit OID will be returned immediately with a 200 status.
//
// If the pull request cannot be merged (e.g. because it is closed, or still a draft) this result will be returned immediately with a 400 response status. Branch protection rules and repository rules are not run at this stage, only basic pull request state checks are performed.
//
pub fn pullsMergeAsync(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !Owned(models.@"pull-request-merge-async-result") {
    var result = try pullsMergeAsyncResult(client, owner, repo, pull_number, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullsMergeAsyncRaw(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pulls/{d}/merge-async", .{ client.base_url, owner, repo, pull_number });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PUT, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullsMergeAsyncResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !ApiResult(models.@"pull-request-merge-async-result") {
    return parseRawResponse(models.@"pull-request-merge-async-result", try pullsMergeAsyncRaw(client, owner, repo, pull_number, requestBody));
}

/////////////////
// Summary:
// Get a webhook configuration for a repository
//
// Description:
// Returns the webhook configuration for a repository. To get more information about the webhook, including the `active` state and `events`, use "[Get a repository webhook](/rest/webhooks/repos#get-a-repository-webhook)."
//
// OAuth app tokens and personal access tokens (classic) need the `read:repo_hook` or `repo` scope to use this endpoint.
//
pub fn reposGetWebhookConfigForRepo(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64) !Owned(models.@"webhook-config") {
    var result = try reposGetWebhookConfigForRepoResult(client, owner, repo, hook_id);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetWebhookConfigForRepoRaw(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/hooks/{d}/config", .{ client.base_url, owner, repo, hook_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetWebhookConfigForRepoResult(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64) !ApiResult(models.@"webhook-config") {
    return parseRawResponse(models.@"webhook-config", try reposGetWebhookConfigForRepoRaw(client, owner, repo, hook_id));
}

/////////////////
// Summary:
// Update a webhook configuration for a repository
//
// Description:
// Updates the webhook configuration for a repository. To update more information about the webhook, including the `active` state and `events`, use "[Update a repository webhook](/rest/webhooks/repos#update-a-repository-webhook)."
//
// OAuth app tokens and personal access tokens (classic) need the `write:repo_hook` or `repo` scope to use this endpoint.
//
pub fn reposUpdateWebhookConfigForRepo(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64, requestBody: std.json.Value) !Owned(models.@"webhook-config") {
    var result = try reposUpdateWebhookConfigForRepoResult(client, owner, repo, hook_id, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposUpdateWebhookConfigForRepoRaw(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/hooks/{d}/config", .{ client.base_url, owner, repo, hook_id });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PATCH, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposUpdateWebhookConfigForRepoResult(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64, requestBody: std.json.Value) !ApiResult(models.@"webhook-config") {
    return parseRawResponse(models.@"webhook-config", try reposUpdateWebhookConfigForRepoRaw(client, owner, repo, hook_id, requestBody));
}

/////////////////
// Summary:
// Get a repository ruleset
//
// Description:
// Get a ruleset for a repository.
//
// **Note:** To prevent leaking sensitive information, the `bypass_actors` property is only returned if the user
// making the API request has write access to the ruleset.
//
pub fn reposGetRepoRuleset(client: *Client, owner: []const u8, repo: []const u8, ruleset_id: i64, includes_parents: ?bool) !Owned(models.@"repository-ruleset") {
    var result = try reposGetRepoRulesetResult(client, owner, repo, ruleset_id, includes_parents);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetRepoRulesetRaw(client: *Client, owner: []const u8, repo: []const u8, ruleset_id: i64, includes_parents: ?bool) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/rulesets/{d}", .{ client.base_url, owner, repo, ruleset_id });
    var first_query = true;
    if (includes_parents) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "includes_parents", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetRepoRulesetResult(client: *Client, owner: []const u8, repo: []const u8, ruleset_id: i64, includes_parents: ?bool) !ApiResult(models.@"repository-ruleset") {
    return parseRawResponse(models.@"repository-ruleset", try reposGetRepoRulesetRaw(client, owner, repo, ruleset_id, includes_parents));
}

/////////////////
// Summary:
// Update a repository ruleset
//
// Description:
// Update a ruleset for a repository.
//
pub fn reposUpdateRepoRuleset(client: *Client, owner: []const u8, repo: []const u8, ruleset_id: i64, requestBody: std.json.Value) !Owned(models.@"repository-ruleset") {
    var result = try reposUpdateRepoRulesetResult(client, owner, repo, ruleset_id, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposUpdateRepoRulesetRaw(client: *Client, owner: []const u8, repo: []const u8, ruleset_id: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/rulesets/{d}", .{ client.base_url, owner, repo, ruleset_id });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PUT, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposUpdateRepoRulesetResult(client: *Client, owner: []const u8, repo: []const u8, ruleset_id: i64, requestBody: std.json.Value) !ApiResult(models.@"repository-ruleset") {
    return parseRawResponse(models.@"repository-ruleset", try reposUpdateRepoRulesetRaw(client, owner, repo, ruleset_id, requestBody));
}

/////////////////
// Summary:
// Delete a repository ruleset
//
// Description:
// Delete a ruleset for a repository.
//
pub fn reposDeleteRepoRuleset(client: *Client, owner: []const u8, repo: []const u8, ruleset_id: i64) !void {
    var raw = try reposDeleteRepoRulesetRaw(client, owner, repo, ruleset_id);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposDeleteRepoRulesetRaw(client: *Client, owner: []const u8, repo: []const u8, ruleset_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/rulesets/{d}", .{ client.base_url, owner, repo, ruleset_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Get an organization repository ruleset
//
// Description:
// Get a repository ruleset for an organization.
//
// **Note:** To prevent leaking sensitive information, the `bypass_actors` property is only returned if the user
// making the API request has write access to the ruleset.
//
pub fn reposGetOrgRuleset(client: *Client, org: []const u8, ruleset_id: i64) !Owned(models.@"repository-ruleset") {
    var result = try reposGetOrgRulesetResult(client, org, ruleset_id);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetOrgRulesetRaw(client: *Client, org: []const u8, ruleset_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/orgs/{s}/rulesets/{d}", .{ client.base_url, org, ruleset_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetOrgRulesetResult(client: *Client, org: []const u8, ruleset_id: i64) !ApiResult(models.@"repository-ruleset") {
    return parseRawResponse(models.@"repository-ruleset", try reposGetOrgRulesetRaw(client, org, ruleset_id));
}

/////////////////
// Summary:
// Update an organization repository ruleset
//
// Description:
// Update a ruleset for an organization.
//
pub fn reposUpdateOrgRuleset(client: *Client, org: []const u8, ruleset_id: i64, requestBody: std.json.Value) !Owned(models.@"repository-ruleset") {
    var result = try reposUpdateOrgRulesetResult(client, org, ruleset_id, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposUpdateOrgRulesetRaw(client: *Client, org: []const u8, ruleset_id: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/orgs/{s}/rulesets/{d}", .{ client.base_url, org, ruleset_id });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PUT, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposUpdateOrgRulesetResult(client: *Client, org: []const u8, ruleset_id: i64, requestBody: std.json.Value) !ApiResult(models.@"repository-ruleset") {
    return parseRawResponse(models.@"repository-ruleset", try reposUpdateOrgRulesetRaw(client, org, ruleset_id, requestBody));
}

/////////////////
// Summary:
// Delete an organization repository ruleset
//
// Description:
// Delete a ruleset for an organization.
//
pub fn reposDeleteOrgRuleset(client: *Client, org: []const u8, ruleset_id: i64) !void {
    var raw = try reposDeleteOrgRulesetRaw(client, org, ruleset_id);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposDeleteOrgRulesetRaw(client: *Client, org: []const u8, ruleset_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/orgs/{s}/rulesets/{d}", .{ client.base_url, org, ruleset_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Get a review comment for a pull request
//
// Description:
// Provides details for a specified review comment.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn pullsGetReviewComment(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !Owned(models.@"pull-request-review-comment") {
    var result = try pullsGetReviewCommentResult(client, owner, repo, comment_id);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullsGetReviewCommentRaw(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pulls/comments/{d}", .{ client.base_url, owner, repo, comment_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullsGetReviewCommentResult(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !ApiResult(models.@"pull-request-review-comment") {
    return parseRawResponse(models.@"pull-request-review-comment", try pullsGetReviewCommentRaw(client, owner, repo, comment_id));
}

/////////////////
// Summary:
// Delete a review comment for a pull request
//
// Description:
// Deletes a review comment.
//
pub fn pullsDeleteReviewComment(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !void {
    var raw = try pullsDeleteReviewCommentRaw(client, owner, repo, comment_id);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn pullsDeleteReviewCommentRaw(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pulls/comments/{d}", .{ client.base_url, owner, repo, comment_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Update a review comment for a pull request
//
// Description:
// Edits the content of a specified review comment.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn pullsUpdateReviewComment(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64, requestBody: std.json.Value) !Owned(models.@"pull-request-review-comment") {
    var result = try pullsUpdateReviewCommentResult(client, owner, repo, comment_id, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullsUpdateReviewCommentRaw(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pulls/comments/{d}", .{ client.base_url, owner, repo, comment_id });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PATCH, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullsUpdateReviewCommentResult(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64, requestBody: std.json.Value) !ApiResult(models.@"pull-request-review-comment") {
    return parseRawResponse(models.@"pull-request-review-comment", try pullsUpdateReviewCommentRaw(client, owner, repo, comment_id, requestBody));
}

/////////////////
// Summary:
// Get the last year of commit activity
//
// Description:
// Returns the last year of commit activity grouped by week. The `days` array is a group of commits per day, starting on `Sunday`.
//
pub fn reposGetCommitActivityStats(client: *Client, owner: []const u8, repo: []const u8) !Owned([]const std.json.Value) {
    var result = try reposGetCommitActivityStatsResult(client, owner, repo);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetCommitActivityStatsRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/stats/commit_activity", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetCommitActivityStatsResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposGetCommitActivityStatsRaw(client, owner, repo));
}

/////////////////
// Summary:
// Download a repository archive (tar)
//
// Description:
// Gets a redirect URL to download a tar archive for a repository. If you omit `:ref`, the repository’s default branch (usually
// `main`) will be used. Please make sure your HTTP framework is configured to follow redirects or you will need to use
// the `Location` header to make a second `GET` request.
//
// > [!NOTE]
// > For private repositories, these links are temporary and expire after five minutes.
//
pub fn reposDownloadTarballArchive(client: *Client, owner: []const u8, repo: []const u8, ref: []const u8) !void {
    var raw = try reposDownloadTarballArchiveRaw(client, owner, repo, ref);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposDownloadTarballArchiveRaw(client: *Client, owner: []const u8, repo: []const u8, ref: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/tarball/{s}", .{ client.base_url, owner, repo, ref });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// List public repositories
//
// Description:
// Lists all public repositories in the order that they were created.
//
// Note:
// - For GitHub Enterprise Server, this endpoint will only list repositories available to all users on the enterprise.
// - Pagination is powered exclusively by the `since` parameter. Use the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers) to get the URL for the next page of repositories.
//
pub fn reposListPublic(client: *Client, since: ?i64) !Owned([]const std.json.Value) {
    var result = try reposListPublicResult(client, since);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListPublicRaw(client: *Client, since: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repositories", .{client.base_url});
    var first_query = true;
    if (since) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "since", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListPublicResult(client: *Client, since: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposListPublicRaw(client, since));
}

/////////////////
// Summary:
// Get a label
//
// Description:
// Gets a label using the given name.
//
pub fn issuesGetLabel(client: *Client, owner: []const u8, repo: []const u8, name: []const u8) !Owned(models.label) {
    var result = try issuesGetLabelResult(client, owner, repo, name);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesGetLabelRaw(client: *Client, owner: []const u8, repo: []const u8, name: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/labels/{s}", .{ client.base_url, owner, repo, name });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesGetLabelResult(client: *Client, owner: []const u8, repo: []const u8, name: []const u8) !ApiResult(models.label) {
    return parseRawResponse(models.label, try issuesGetLabelRaw(client, owner, repo, name));
}

/////////////////
// Summary:
// Delete a label
//
// Description:
// Deletes a label using the given label name.
//
pub fn issuesDeleteLabel(client: *Client, owner: []const u8, repo: []const u8, name: []const u8) !void {
    var raw = try issuesDeleteLabelRaw(client, owner, repo, name);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn issuesDeleteLabelRaw(client: *Client, owner: []const u8, repo: []const u8, name: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/labels/{s}", .{ client.base_url, owner, repo, name });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Update a label
//
// Description:
// Updates a label using the given label name.
//
pub fn issuesUpdateLabel(client: *Client, owner: []const u8, repo: []const u8, name: []const u8, requestBody: std.json.Value) !Owned(models.label) {
    var result = try issuesUpdateLabelResult(client, owner, repo, name, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesUpdateLabelRaw(client: *Client, owner: []const u8, repo: []const u8, name: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/labels/{s}", .{ client.base_url, owner, repo, name });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PATCH, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesUpdateLabelResult(client: *Client, owner: []const u8, repo: []const u8, name: []const u8, requestBody: std.json.Value) !ApiResult(models.label) {
    return parseRawResponse(models.label, try issuesUpdateLabelRaw(client, owner, repo, name, requestBody));
}

/////////////////
// Summary:
// Get a release
//
// Description:
// Gets a public release with the specified release ID.
//
// > [!NOTE]
// > This returns an `upload_url` key corresponding to the endpoint for uploading release assets. This key is a hypermedia resource. For more information, see "[Getting started with the REST API](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#hypermedia)."
//
pub fn reposGetRelease(client: *Client, owner: []const u8, repo: []const u8, release_id: i64) !Owned(models.release) {
    var result = try reposGetReleaseResult(client, owner, repo, release_id);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetReleaseRaw(client: *Client, owner: []const u8, repo: []const u8, release_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/releases/{d}", .{ client.base_url, owner, repo, release_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetReleaseResult(client: *Client, owner: []const u8, repo: []const u8, release_id: i64) !ApiResult(models.release) {
    return parseRawResponse(models.release, try reposGetReleaseRaw(client, owner, repo, release_id));
}

/////////////////
// Summary:
// Delete a release
//
// Description:
// Users with push access to the repository can delete a release.
//
pub fn reposDeleteRelease(client: *Client, owner: []const u8, repo: []const u8, release_id: i64) !void {
    var raw = try reposDeleteReleaseRaw(client, owner, repo, release_id);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposDeleteReleaseRaw(client: *Client, owner: []const u8, repo: []const u8, release_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/releases/{d}", .{ client.base_url, owner, repo, release_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Update a release
//
// Description:
// Users with push access to the repository can edit a release.
//
// > [!NOTE]
// > If the resolved target commit (the new value of `target_commitish` if you are changing it, otherwise the existing target) adds or modifies any file under `.github/workflows/` relative to the repository's default branch, the authenticating token must be authorized to modify workflows. Otherwise, this endpoint returns `404 Not Found`; some authentication paths surface `403 Resource not accessible by integration` instead.
//
// OAuth app tokens and personal access tokens (classic) need the `workflow` scope when the resolved target commit modifies workflow files. Fine-grained access tokens and GitHub App installation tokens also need the "Workflows" repository permission (write). The `GITHUB_TOKEN` available to GitHub Actions cannot be authorized for this; for more information, see "[Automatic token authentication](https://docs.github.com/actions/security-guides/automatic-token-authentication#permissions-for-the-github_token)".
//
pub fn reposUpdateRelease(client: *Client, owner: []const u8, repo: []const u8, release_id: i64, requestBody: std.json.Value) !Owned(models.release) {
    var result = try reposUpdateReleaseResult(client, owner, repo, release_id, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposUpdateReleaseRaw(client: *Client, owner: []const u8, repo: []const u8, release_id: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/releases/{d}", .{ client.base_url, owner, repo, release_id });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PATCH, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposUpdateReleaseResult(client: *Client, owner: []const u8, repo: []const u8, release_id: i64, requestBody: std.json.Value) !ApiResult(models.release) {
    return parseRawResponse(models.release, try reposUpdateReleaseRaw(client, owner, repo, release_id, requestBody));
}

/////////////////
// Summary:
// Download a repository archive (zip)
//
// Description:
// Gets a redirect URL to download a zip archive for a repository. If you omit `:ref`, the repository’s default branch (usually
// `main`) will be used. Please make sure your HTTP framework is configured to follow redirects or you will need to use
// the `Location` header to make a second `GET` request.
//
// > [!NOTE]
// > For private repositories, these links are temporary and expire after five minutes. If the repository is empty, you will receive a 404 when you follow the redirect.
//
pub fn reposDownloadZipballArchive(client: *Client, owner: []const u8, repo: []const u8, ref: []const u8) !void {
    var raw = try reposDownloadZipballArchiveRaw(client, owner, repo, ref);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposDownloadZipballArchiveRaw(client: *Client, owner: []const u8, repo: []const u8, ref: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/zipball/{s}", .{ client.base_url, owner, repo, ref });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// List assignees
//
// Description:
// Lists the [available assignees](https://docs.github.com/articles/assigning-issues-and-pull-requests-to-other-github-users/) for issues in a repository.
//
pub fn issuesListAssignees(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try issuesListAssigneesResult(client, owner, repo, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesListAssigneesRaw(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/assignees", .{ client.base_url, owner, repo });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesListAssigneesResult(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try issuesListAssigneesRaw(client, owner, repo, per_page, page));
}

/////////////////
// Summary:
// List issue comments
//
// Description:
// You can use the REST API to list comments on issues and pull requests. Every pull request is an issue, but not every issue is a pull request.
//
// Issue comments are ordered by ascending ID.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn issuesListComments(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, since: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try issuesListCommentsResult(client, owner, repo, issue_number, since, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesListCommentsRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, since: ?[]const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}/comments", .{ client.base_url, owner, repo, issue_number });
    var first_query = true;
    if (since) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "since", value);
    }
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesListCommentsResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, since: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try issuesListCommentsRaw(client, owner, repo, issue_number, since, per_page, page));
}

/////////////////
// Summary:
// Create an issue comment
//
// Description:
// You can use the REST API to create comments on issues and pull requests. Every pull request is an issue, but not every issue is a pull request.
//
// This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications).
// Creating content too quickly using this endpoint may result in secondary rate limiting.
// For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)"
// and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn issuesCreateComment(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !Owned(models.@"issue-comment") {
    var result = try issuesCreateCommentResult(client, owner, repo, issue_number, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesCreateCommentRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}/comments", .{ client.base_url, owner, repo, issue_number });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesCreateCommentResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !ApiResult(models.@"issue-comment") {
    return parseRawResponse(models.@"issue-comment", try issuesCreateCommentRaw(client, owner, repo, issue_number, requestBody));
}

/////////////////
// Summary:
// List attestations
//
// Description:
// List a collection of artifact attestations with a given subject digest that are associated with a repository.
//
// The authenticated user making the request must have read access to the repository. In addition, when using a fine-grained access token the `attestations:read` permission is required.
//
// **Please note:** in order to offer meaningful security benefits, an attestation's signature and timestamps **must** be cryptographically verified, and the identity of the attestation signer **must** be validated. Attestations can be verified using the [GitHub CLI `attestation verify` command](https://cli.github.com/manual/gh_attestation_verify). For more information, see [our guide on how to use artifact attestations to establish a build's provenance](https://docs.github.com/actions/security-guides/using-artifact-attestations-to-establish-provenance-for-builds).
//
pub fn reposListAttestations(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, before: ?[]const u8, after: ?[]const u8, subject_digest: []const u8, predicate_type: ?[]const u8) !Owned(std.json.Value) {
    var result = try reposListAttestationsResult(client, owner, repo, per_page, before, after, subject_digest, predicate_type);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListAttestationsRaw(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, before: ?[]const u8, after: ?[]const u8, subject_digest: []const u8, predicate_type: ?[]const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/attestations/{s}", .{ client.base_url, owner, repo, subject_digest });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (before) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "before", value);
    }
    if (after) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "after", value);
    }
    if (predicate_type) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "predicate_type", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListAttestationsResult(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, before: ?[]const u8, after: ?[]const u8, subject_digest: []const u8, predicate_type: ?[]const u8) !ApiResult(std.json.Value) {
    return parseRawResponse(std.json.Value, try reposListAttestationsRaw(client, owner, repo, per_page, before, after, subject_digest, predicate_type));
}

/////////////////
// Summary:
// Get a pull request stack
//
// Description:
// Gets a pull request stack by providing its stack number.
//
pub fn pullRequestStacksGet(client: *Client, owner: []const u8, repo: []const u8, stack_number: i64) !Owned(std.json.Value) {
    var result = try pullRequestStacksGetResult(client, owner, repo, stack_number);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullRequestStacksGetRaw(client: *Client, owner: []const u8, repo: []const u8, stack_number: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/stacks/{d}", .{ client.base_url, owner, repo, stack_number });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullRequestStacksGetResult(client: *Client, owner: []const u8, repo: []const u8, stack_number: i64) !ApiResult(std.json.Value) {
    return parseRawResponse(std.json.Value, try pullRequestStacksGetRaw(client, owner, repo, stack_number));
}

/////////////////
// Summary:
// Dismiss an issue suggestion
//
// Description:
// Dismisses a pending suggestion on an issue. Transitions the suggestion to `dismissed` without applying any change or creating a timeline event.
//
// Requires triage access to the repository. This endpoint only supports issues, not pull requests.
//
pub fn issuesDismissSuggestion(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, suggestion_id: i64) !Owned(models.@"issue-suggestion") {
    var result = try issuesDismissSuggestionResult(client, owner, repo, issue_number, suggestion_id);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesDismissSuggestionRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, suggestion_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}/suggestions/{d}/dismiss", .{ client.base_url, owner, repo, issue_number, suggestion_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesDismissSuggestionResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, suggestion_id: i64) !ApiResult(models.@"issue-suggestion") {
    return parseRawResponse(models.@"issue-suggestion", try issuesDismissSuggestionRaw(client, owner, repo, issue_number, suggestion_id));
}

/////////////////
// Summary:
// List labels for a repository
//
// Description:
// Lists all labels for a repository.
//
pub fn issuesListLabelsForRepo(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try issuesListLabelsForRepoResult(client, owner, repo, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesListLabelsForRepoRaw(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/labels", .{ client.base_url, owner, repo });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesListLabelsForRepoResult(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try issuesListLabelsForRepoRaw(client, owner, repo, per_page, page));
}

/////////////////
// Summary:
// Create a label
//
// Description:
// Creates a label for the specified repository with the given name and color. The name and color parameters are required. The color must be a valid [hexadecimal color code](http://www.color-hex.com/).
//
pub fn issuesCreateLabel(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.label) {
    var result = try issuesCreateLabelResult(client, owner, repo, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesCreateLabelRaw(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/labels", .{ client.base_url, owner, repo });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesCreateLabelResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.label) {
    return parseRawResponse(models.label, try issuesCreateLabelRaw(client, owner, repo, requestBody));
}

/////////////////
// Summary:
// Get all organization repository rulesets
//
// Description:
// Get all the repository rulesets for an organization.
//
pub fn reposGetOrgRulesets(client: *Client, org: []const u8, per_page: ?i64, page: ?i64, targets: ?[]const u8) !Owned([]const std.json.Value) {
    var result = try reposGetOrgRulesetsResult(client, org, per_page, page, targets);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetOrgRulesetsRaw(client: *Client, org: []const u8, per_page: ?i64, page: ?i64, targets: ?[]const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/orgs/{s}/rulesets", .{ client.base_url, org });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    if (targets) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "targets", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetOrgRulesetsResult(client: *Client, org: []const u8, per_page: ?i64, page: ?i64, targets: ?[]const u8) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposGetOrgRulesetsRaw(client, org, per_page, page, targets));
}

/////////////////
// Summary:
// Create an organization repository ruleset
//
// Description:
// Create a repository ruleset for an organization.
//
pub fn reposCreateOrgRuleset(client: *Client, org: []const u8, requestBody: std.json.Value) !Owned(models.@"repository-ruleset") {
    var result = try reposCreateOrgRulesetResult(client, org, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposCreateOrgRulesetRaw(client: *Client, org: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/orgs/{s}/rulesets", .{ client.base_url, org });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposCreateOrgRulesetResult(client: *Client, org: []const u8, requestBody: std.json.Value) !ApiResult(models.@"repository-ruleset") {
    return parseRawResponse(models.@"repository-ruleset", try reposCreateOrgRulesetRaw(client, org, requestBody));
}

/////////////////
// Summary:
// List deployment branch policies
//
// Description:
// Lists the deployment branch policies for an environment.
//
// Anyone with read access to the repository can use this endpoint.
//
// OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
//
pub fn reposListDeploymentBranchPolicies(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, per_page: ?i64, page: ?i64) !Owned(std.json.Value) {
    var result = try reposListDeploymentBranchPoliciesResult(client, owner, repo, environment_name, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListDeploymentBranchPoliciesRaw(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/environments/{s}/deployment-branch-policies", .{ client.base_url, owner, repo, environment_name });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListDeploymentBranchPoliciesResult(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, per_page: ?i64, page: ?i64) !ApiResult(std.json.Value) {
    return parseRawResponse(std.json.Value, try reposListDeploymentBranchPoliciesRaw(client, owner, repo, environment_name, per_page, page));
}

/////////////////
// Summary:
// Create a deployment branch policy
//
// Description:
// Creates a deployment branch or tag policy for an environment.
//
// OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
//
pub fn reposCreateDeploymentBranchPolicy(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, requestBody: models.@"deployment-branch-policy-name-pattern-with-type") !Owned(models.@"deployment-branch-policy") {
    var result = try reposCreateDeploymentBranchPolicyResult(client, owner, repo, environment_name, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposCreateDeploymentBranchPolicyRaw(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, requestBody: models.@"deployment-branch-policy-name-pattern-with-type") !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/environments/{s}/deployment-branch-policies", .{ client.base_url, owner, repo, environment_name });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposCreateDeploymentBranchPolicyResult(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, requestBody: models.@"deployment-branch-policy-name-pattern-with-type") !ApiResult(models.@"deployment-branch-policy") {
    return parseRawResponse(models.@"deployment-branch-policy", try reposCreateDeploymentBranchPolicyRaw(client, owner, repo, environment_name, requestBody));
}

/////////////////
// Summary:
// Get repository ruleset version
//
// Description:
// Get a version of a repository ruleset.
//
pub fn reposGetRepoRulesetVersion(client: *Client, owner: []const u8, repo: []const u8, ruleset_id: i64, version_id: i64) !Owned(models.@"ruleset-version-with-state") {
    var result = try reposGetRepoRulesetVersionResult(client, owner, repo, ruleset_id, version_id);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetRepoRulesetVersionRaw(client: *Client, owner: []const u8, repo: []const u8, ruleset_id: i64, version_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/rulesets/{d}/history/{d}", .{ client.base_url, owner, repo, ruleset_id, version_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetRepoRulesetVersionResult(client: *Client, owner: []const u8, repo: []const u8, ruleset_id: i64, version_id: i64) !ApiResult(models.@"ruleset-version-with-state") {
    return parseRawResponse(models.@"ruleset-version-with-state", try reposGetRepoRulesetVersionRaw(client, owner, repo, ruleset_id, version_id));
}

/////////////////
// Summary:
// List issue suggestions
//
// Description:
// Lists the suggestions on an issue. A suggestion is an agent-proposed change to an issue's type, labels, fields, assignees, or closed state that a maintainer can approve or dismiss.
//
// By default only pending suggestions are returned. Use `state=all` to return suggestions in every state, or `state=<state>` to filter to a single state. Use `action=<action>` to return only suggestions for a specific change.
//
// This endpoint is only available while the issue suggestions feature is enabled for the repository, and only supports issues, not pull requests.
//
// Requires triage access to the repository.
//
pub fn issuesListSuggestions(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, state: ?[]const u8, action: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try issuesListSuggestionsResult(client, owner, repo, issue_number, state, action, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesListSuggestionsRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, state: ?[]const u8, action: ?[]const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}/suggestions", .{ client.base_url, owner, repo, issue_number });
    var first_query = true;
    if (state) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "state", value);
    }
    if (action) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "action", value);
    }
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesListSuggestionsResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, state: ?[]const u8, action: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try issuesListSuggestionsRaw(client, owner, repo, issue_number, state, action, per_page, page));
}

/////////////////
// Summary:
// Check if immutable releases are enabled for a repository
//
// Description:
// Shows whether immutable releases are enabled or disabled. Also identifies whether immutability is being
// enforced by the repository owner.  The authenticated user must have admin read access to the repository.
//
pub fn reposCheckImmutableReleases(client: *Client, owner: []const u8, repo: []const u8) !Owned(models.@"check-immutable-releases") {
    var result = try reposCheckImmutableReleasesResult(client, owner, repo);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposCheckImmutableReleasesRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/immutable-releases", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposCheckImmutableReleasesResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult(models.@"check-immutable-releases") {
    return parseRawResponse(models.@"check-immutable-releases", try reposCheckImmutableReleasesRaw(client, owner, repo));
}

/////////////////
// Summary:
// Enable immutable releases
//
// Description:
// Enables immutable releases for a repository. The authenticated user must have admin access to the repository.
//
pub fn reposEnableImmutableReleases(client: *Client, owner: []const u8, repo: []const u8) !void {
    var raw = try reposEnableImmutableReleasesRaw(client, owner, repo);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposEnableImmutableReleasesRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/immutable-releases", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PUT, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Disable immutable releases
//
// Description:
// Disables immutable releases for a repository. The authenticated user must have admin access to the repository.
//
pub fn reposDisableImmutableReleases(client: *Client, owner: []const u8, repo: []const u8) !void {
    var raw = try reposDisableImmutableReleasesRaw(client, owner, repo);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposDisableImmutableReleasesRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/immutable-releases", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Delete an issue field value from an issue
//
// Description:
// Remove a specific custom field value from an issue.
//
// Only users with push access to the repository can delete issue field values. If you don't have the proper permissions, you'll receive a `403 Forbidden` response.
//
// If the specified field does not have a value set on the issue, this operation will return a `404` error.
//
// This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating content too quickly using this endpoint may result in secondary rate limiting. For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)"
// and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
//
pub fn issuesDeleteIssueFieldValue(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, issue_field_id: i64) !void {
    var raw = try issuesDeleteIssueFieldValueRaw(client, owner, repo, issue_number, issue_field_id);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn issuesDeleteIssueFieldValueRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, issue_field_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}/issue-field-values/{d}", .{ client.base_url, owner, repo, issue_number, issue_field_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// List milestones
//
// Description:
// Lists milestones for a repository.
//
pub fn issuesListMilestones(client: *Client, owner: []const u8, repo: []const u8, state: ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try issuesListMilestonesResult(client, owner, repo, state, sort, direction, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesListMilestonesRaw(client: *Client, owner: []const u8, repo: []const u8, state: ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/milestones", .{ client.base_url, owner, repo });
    var first_query = true;
    if (state) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "state", value);
    }
    if (sort) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "sort", value);
    }
    if (direction) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "direction", value);
    }
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesListMilestonesResult(client: *Client, owner: []const u8, repo: []const u8, state: ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try issuesListMilestonesRaw(client, owner, repo, state, sort, direction, per_page, page));
}

/////////////////
// Summary:
// Create a milestone
//
// Description:
// Creates a milestone.
//
pub fn issuesCreateMilestone(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.milestone) {
    var result = try issuesCreateMilestoneResult(client, owner, repo, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesCreateMilestoneRaw(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/milestones", .{ client.base_url, owner, repo });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesCreateMilestoneResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.milestone) {
    return parseRawResponse(models.milestone, try issuesCreateMilestoneRaw(client, owner, repo, requestBody));
}

/////////////////
// Summary:
// List deployment statuses
//
// Description:
// Users with pull access can view deployment statuses for a deployment:
//
pub fn reposListDeploymentStatuses(client: *Client, owner: []const u8, repo: []const u8, deployment_id: i64, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try reposListDeploymentStatusesResult(client, owner, repo, deployment_id, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListDeploymentStatusesRaw(client: *Client, owner: []const u8, repo: []const u8, deployment_id: i64, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/deployments/{d}/statuses", .{ client.base_url, owner, repo, deployment_id });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListDeploymentStatusesResult(client: *Client, owner: []const u8, repo: []const u8, deployment_id: i64, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposListDeploymentStatusesRaw(client, owner, repo, deployment_id, per_page, page));
}

/////////////////
// Summary:
// Create a deployment status
//
// Description:
// Users with `push` access can create deployment statuses for a given deployment.
//
// OAuth app tokens and personal access tokens (classic) need the `repo_deployment` scope to use this endpoint.
//
pub fn reposCreateDeploymentStatus(client: *Client, owner: []const u8, repo: []const u8, deployment_id: i64, requestBody: std.json.Value) !Owned(models.@"deployment-status") {
    var result = try reposCreateDeploymentStatusResult(client, owner, repo, deployment_id, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposCreateDeploymentStatusRaw(client: *Client, owner: []const u8, repo: []const u8, deployment_id: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/deployments/{d}/statuses", .{ client.base_url, owner, repo, deployment_id });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposCreateDeploymentStatusResult(client: *Client, owner: []const u8, repo: []const u8, deployment_id: i64, requestBody: std.json.Value) !ApiResult(models.@"deployment-status") {
    return parseRawResponse(models.@"deployment-status", try reposCreateDeploymentStatusRaw(client, owner, repo, deployment_id, requestBody));
}

/////////////////
// Summary:
// List issue field values for an issue
//
// Description:
// Lists all issue field values for an issue.
//
pub fn issuesListIssueFieldValuesForIssue(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try issuesListIssueFieldValuesForIssueResult(client, owner, repo, issue_number, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesListIssueFieldValuesForIssueRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}/issue-field-values", .{ client.base_url, owner, repo, issue_number });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesListIssueFieldValuesForIssueResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try issuesListIssueFieldValuesForIssueRaw(client, owner, repo, issue_number, per_page, page));
}

/////////////////
// Summary:
// Add issue field values to an issue
//
// Description:
// Add custom field values to an issue. You can set values for organization-level issue fields that have been defined for the repository's organization.
// Adding an empty array will clear all existing field values for the issue.
//
// This endpoint supports the following field data types:
// - **`text`**: String values for text fields
// - **`single_select`**: Option names for single-select fields (must match an existing option name)
// - **`number`**: Numeric values for number fields
// - **`date`**: ISO 8601 date strings for date fields
//
// Only users with push access to the repository can add issue field values. If you don't have the proper permissions, you'll receive a `403 Forbidden` response.
//
// This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating content too quickly using this endpoint may result in secondary rate limiting. For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)"
// and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
//
pub fn issuesAddIssueFieldValues(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !Owned([]const std.json.Value) {
    var result = try issuesAddIssueFieldValuesResult(client, owner, repo, issue_number, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesAddIssueFieldValuesRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}/issue-field-values", .{ client.base_url, owner, repo, issue_number });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesAddIssueFieldValuesResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try issuesAddIssueFieldValuesRaw(client, owner, repo, issue_number, requestBody));
}

/////////////////
// Summary:
// Set issue field values for an issue
//
// Description:
// Set custom field values for an issue, replacing any existing values. You can set values for organization-level issue fields that have been defined for the repository's organization.
//
// This endpoint supports the following field data types:
// - **`text`**: String values for text fields
// - **`single_select`**: Option names for single-select fields (must match an existing option name)
// - **`number`**: Numeric values for number fields
// - **`date`**: ISO 8601 date strings for date fields
//
// This operation will replace all existing field values with the provided ones. If you want to add field values without replacing existing ones, use the `POST` endpoint instead.
//
// Only users with push access to the repository can set issue field values. If you don't have the proper permissions, you'll receive a `403 Forbidden` response.
//
// This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating content too quickly using this endpoint may result in secondary rate limiting. For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)"
// and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
//
pub fn issuesSetIssueFieldValues(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !Owned([]const std.json.Value) {
    var result = try issuesSetIssueFieldValuesResult(client, owner, repo, issue_number, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesSetIssueFieldValuesRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}/issue-field-values", .{ client.base_url, owner, repo, issue_number });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PUT, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesSetIssueFieldValuesResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try issuesSetIssueFieldValuesRaw(client, owner, repo, issue_number, requestBody));
}

/////////////////
// Summary:
// Get the weekly commit count
//
// Description:
// Returns the total commit counts for the `owner` and total commit counts in `all`. `all` is everyone combined, including the `owner` in the last 52 weeks. If you'd like to get the commit counts for non-owners, you can subtract `owner` from `all`.
//
// The array order is oldest week (index 0) to most recent week.
//
// The most recent week is seven days ago at UTC midnight to today at UTC midnight.
//
pub fn reposGetParticipationStats(client: *Client, owner: []const u8, repo: []const u8) !Owned(models.@"participation-stats") {
    var result = try reposGetParticipationStatsResult(client, owner, repo);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetParticipationStatsRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/stats/participation", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetParticipationStatsResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult(models.@"participation-stats") {
    return parseRawResponse(models.@"participation-stats", try reposGetParticipationStatsRaw(client, owner, repo));
}

/////////////////
// Summary:
// Test the push repository webhook
//
// Description:
// This will trigger the hook with the latest push to the current repository if the hook is subscribed to `push` events. If the hook is not subscribed to `push` events, the server will respond with 204 but no test POST will be generated.
//
// > [!NOTE]
// > Previously `/repos/:owner/:repo/hooks/:hook_id/test`
//
pub fn reposTestPushWebhook(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64) !void {
    var raw = try reposTestPushWebhookRaw(client, owner, repo, hook_id);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposTestPushWebhookRaw(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/hooks/{d}/tests", .{ client.base_url, owner, repo, hook_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// List repository rule suites
//
// Description:
// Lists suites of rule evaluations at the repository level.
// For more information, see "[Managing rulesets for a repository](https://docs.github.com/repositories/configuring-branches-and-merges-in-your-repository/managing-rulesets/managing-rulesets-for-a-repository#viewing-insights-for-rulesets)."
//
pub fn reposGetRepoRuleSuites(client: *Client, owner: []const u8, repo: []const u8, ref: ?[]const u8, time_period: ?[]const u8, actor_name: ?[]const u8, rule_suite_result: ?[]const u8, evaluate_status: ?[]const u8, per_page: ?i64, page: ?i64) !Owned(models.@"rule-suites") {
    var result = try reposGetRepoRuleSuitesResult(client, owner, repo, ref, time_period, actor_name, rule_suite_result, evaluate_status, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetRepoRuleSuitesRaw(client: *Client, owner: []const u8, repo: []const u8, ref: ?[]const u8, time_period: ?[]const u8, actor_name: ?[]const u8, rule_suite_result: ?[]const u8, evaluate_status: ?[]const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/rulesets/rule-suites", .{ client.base_url, owner, repo });
    var first_query = true;
    if (ref) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "ref", value);
    }
    if (time_period) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "time_period", value);
    }
    if (actor_name) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "actor_name", value);
    }
    if (rule_suite_result) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "rule_suite_result", value);
    }
    if (evaluate_status) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "evaluate_status", value);
    }
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetRepoRuleSuitesResult(client: *Client, owner: []const u8, repo: []const u8, ref: ?[]const u8, time_period: ?[]const u8, actor_name: ?[]const u8, rule_suite_result: ?[]const u8, evaluate_status: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult(models.@"rule-suites") {
    return parseRawResponse(models.@"rule-suites", try reposGetRepoRuleSuitesRaw(client, owner, repo, ref, time_period, actor_name, rule_suite_result, evaluate_status, per_page, page));
}

/////////////////
// Summary:
// Add pull requests to a pull request stack
//
// Description:
// Appends an ordered list of pull request numbers onto the top of an existing
// stack. Provide only the pull requests you want to add, from the current top of
// the stack upward. The first new pull request's base ref must match the current
// top pull request's head ref.
//
pub fn pullRequestStacksAdd(client: *Client, owner: []const u8, repo: []const u8, stack_number: i64, requestBody: std.json.Value) !Owned(std.json.Value) {
    var result = try pullRequestStacksAddResult(client, owner, repo, stack_number, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullRequestStacksAddRaw(client: *Client, owner: []const u8, repo: []const u8, stack_number: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/stacks/{d}/add", .{ client.base_url, owner, repo, stack_number });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullRequestStacksAddResult(client: *Client, owner: []const u8, repo: []const u8, stack_number: i64, requestBody: std.json.Value) !ApiResult(std.json.Value) {
    return parseRawResponse(std.json.Value, try pullRequestStacksAddRaw(client, owner, repo, stack_number, requestBody));
}

/////////////////
// Summary:
// List issue comments for a repository
//
// Description:
// You can use the REST API to list comments on issues and pull requests for a repository. Every pull request is an issue, but not every issue is a pull request.
//
// By default, issue comments are ordered by ascending ID.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn issuesListCommentsForRepo(client: *Client, owner: []const u8, repo: []const u8, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try issuesListCommentsForRepoResult(client, owner, repo, sort, direction, since, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesListCommentsForRepoRaw(client: *Client, owner: []const u8, repo: []const u8, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/comments", .{ client.base_url, owner, repo });
    var first_query = true;
    if (sort) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "sort", value);
    }
    if (direction) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "direction", value);
    }
    if (since) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "since", value);
    }
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesListCommentsForRepoResult(client: *Client, owner: []const u8, repo: []const u8, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try issuesListCommentsForRepoRaw(client, owner, repo, sort, direction, since, per_page, page));
}

/////////////////
// Summary:
// Check if a user can be assigned
//
// Description:
// Checks if a user has permission to be assigned to an issue in this repository.
//
// If the `assignee` can be assigned to issues in the repository, a `204` header with no content is returned.
//
// Otherwise a `404` status code is returned.
//
pub fn issuesCheckUserCanBeAssigned(client: *Client, owner: []const u8, repo: []const u8, assignee: []const u8) !void {
    var raw = try issuesCheckUserCanBeAssignedRaw(client, owner, repo, assignee);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn issuesCheckUserCanBeAssignedRaw(client: *Client, owner: []const u8, repo: []const u8, assignee: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/assignees/{s}", .{ client.base_url, owner, repo, assignee });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Create a reply for a review comment
//
// Description:
// Creates a reply to a review comment for a pull request. For the `comment_id`, provide the ID of the review comment you are replying to. This must be the ID of a _top-level review comment_, not a reply to that comment. Replies to replies are not supported.
//
// This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating content too quickly using this endpoint may result in secondary rate limiting. For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)"
// and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn pullsCreateReplyForReviewComment(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, comment_id: i64, requestBody: std.json.Value) !Owned(models.@"pull-request-review-comment") {
    var result = try pullsCreateReplyForReviewCommentResult(client, owner, repo, pull_number, comment_id, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullsCreateReplyForReviewCommentRaw(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, comment_id: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pulls/{d}/comments/{d}/replies", .{ client.base_url, owner, repo, pull_number, comment_id });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullsCreateReplyForReviewCommentResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, comment_id: i64, requestBody: std.json.Value) !ApiResult(models.@"pull-request-review-comment") {
    return parseRawResponse(models.@"pull-request-review-comment", try pullsCreateReplyForReviewCommentRaw(client, owner, repo, pull_number, comment_id, requestBody));
}

/////////////////
// Summary:
// Get a repository README for a directory
//
// Description:
// Gets the README from a repository directory.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github.raw+json`**: Returns the raw file contents. This is the default if you do not specify a media type.
// - **`application/vnd.github.html+json`**: Returns the README in HTML. Markup languages are rendered to HTML using GitHub's open-source [Markup library](https://github.com/github/markup).
//
pub fn reposGetReadmeInDirectory(client: *Client, owner: []const u8, repo: []const u8, dir: []const u8, ref: ?[]const u8) !Owned(models.@"content-file") {
    var result = try reposGetReadmeInDirectoryResult(client, owner, repo, dir, ref);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetReadmeInDirectoryRaw(client: *Client, owner: []const u8, repo: []const u8, dir: []const u8, ref: ?[]const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/readme/{s}", .{ client.base_url, owner, repo, dir });
    var first_query = true;
    if (ref) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "ref", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetReadmeInDirectoryResult(client: *Client, owner: []const u8, repo: []const u8, dir: []const u8, ref: ?[]const u8) !ApiResult(models.@"content-file") {
    return parseRawResponse(models.@"content-file", try reposGetReadmeInDirectoryRaw(client, owner, repo, dir, ref));
}

/////////////////
// Summary:
// List repository invitations for the authenticated user
//
// Description:
// When authenticating as a user, this endpoint will list all currently open repository invitations for that user.
//
pub fn reposListInvitationsForAuthenticatedUser(client: *Client, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try reposListInvitationsForAuthenticatedUserResult(client, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListInvitationsForAuthenticatedUserRaw(client: *Client, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/user/repository_invitations", .{client.base_url});
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListInvitationsForAuthenticatedUserResult(client: *Client, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposListInvitationsForAuthenticatedUserRaw(client, per_page, page));
}

/////////////////
// Summary:
// Create a repository using a template
//
// Description:
// Creates a new repository using a repository template. Use the `template_owner` and `template_repo` route parameters to specify the repository to use as the template. If the repository is not public, the authenticated user must own or be a member of an organization that owns the repository. To check if a repository is available to use as a template, get the repository's information using the [Get a repository](https://docs.github.com/rest/repos/repos#get-a-repository) endpoint and check that the `is_template` key is `true`.
//
// OAuth app tokens and personal access tokens (classic) need the `public_repo` or `repo` scope to create a public repository, and `repo` scope to create a private repository.
//
pub fn reposCreateUsingTemplate(client: *Client, template_owner: []const u8, template_repo: []const u8, requestBody: std.json.Value) !Owned(models.@"full-repository") {
    var result = try reposCreateUsingTemplateResult(client, template_owner, template_repo, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposCreateUsingTemplateRaw(client: *Client, template_owner: []const u8, template_repo: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/generate", .{ client.base_url, template_owner, template_repo });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposCreateUsingTemplateResult(client: *Client, template_owner: []const u8, template_repo: []const u8, requestBody: std.json.Value) !ApiResult(models.@"full-repository") {
    return parseRawResponse(models.@"full-repository", try reposCreateUsingTemplateRaw(client, template_owner, template_repo, requestBody));
}

/////////////////
// Summary:
// List timeline events for an issue
//
// Description:
// List all timeline events for an issue.
//
pub fn issuesListEventsForTimeline(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64, exclude: ?[]const u8) !Owned([]const std.json.Value) {
    var result = try issuesListEventsForTimelineResult(client, owner, repo, issue_number, per_page, page, exclude);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesListEventsForTimelineRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64, exclude: ?[]const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}/timeline", .{ client.base_url, owner, repo, issue_number });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    if (exclude) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "exclude", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesListEventsForTimelineResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64, exclude: ?[]const u8) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try issuesListEventsForTimelineRaw(client, owner, repo, issue_number, per_page, page, exclude));
}

/////////////////
// Summary:
// List dependencies an issue is blocked by
//
// Description:
// You can use the REST API to list the dependencies an issue is blocked by.
//
// This endpoint supports the following custom media types. For more information, see [Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types).
//
// - **`application/vnd.github.raw+json`**: Returns the raw Markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github.text+json`**: Returns a text only representation of the Markdown body. Response will include `body_text`.
// - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's Markdown. Response will include `body_html`.
// - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn issuesListDependenciesBlockedBy(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try issuesListDependenciesBlockedByResult(client, owner, repo, issue_number, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesListDependenciesBlockedByRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}/dependencies/blocked_by", .{ client.base_url, owner, repo, issue_number });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesListDependenciesBlockedByResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try issuesListDependenciesBlockedByRaw(client, owner, repo, issue_number, per_page, page));
}

/////////////////
// Summary:
// Add a dependency an issue is blocked by
//
// Description:
// You can use the REST API to add a 'blocked by' relationship to an issue.
//
// Creating content too quickly using this endpoint may result in secondary rate limiting.
// For more information, see [Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)
// and [Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api).
//
// This endpoint supports the following custom media types. For more information, see [Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types).
//
// - **`application/vnd.github.raw+json`**: Returns the raw Markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github.text+json`**: Returns a text only representation of the Markdown body. Response will include `body_text`.
// - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's Markdown. Response will include `body_html`.
// - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn issuesAddBlockedByDependency(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !Owned(models.issue) {
    var result = try issuesAddBlockedByDependencyResult(client, owner, repo, issue_number, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesAddBlockedByDependencyRaw(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/{d}/dependencies/blocked_by", .{ client.base_url, owner, repo, issue_number });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesAddBlockedByDependencyResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !ApiResult(models.issue) {
    return parseRawResponse(models.issue, try issuesAddBlockedByDependencyRaw(client, owner, repo, issue_number, requestBody));
}

/////////////////
// Summary:
// Get the status of a GitHub Pages deployment
//
// Description:
// Gets the current status of a GitHub Pages deployment.
//
// The authenticated user must have read permission for the GitHub Pages site.
//
pub fn reposGetPagesDeployment(client: *Client, owner: []const u8, repo: []const u8, pages_deployment_id: []const u8) !Owned(models.@"pages-deployment-status") {
    var result = try reposGetPagesDeploymentResult(client, owner, repo, pages_deployment_id);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetPagesDeploymentRaw(client: *Client, owner: []const u8, repo: []const u8, pages_deployment_id: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pages/deployments/{s}", .{ client.base_url, owner, repo, pages_deployment_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetPagesDeploymentResult(client: *Client, owner: []const u8, repo: []const u8, pages_deployment_id: []const u8) !ApiResult(models.@"pages-deployment-status") {
    return parseRawResponse(models.@"pages-deployment-status", try reposGetPagesDeploymentRaw(client, owner, repo, pages_deployment_id));
}

/////////////////
// Summary:
// List commit comments
//
// Description:
// Lists the comments for a specified commit.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn reposListCommentsForCommit(client: *Client, owner: []const u8, repo: []const u8, commit_sha: []const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try reposListCommentsForCommitResult(client, owner, repo, commit_sha, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListCommentsForCommitRaw(client: *Client, owner: []const u8, repo: []const u8, commit_sha: []const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/commits/{s}/comments", .{ client.base_url, owner, repo, commit_sha });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListCommentsForCommitResult(client: *Client, owner: []const u8, repo: []const u8, commit_sha: []const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposListCommentsForCommitRaw(client, owner, repo, commit_sha, per_page, page));
}

/////////////////
// Summary:
// Create a commit comment
//
// Description:
// Create a comment for a commit using its `:commit_sha`.
//
// Access to commit comments can be controlled by organization owners. For more information, see "[Managing commit comments for your organization](https://docs.github.com/organizations/managing-organization-settings/managing-commit-comments-for-your-organization)".
//
// This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating content too quickly using this endpoint may result in secondary rate limiting. For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)" and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn reposCreateCommitComment(client: *Client, owner: []const u8, repo: []const u8, commit_sha: []const u8, requestBody: std.json.Value) !Owned(models.@"commit-comment") {
    var result = try reposCreateCommitCommentResult(client, owner, repo, commit_sha, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposCreateCommitCommentRaw(client: *Client, owner: []const u8, repo: []const u8, commit_sha: []const u8, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/commits/{s}/comments", .{ client.base_url, owner, repo, commit_sha });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposCreateCommitCommentResult(client: *Client, owner: []const u8, repo: []const u8, commit_sha: []const u8, requestBody: std.json.Value) !ApiResult(models.@"commit-comment") {
    return parseRawResponse(models.@"commit-comment", try reposCreateCommitCommentRaw(client, owner, repo, commit_sha, requestBody));
}

/////////////////
// Summary:
// Get the hash algorithm for a repository
//
// Description:
// Returns the hash algorithm used to store repository objects.
//
pub fn reposGetHashAlgorithm(client: *Client, owner: []const u8, repo: []const u8) !Owned(models.@"repository-hash-algorithm") {
    var result = try reposGetHashAlgorithmResult(client, owner, repo);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetHashAlgorithmRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/hash-algorithm", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetHashAlgorithmResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult(models.@"repository-hash-algorithm") {
    return parseRawResponse(models.@"repository-hash-algorithm", try reposGetHashAlgorithmRaw(client, owner, repo));
}

/////////////////
// Summary:
// Get the combined status for a specific reference
//
// Description:
// Users with pull access in a repository can access a combined view of commit statuses for a given ref. The ref can be a SHA, a branch name, or a tag name.
//
//
// Additionally, a combined `state` is returned. The `state` is one of:
//
// *   **failure** if any of the contexts report as `error` or `failure`
// *   **pending** if there are no statuses or a context is `pending`
// *   **success** if the latest status for all contexts is `success`
//
pub fn reposGetCombinedStatusForRef(client: *Client, owner: []const u8, repo: []const u8, ref: []const u8, per_page: ?i64, page: ?i64) !Owned(models.@"combined-commit-status") {
    var result = try reposGetCombinedStatusForRefResult(client, owner, repo, ref, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetCombinedStatusForRefRaw(client: *Client, owner: []const u8, repo: []const u8, ref: []const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/commits/{s}/status", .{ client.base_url, owner, repo, ref });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetCombinedStatusForRefResult(client: *Client, owner: []const u8, repo: []const u8, ref: []const u8, per_page: ?i64, page: ?i64) !ApiResult(models.@"combined-commit-status") {
    return parseRawResponse(models.@"combined-commit-status", try reposGetCombinedStatusForRefRaw(client, owner, repo, ref, per_page, page));
}

/////////////////
// Summary:
// Check if Dependabot security updates are enabled for a repository
//
// Description:
// Shows whether Dependabot security updates are enabled, disabled or paused for a repository. The authenticated user must have admin read access to the repository. For more information, see "[Configuring Dependabot security updates](https://docs.github.com/articles/configuring-automated-security-fixes)".
//
pub fn reposCheckAutomatedSecurityFixes(client: *Client, owner: []const u8, repo: []const u8) !Owned(models.@"check-automated-security-fixes") {
    var result = try reposCheckAutomatedSecurityFixesResult(client, owner, repo);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposCheckAutomatedSecurityFixesRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/automated-security-fixes", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposCheckAutomatedSecurityFixesResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult(models.@"check-automated-security-fixes") {
    return parseRawResponse(models.@"check-automated-security-fixes", try reposCheckAutomatedSecurityFixesRaw(client, owner, repo));
}

/////////////////
// Summary:
// Enable Dependabot security updates
//
// Description:
// Enables Dependabot security updates for a repository. The authenticated user must have admin access to the repository. For more information, see "[Configuring Dependabot security updates](https://docs.github.com/articles/configuring-automated-security-fixes)".
//
pub fn reposEnableAutomatedSecurityFixes(client: *Client, owner: []const u8, repo: []const u8) !void {
    var raw = try reposEnableAutomatedSecurityFixesRaw(client, owner, repo);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposEnableAutomatedSecurityFixesRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/automated-security-fixes", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.PUT, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// Disable Dependabot security updates
//
// Description:
// Disables Dependabot security updates for a repository. The authenticated user must have admin access to the repository. For more information, see "[Configuring Dependabot security updates](https://docs.github.com/articles/configuring-automated-security-fixes)".
//
pub fn reposDisableAutomatedSecurityFixes(client: *Client, owner: []const u8, repo: []const u8) !void {
    var raw = try reposDisableAutomatedSecurityFixesRaw(client, owner, repo);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposDisableAutomatedSecurityFixesRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/automated-security-fixes", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.DELETE, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// List issue events for a repository
//
// Description:
// Lists events for a repository.
//
pub fn issuesListEventsForRepo(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try issuesListEventsForRepoResult(client, owner, repo, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn issuesListEventsForRepoRaw(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/issues/events", .{ client.base_url, owner, repo });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn issuesListEventsForRepoResult(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try issuesListEventsForRepoRaw(client, owner, repo, per_page, page));
}

/////////////////
// Summary:
// Get the result of an asynchronous merge
//
// Description:
// Fetches the current result of an asynchronous merge request, identified by the UUID that was returned when the merge was requested.
//
// While the merge is still queued, the response includes the UUID, merge method, and expected head SHA of the request. Once the merge has completed, the response reports whether it was merged, including the merge commit OID on success or a message describing why it could not be merged on failure.
//
// The result of an asynchronous merge request is retained for 24 hours after its most recent update. After this window the request expires and this endpoint returns a `404` response for its UUID.
//
pub fn pullsGetMergeAsyncResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, uuid: []const u8) !Owned(models.@"pull-request-merge-async-result") {
    var result = try pullsGetMergeAsyncResultResult(client, owner, repo, pull_number, uuid);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullsGetMergeAsyncResultRaw(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, uuid: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pulls/{d}/merge-async/{s}", .{ client.base_url, owner, repo, pull_number, uuid });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullsGetMergeAsyncResultResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, uuid: []const u8) !ApiResult(models.@"pull-request-merge-async-result") {
    return parseRawResponse(models.@"pull-request-merge-async-result", try pullsGetMergeAsyncResultRaw(client, owner, repo, pull_number, uuid));
}

/////////////////
// Summary:
// Get community profile metrics
//
// Description:
// Returns all community profile metrics for a repository. The repository cannot be a fork.
//
// The returned metrics include an overall health score, the repository description, the presence of documentation, the
// detected code of conduct, the detected license, and the presence of ISSUE\_TEMPLATE, PULL\_REQUEST\_TEMPLATE,
// README, and CONTRIBUTING files.
//
// The `health_percentage` score is defined as a percentage of how many of
// the recommended community health files are present. For more information, see
// "[About community profiles for public repositories](https://docs.github.com/communities/setting-up-your-project-for-healthy-contributions/about-community-profiles-for-public-repositories)."
//
// `content_reports_enabled` is only returned for organization-owned repositories.
//
pub fn reposGetCommunityProfileMetrics(client: *Client, owner: []const u8, repo: []const u8) !Owned(models.@"community-profile") {
    var result = try reposGetCommunityProfileMetricsResult(client, owner, repo);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetCommunityProfileMetricsRaw(client: *Client, owner: []const u8, repo: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/community/profile", .{ client.base_url, owner, repo });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetCommunityProfileMetricsResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult(models.@"community-profile") {
    return parseRawResponse(models.@"community-profile", try reposGetCommunityProfileMetricsRaw(client, owner, repo));
}

/////////////////
// Summary:
// List reviews for a pull request
//
// Description:
// Lists all reviews for a specified pull request. The list of reviews returns in chronological order.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn pullsListReviews(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try pullsListReviewsResult(client, owner, repo, pull_number, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullsListReviewsRaw(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pulls/{d}/reviews", .{ client.base_url, owner, repo, pull_number });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullsListReviewsResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try pullsListReviewsRaw(client, owner, repo, pull_number, per_page, page));
}

/////////////////
// Summary:
// Create a review for a pull request
//
// Description:
// Creates a review on a specified pull request.
//
// This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating content too quickly using this endpoint may result in secondary rate limiting. For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)" and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
//
// Pull request reviews created in the `PENDING` state are not submitted and therefore do not include the `submitted_at` property in the response. To create a pending review for a pull request, leave the `event` parameter blank. For more information about submitting a `PENDING` review, see "[Submit a review for a pull request](https://docs.github.com/rest/pulls/reviews#submit-a-review-for-a-pull-request)."
//
// > [!NOTE]
// > To comment on a specific line in a file, you need to first determine the position of that line in the diff. To see a pull request diff, add the `application/vnd.github.v3.diff` media type to the `Accept` header of a call to the [Get a pull request](https://docs.github.com/rest/pulls/pulls#get-a-pull-request) endpoint.
//
// The `position` value equals the number of lines down from the first "@@" hunk header in the file you want to add a comment. The line just below the "@@" line is position 1, the next line is position 2, and so on. The position in the diff continues to increase through lines of whitespace and additional hunks until the beginning of a new file.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github-commitcomment.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github-commitcomment.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github-commitcomment.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github-commitcomment.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn pullsCreateReview(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !Owned(models.@"pull-request-review") {
    var result = try pullsCreateReviewResult(client, owner, repo, pull_number, requestBody);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullsCreateReviewRaw(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pulls/{d}/reviews", .{ client.base_url, owner, repo, pull_number });

    var str: std.Io.Writer.Allocating = .init(allocator);
    defer str.deinit();
    try std.json.Stringify.value(requestBody, .{ .emit_null_optional_fields = false }, &str.writer);
    const payload: ?[]const u8 = str.written();

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullsCreateReviewResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !ApiResult(models.@"pull-request-review") {
    return parseRawResponse(models.@"pull-request-review", try pullsCreateReviewRaw(client, owner, repo, pull_number, requestBody));
}

/////////////////
// Summary:
// Get rules for a branch
//
// Description:
// Returns all active rules that apply to the specified branch. The branch does not need to exist; rules that would apply
// to a branch with that name will be returned. All active rules that apply will be returned, regardless of the level
// at which they are configured (e.g. repository or organization). Rules in rulesets with "evaluate" or "disabled"
// enforcement statuses are not returned.
//
pub fn reposGetBranchRules(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try reposGetBranchRulesResult(client, owner, repo, branch, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetBranchRulesRaw(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/rules/branches/{s}", .{ client.base_url, owner, repo, branch });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetBranchRulesResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposGetBranchRulesRaw(client, owner, repo, branch, per_page, page));
}

/////////////////
// Summary:
// List pull requests files
//
// Description:
// Lists the files in a specified pull request.
//
// > [!NOTE]
// > Responses include a maximum of 3000 files. The paginated response returns 30 files per page by default.
//
// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
//
// - **`application/vnd.github.raw+json`**: Returns the raw markdown body. Response will include `body`. This is the default if you do not pass any specific media type.
// - **`application/vnd.github.text+json`**: Returns a text only representation of the markdown body. Response will include `body_text`.
// - **`application/vnd.github.html+json`**: Returns HTML rendered from the body's markdown. Response will include `body_html`.
// - **`application/vnd.github.full+json`**: Returns raw, text, and HTML representations. Response will include `body`, `body_text`, and `body_html`.
//
pub fn pullsListFiles(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try pullsListFilesResult(client, owner, repo, pull_number, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn pullsListFilesRaw(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pulls/{d}/files", .{ client.base_url, owner, repo, pull_number });
    var first_query = true;
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn pullsListFilesResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try pullsListFilesRaw(client, owner, repo, pull_number, per_page, page));
}

/////////////////
// Summary:
// Cancel a GitHub Pages deployment
//
// Description:
// Cancels a GitHub Pages deployment.
//
// The authenticated user must have write permissions for the GitHub Pages site.
//
pub fn reposCancelPagesDeployment(client: *Client, owner: []const u8, repo: []const u8, pages_deployment_id: []const u8) !void {
    var raw = try reposCancelPagesDeploymentRaw(client, owner, repo, pages_deployment_id);
    defer raw.deinit();
    if (raw.status.class() != .success) return error.ResponseError;
}

pub fn reposCancelPagesDeploymentRaw(client: *Client, owner: []const u8, repo: []const u8, pages_deployment_id: []const u8) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pages/deployments/{s}/cancel", .{ client.base_url, owner, repo, pages_deployment_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.POST, uri_buf.written(), payload, operation_headers.items);
}

/////////////////
// Summary:
// List repositories for a user
//
// Description:
// Lists public repositories for the specified user.
//
pub fn reposListForUser(client: *Client, username: []const u8, @"type": ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try reposListForUserResult(client, username, @"type", sort, direction, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListForUserRaw(client: *Client, username: []const u8, @"type": ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/users/{s}/repos", .{ client.base_url, username });
    var first_query = true;
    if (@"type") |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "type", value);
    }
    if (sort) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "sort", value);
    }
    if (direction) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "direction", value);
    }
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListForUserResult(client: *Client, username: []const u8, @"type": ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposListForUserRaw(client, username, @"type", sort, direction, per_page, page));
}

/////////////////
// Summary:
// List branches
//
// Description:
//
//
pub fn reposListBranches(client: *Client, owner: []const u8, repo: []const u8, protected: ?bool, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
    var result = try reposListBranchesResult(client, owner, repo, protected, per_page, page);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposListBranchesRaw(client: *Client, owner: []const u8, repo: []const u8, protected: ?bool, per_page: ?i64, page: ?i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/branches", .{ client.base_url, owner, repo });
    var first_query = true;
    if (protected) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "protected", value);
    }
    if (per_page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "per_page", value);
    }
    if (page) |value| {
        try appendQueryParam(&uri_buf.writer, &first_query, "page", value);
    }
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposListBranchesResult(client: *Client, owner: []const u8, repo: []const u8, protected: ?bool, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
    return parseRawResponse([]const std.json.Value, try reposListBranchesRaw(client, owner, repo, protected, per_page, page));
}

/////////////////
// Summary:
// Get GitHub Pages build
//
// Description:
// Gets information about a GitHub Pages build.
//
// OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
//
pub fn reposGetPagesBuild(client: *Client, owner: []const u8, repo: []const u8, build_id: i64) !Owned(models.@"page-build") {
    var result = try reposGetPagesBuildResult(client, owner, repo, build_id);
    switch (result) {
        .ok => |ok| return ok,
        .api_error => |*err| {
            err.deinit();
            return error.ResponseError;
        },
        .parse_error => |*err| {
            err.raw.deinit();
            return error.ResponseParseError;
        },
    }
}

pub fn reposGetPagesBuildRaw(client: *Client, owner: []const u8, repo: []const u8, build_id: i64) !RawResponse {
    const allocator = client.allocator;
    var uri_buf: std.Io.Writer.Allocating = .init(allocator);
    defer uri_buf.deinit();
    try uri_buf.writer.print("{s}/repos/{s}/{s}/pages/builds/{d}", .{ client.base_url, owner, repo, build_id });
    const payload: ?[]const u8 = null;

    var operation_headers = std.ArrayList(std.http.Header).empty;
    defer operation_headers.deinit(allocator);
    var operation_header_values = std.ArrayList([]u8).empty;
    defer {
        for (operation_header_values.items) |item| allocator.free(item);
        operation_header_values.deinit(allocator);
    }
    var auth_header: ?[]u8 = null;
    defer if (auth_header) |value| allocator.free(value);
    if (client.api_key.len > 0) {
        auth_header = try std.fmt.allocPrint(allocator, "Bearer {s}", .{client.api_key});
        try operation_headers.append(allocator, .{ .name = "Authorization", .value = auth_header.? });
    }

    return requestRawWithExtraHeaders(client, std.http.Method.GET, uri_buf.written(), payload, operation_headers.items);
}

pub fn reposGetPagesBuildResult(client: *Client, owner: []const u8, repo: []const u8, build_id: i64) !ApiResult(models.@"page-build") {
    return parseRawResponse(models.@"page-build", try reposGetPagesBuildRaw(client, owner, repo, build_id));
}

pub const resources = struct {
    pub const issues = struct {
        pub fn issueslist(client: *Client, filter: ?[]const u8, state: ?[]const u8, labels: ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, collab: ?bool, orgs_param: ?bool, owned: ?bool, pulls: ?bool, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
            return issuesList(client, filter, state, labels, sort, direction, since, collab, orgs_param, owned, pulls, per_page, page);
        }
        pub fn issueslistResult(client: *Client, filter: ?[]const u8, state: ?[]const u8, labels: ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, collab: ?bool, orgs_param: ?bool, owned: ?bool, pulls: ?bool, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
            return issuesListResult(client, filter, state, labels, sort, direction, since, collab, orgs_param, owned, pulls, per_page, page);
        }
    };
    pub const orgs = struct {
        pub const issues = struct {
            pub fn issueslistfororg(client: *Client, org: []const u8, filter: ?[]const u8, state: ?[]const u8, labels: ?[]const u8, @"type": ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                return issuesListForOrg(client, org, filter, state, labels, @"type", sort, direction, since, per_page, page);
            }
            pub fn issueslistfororgResult(client: *Client, org: []const u8, filter: ?[]const u8, state: ?[]const u8, labels: ?[]const u8, @"type": ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                return issuesListForOrgResult(client, org, filter, state, labels, @"type", sort, direction, since, per_page, page);
            }
        };
        pub const repos = struct {
            pub fn reposcreateinorg(client: *Client, org: []const u8, requestBody: std.json.Value) !Owned(models.@"full-repository") {
                return reposCreateInOrg(client, org, requestBody);
            }
            pub fn reposcreateinorgResult(client: *Client, org: []const u8, requestBody: std.json.Value) !ApiResult(models.@"full-repository") {
                return reposCreateInOrgResult(client, org, requestBody);
            }
            pub fn reposlistfororg(client: *Client, org: []const u8, @"type": ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                return reposListForOrg(client, org, @"type", sort, direction, per_page, page);
            }
            pub fn reposlistfororgResult(client: *Client, org: []const u8, @"type": ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                return reposListForOrgResult(client, org, @"type", sort, direction, per_page, page);
            }
        };
        pub const rulesets = struct {
            pub fn reposcreateorgruleset(client: *Client, org: []const u8, requestBody: std.json.Value) !Owned(models.@"repository-ruleset") {
                return reposCreateOrgRuleset(client, org, requestBody);
            }
            pub fn reposcreateorgrulesetResult(client: *Client, org: []const u8, requestBody: std.json.Value) !ApiResult(models.@"repository-ruleset") {
                return reposCreateOrgRulesetResult(client, org, requestBody);
            }
            pub fn reposdeleteorgruleset(client: *Client, org: []const u8, ruleset_id: i64) !void {
                return reposDeleteOrgRuleset(client, org, ruleset_id);
            }
            pub fn reposgetorgruleset(client: *Client, org: []const u8, ruleset_id: i64) !Owned(models.@"repository-ruleset") {
                return reposGetOrgRuleset(client, org, ruleset_id);
            }
            pub fn reposgetorgrulesetResult(client: *Client, org: []const u8, ruleset_id: i64) !ApiResult(models.@"repository-ruleset") {
                return reposGetOrgRulesetResult(client, org, ruleset_id);
            }
            pub fn reposgetorgrulesets(client: *Client, org: []const u8, per_page: ?i64, page: ?i64, targets: ?[]const u8) !Owned([]const std.json.Value) {
                return reposGetOrgRulesets(client, org, per_page, page, targets);
            }
            pub fn reposgetorgrulesetsResult(client: *Client, org: []const u8, per_page: ?i64, page: ?i64, targets: ?[]const u8) !ApiResult([]const std.json.Value) {
                return reposGetOrgRulesetsResult(client, org, per_page, page, targets);
            }
            pub fn reposupdateorgruleset(client: *Client, org: []const u8, ruleset_id: i64, requestBody: std.json.Value) !Owned(models.@"repository-ruleset") {
                return reposUpdateOrgRuleset(client, org, ruleset_id, requestBody);
            }
            pub fn reposupdateorgrulesetResult(client: *Client, org: []const u8, ruleset_id: i64, requestBody: std.json.Value) !ApiResult(models.@"repository-ruleset") {
                return reposUpdateOrgRulesetResult(client, org, ruleset_id, requestBody);
            }
            pub const rule_suites = struct {
                pub fn reposgetorgrulesuite(client: *Client, org: []const u8, rule_suite_id: i64) !Owned(models.@"rule-suite") {
                    return reposGetOrgRuleSuite(client, org, rule_suite_id);
                }
                pub fn reposgetorgrulesuiteResult(client: *Client, org: []const u8, rule_suite_id: i64) !ApiResult(models.@"rule-suite") {
                    return reposGetOrgRuleSuiteResult(client, org, rule_suite_id);
                }
                pub fn reposgetorgrulesuites(client: *Client, org: []const u8, ref: ?[]const u8, repository_name: ?[]const u8, time_period: ?[]const u8, actor_name: ?[]const u8, rule_suite_result: ?[]const u8, evaluate_status: ?[]const u8, per_page: ?i64, page: ?i64) !Owned(models.@"rule-suites") {
                    return reposGetOrgRuleSuites(client, org, ref, repository_name, time_period, actor_name, rule_suite_result, evaluate_status, per_page, page);
                }
                pub fn reposgetorgrulesuitesResult(client: *Client, org: []const u8, ref: ?[]const u8, repository_name: ?[]const u8, time_period: ?[]const u8, actor_name: ?[]const u8, rule_suite_result: ?[]const u8, evaluate_status: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult(models.@"rule-suites") {
                    return reposGetOrgRuleSuitesResult(client, org, ref, repository_name, time_period, actor_name, rule_suite_result, evaluate_status, per_page, page);
                }
            };
        };
    };
    pub const repos = struct {
        pub fn reposdelete(client: *Client, owner: []const u8, repo: []const u8) !void {
            return reposDelete(client, owner, repo);
        }
        pub fn reposget(client: *Client, owner: []const u8, repo: []const u8) !Owned(models.@"full-repository") {
            return reposGet(client, owner, repo);
        }
        pub fn reposgetResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult(models.@"full-repository") {
            return reposGetResult(client, owner, repo);
        }
        pub fn reposupdate(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.@"full-repository") {
            return reposUpdate(client, owner, repo, requestBody);
        }
        pub fn reposupdateResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.@"full-repository") {
            return reposUpdateResult(client, owner, repo, requestBody);
        }
        pub const activity = struct {
            pub fn reposlistactivities(client: *Client, owner: []const u8, repo: []const u8, direction: ?[]const u8, per_page: ?i64, before: ?[]const u8, after: ?[]const u8, ref: ?[]const u8, actor: ?[]const u8, time_period: ?[]const u8, activity_type: ?[]const u8) !Owned([]const std.json.Value) {
                return reposListActivities(client, owner, repo, direction, per_page, before, after, ref, actor, time_period, activity_type);
            }
            pub fn reposlistactivitiesResult(client: *Client, owner: []const u8, repo: []const u8, direction: ?[]const u8, per_page: ?i64, before: ?[]const u8, after: ?[]const u8, ref: ?[]const u8, actor: ?[]const u8, time_period: ?[]const u8, activity_type: ?[]const u8) !ApiResult([]const std.json.Value) {
                return reposListActivitiesResult(client, owner, repo, direction, per_page, before, after, ref, actor, time_period, activity_type);
            }
        };
        pub const assignees = struct {
            pub fn issuescheckusercanbeassigned(client: *Client, owner: []const u8, repo: []const u8, assignee: []const u8) !void {
                return issuesCheckUserCanBeAssigned(client, owner, repo, assignee);
            }
            pub fn issueslistassignees(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                return issuesListAssignees(client, owner, repo, per_page, page);
            }
            pub fn issueslistassigneesResult(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                return issuesListAssigneesResult(client, owner, repo, per_page, page);
            }
        };
        pub const attestations = struct {
            pub fn reposcreateattestation(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(std.json.Value) {
                return reposCreateAttestation(client, owner, repo, requestBody);
            }
            pub fn reposcreateattestationResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(std.json.Value) {
                return reposCreateAttestationResult(client, owner, repo, requestBody);
            }
            pub fn reposlistattestations(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, before: ?[]const u8, after: ?[]const u8, subject_digest: []const u8, predicate_type: ?[]const u8) !Owned(std.json.Value) {
                return reposListAttestations(client, owner, repo, per_page, before, after, subject_digest, predicate_type);
            }
            pub fn reposlistattestationsResult(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, before: ?[]const u8, after: ?[]const u8, subject_digest: []const u8, predicate_type: ?[]const u8) !ApiResult(std.json.Value) {
                return reposListAttestationsResult(client, owner, repo, per_page, before, after, subject_digest, predicate_type);
            }
        };
        pub const autolinks = struct {
            pub fn reposcreateautolink(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.autolink) {
                return reposCreateAutolink(client, owner, repo, requestBody);
            }
            pub fn reposcreateautolinkResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.autolink) {
                return reposCreateAutolinkResult(client, owner, repo, requestBody);
            }
            pub fn reposdeleteautolink(client: *Client, owner: []const u8, repo: []const u8, autolink_id: i64) !void {
                return reposDeleteAutolink(client, owner, repo, autolink_id);
            }
            pub fn reposgetautolink(client: *Client, owner: []const u8, repo: []const u8, autolink_id: i64) !Owned(models.autolink) {
                return reposGetAutolink(client, owner, repo, autolink_id);
            }
            pub fn reposgetautolinkResult(client: *Client, owner: []const u8, repo: []const u8, autolink_id: i64) !ApiResult(models.autolink) {
                return reposGetAutolinkResult(client, owner, repo, autolink_id);
            }
            pub fn reposlistautolinks(client: *Client, owner: []const u8, repo: []const u8) !Owned([]const std.json.Value) {
                return reposListAutolinks(client, owner, repo);
            }
            pub fn reposlistautolinksResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult([]const std.json.Value) {
                return reposListAutolinksResult(client, owner, repo);
            }
        };
        pub const automated_security_fixes = struct {
            pub fn reposcheckautomatedsecurityfixes(client: *Client, owner: []const u8, repo: []const u8) !Owned(models.@"check-automated-security-fixes") {
                return reposCheckAutomatedSecurityFixes(client, owner, repo);
            }
            pub fn reposcheckautomatedsecurityfixesResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult(models.@"check-automated-security-fixes") {
                return reposCheckAutomatedSecurityFixesResult(client, owner, repo);
            }
            pub fn reposdisableautomatedsecurityfixes(client: *Client, owner: []const u8, repo: []const u8) !void {
                return reposDisableAutomatedSecurityFixes(client, owner, repo);
            }
            pub fn reposenableautomatedsecurityfixes(client: *Client, owner: []const u8, repo: []const u8) !void {
                return reposEnableAutomatedSecurityFixes(client, owner, repo);
            }
        };
        pub const branches = struct {
            pub fn reposgetbranch(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !Owned(models.@"branch-with-protection") {
                return reposGetBranch(client, owner, repo, branch);
            }
            pub fn reposgetbranchResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !ApiResult(models.@"branch-with-protection") {
                return reposGetBranchResult(client, owner, repo, branch);
            }
            pub fn reposlistbranches(client: *Client, owner: []const u8, repo: []const u8, protected: ?bool, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                return reposListBranches(client, owner, repo, protected, per_page, page);
            }
            pub fn reposlistbranchesResult(client: *Client, owner: []const u8, repo: []const u8, protected: ?bool, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                return reposListBranchesResult(client, owner, repo, protected, per_page, page);
            }
            pub const protection = struct {
                pub fn reposdeletebranchprotection(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !void {
                    return reposDeleteBranchProtection(client, owner, repo, branch);
                }
                pub fn reposgetbranchprotection(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !Owned(models.@"branch-protection") {
                    return reposGetBranchProtection(client, owner, repo, branch);
                }
                pub fn reposgetbranchprotectionResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !ApiResult(models.@"branch-protection") {
                    return reposGetBranchProtectionResult(client, owner, repo, branch);
                }
                pub fn reposupdatebranchprotection(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned(models.@"protected-branch") {
                    return reposUpdateBranchProtection(client, owner, repo, branch, requestBody);
                }
                pub fn reposupdatebranchprotectionResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult(models.@"protected-branch") {
                    return reposUpdateBranchProtectionResult(client, owner, repo, branch, requestBody);
                }
                pub const enforce_admins = struct {
                    pub fn reposdeleteadminbranchprotection(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !void {
                        return reposDeleteAdminBranchProtection(client, owner, repo, branch);
                    }
                    pub fn reposgetadminbranchprotection(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !Owned(models.@"protected-branch-admin-enforced") {
                        return reposGetAdminBranchProtection(client, owner, repo, branch);
                    }
                    pub fn reposgetadminbranchprotectionResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !ApiResult(models.@"protected-branch-admin-enforced") {
                        return reposGetAdminBranchProtectionResult(client, owner, repo, branch);
                    }
                    pub fn repossetadminbranchprotection(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !Owned(models.@"protected-branch-admin-enforced") {
                        return reposSetAdminBranchProtection(client, owner, repo, branch);
                    }
                    pub fn repossetadminbranchprotectionResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !ApiResult(models.@"protected-branch-admin-enforced") {
                        return reposSetAdminBranchProtectionResult(client, owner, repo, branch);
                    }
                };
                pub const required_pull_request_reviews = struct {
                    pub fn reposdeletepullrequestreviewprotection(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !void {
                        return reposDeletePullRequestReviewProtection(client, owner, repo, branch);
                    }
                    pub fn reposgetpullrequestreviewprotection(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !Owned(models.@"protected-branch-pull-request-review") {
                        return reposGetPullRequestReviewProtection(client, owner, repo, branch);
                    }
                    pub fn reposgetpullrequestreviewprotectionResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !ApiResult(models.@"protected-branch-pull-request-review") {
                        return reposGetPullRequestReviewProtectionResult(client, owner, repo, branch);
                    }
                    pub fn reposupdatepullrequestreviewprotection(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned(models.@"protected-branch-pull-request-review") {
                        return reposUpdatePullRequestReviewProtection(client, owner, repo, branch, requestBody);
                    }
                    pub fn reposupdatepullrequestreviewprotectionResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult(models.@"protected-branch-pull-request-review") {
                        return reposUpdatePullRequestReviewProtectionResult(client, owner, repo, branch, requestBody);
                    }
                };
                pub const required_signatures = struct {
                    pub fn reposcreatecommitsignatureprotection(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !Owned(models.@"protected-branch-admin-enforced") {
                        return reposCreateCommitSignatureProtection(client, owner, repo, branch);
                    }
                    pub fn reposcreatecommitsignatureprotectionResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !ApiResult(models.@"protected-branch-admin-enforced") {
                        return reposCreateCommitSignatureProtectionResult(client, owner, repo, branch);
                    }
                    pub fn reposdeletecommitsignatureprotection(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !void {
                        return reposDeleteCommitSignatureProtection(client, owner, repo, branch);
                    }
                    pub fn reposgetcommitsignatureprotection(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !Owned(models.@"protected-branch-admin-enforced") {
                        return reposGetCommitSignatureProtection(client, owner, repo, branch);
                    }
                    pub fn reposgetcommitsignatureprotectionResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !ApiResult(models.@"protected-branch-admin-enforced") {
                        return reposGetCommitSignatureProtectionResult(client, owner, repo, branch);
                    }
                };
                pub const required_status_checks = struct {
                    pub fn reposgetstatuschecksprotection(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !Owned(models.@"status-check-policy") {
                        return reposGetStatusChecksProtection(client, owner, repo, branch);
                    }
                    pub fn reposgetstatuschecksprotectionResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !ApiResult(models.@"status-check-policy") {
                        return reposGetStatusChecksProtectionResult(client, owner, repo, branch);
                    }
                    pub fn reposremovestatuscheckprotection(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !void {
                        return reposRemoveStatusCheckProtection(client, owner, repo, branch);
                    }
                    pub fn reposupdatestatuscheckprotection(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned(models.@"status-check-policy") {
                        return reposUpdateStatusCheckProtection(client, owner, repo, branch, requestBody);
                    }
                    pub fn reposupdatestatuscheckprotectionResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult(models.@"status-check-policy") {
                        return reposUpdateStatusCheckProtectionResult(client, owner, repo, branch, requestBody);
                    }
                    pub const contexts = struct {
                        pub fn reposaddstatuscheckcontexts(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned([]const std.json.Value) {
                            return reposAddStatusCheckContexts(client, owner, repo, branch, requestBody);
                        }
                        pub fn reposaddstatuscheckcontextsResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
                            return reposAddStatusCheckContextsResult(client, owner, repo, branch, requestBody);
                        }
                        pub fn reposgetallstatuscheckcontexts(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !Owned([]const std.json.Value) {
                            return reposGetAllStatusCheckContexts(client, owner, repo, branch);
                        }
                        pub fn reposgetallstatuscheckcontextsResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !ApiResult([]const std.json.Value) {
                            return reposGetAllStatusCheckContextsResult(client, owner, repo, branch);
                        }
                        pub fn reposremovestatuscheckcontexts(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned([]const std.json.Value) {
                            return reposRemoveStatusCheckContexts(client, owner, repo, branch, requestBody);
                        }
                        pub fn reposremovestatuscheckcontextsResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
                            return reposRemoveStatusCheckContextsResult(client, owner, repo, branch, requestBody);
                        }
                        pub fn repossetstatuscheckcontexts(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned([]const std.json.Value) {
                            return reposSetStatusCheckContexts(client, owner, repo, branch, requestBody);
                        }
                        pub fn repossetstatuscheckcontextsResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
                            return reposSetStatusCheckContextsResult(client, owner, repo, branch, requestBody);
                        }
                    };
                };
                pub const restrictions = struct {
                    pub fn reposdeleteaccessrestrictions(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !void {
                        return reposDeleteAccessRestrictions(client, owner, repo, branch);
                    }
                    pub fn reposgetaccessrestrictions(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !Owned(models.@"branch-restriction-policy") {
                        return reposGetAccessRestrictions(client, owner, repo, branch);
                    }
                    pub fn reposgetaccessrestrictionsResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !ApiResult(models.@"branch-restriction-policy") {
                        return reposGetAccessRestrictionsResult(client, owner, repo, branch);
                    }
                    pub const apps = struct {
                        pub fn reposaddappaccessrestrictions(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned([]const std.json.Value) {
                            return reposAddAppAccessRestrictions(client, owner, repo, branch, requestBody);
                        }
                        pub fn reposaddappaccessrestrictionsResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
                            return reposAddAppAccessRestrictionsResult(client, owner, repo, branch, requestBody);
                        }
                        pub fn reposgetappswithaccesstoprotectedbranch(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !Owned([]const std.json.Value) {
                            return reposGetAppsWithAccessToProtectedBranch(client, owner, repo, branch);
                        }
                        pub fn reposgetappswithaccesstoprotectedbranchResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !ApiResult([]const std.json.Value) {
                            return reposGetAppsWithAccessToProtectedBranchResult(client, owner, repo, branch);
                        }
                        pub fn reposremoveappaccessrestrictions(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned([]const std.json.Value) {
                            return reposRemoveAppAccessRestrictions(client, owner, repo, branch, requestBody);
                        }
                        pub fn reposremoveappaccessrestrictionsResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
                            return reposRemoveAppAccessRestrictionsResult(client, owner, repo, branch, requestBody);
                        }
                        pub fn repossetappaccessrestrictions(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned([]const std.json.Value) {
                            return reposSetAppAccessRestrictions(client, owner, repo, branch, requestBody);
                        }
                        pub fn repossetappaccessrestrictionsResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
                            return reposSetAppAccessRestrictionsResult(client, owner, repo, branch, requestBody);
                        }
                    };
                    pub const teams = struct {
                        pub fn reposaddteamaccessrestrictions(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned([]const std.json.Value) {
                            return reposAddTeamAccessRestrictions(client, owner, repo, branch, requestBody);
                        }
                        pub fn reposaddteamaccessrestrictionsResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
                            return reposAddTeamAccessRestrictionsResult(client, owner, repo, branch, requestBody);
                        }
                        pub fn reposgetteamswithaccesstoprotectedbranch(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !Owned([]const std.json.Value) {
                            return reposGetTeamsWithAccessToProtectedBranch(client, owner, repo, branch);
                        }
                        pub fn reposgetteamswithaccesstoprotectedbranchResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !ApiResult([]const std.json.Value) {
                            return reposGetTeamsWithAccessToProtectedBranchResult(client, owner, repo, branch);
                        }
                        pub fn reposremoveteamaccessrestrictions(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned([]const std.json.Value) {
                            return reposRemoveTeamAccessRestrictions(client, owner, repo, branch, requestBody);
                        }
                        pub fn reposremoveteamaccessrestrictionsResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
                            return reposRemoveTeamAccessRestrictionsResult(client, owner, repo, branch, requestBody);
                        }
                        pub fn repossetteamaccessrestrictions(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned([]const std.json.Value) {
                            return reposSetTeamAccessRestrictions(client, owner, repo, branch, requestBody);
                        }
                        pub fn repossetteamaccessrestrictionsResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
                            return reposSetTeamAccessRestrictionsResult(client, owner, repo, branch, requestBody);
                        }
                    };
                    pub const users = struct {
                        pub fn reposadduseraccessrestrictions(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned([]const std.json.Value) {
                            return reposAddUserAccessRestrictions(client, owner, repo, branch, requestBody);
                        }
                        pub fn reposadduseraccessrestrictionsResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
                            return reposAddUserAccessRestrictionsResult(client, owner, repo, branch, requestBody);
                        }
                        pub fn reposgetuserswithaccesstoprotectedbranch(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !Owned([]const std.json.Value) {
                            return reposGetUsersWithAccessToProtectedBranch(client, owner, repo, branch);
                        }
                        pub fn reposgetuserswithaccesstoprotectedbranchResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8) !ApiResult([]const std.json.Value) {
                            return reposGetUsersWithAccessToProtectedBranchResult(client, owner, repo, branch);
                        }
                        pub fn reposremoveuseraccessrestrictions(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned([]const std.json.Value) {
                            return reposRemoveUserAccessRestrictions(client, owner, repo, branch, requestBody);
                        }
                        pub fn reposremoveuseraccessrestrictionsResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
                            return reposRemoveUserAccessRestrictionsResult(client, owner, repo, branch, requestBody);
                        }
                        pub fn repossetuseraccessrestrictions(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned([]const std.json.Value) {
                            return reposSetUserAccessRestrictions(client, owner, repo, branch, requestBody);
                        }
                        pub fn repossetuseraccessrestrictionsResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
                            return reposSetUserAccessRestrictionsResult(client, owner, repo, branch, requestBody);
                        }
                    };
                };
            };
            pub const rename = struct {
                pub fn reposrenamebranch(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !Owned(models.@"branch-with-protection") {
                    return reposRenameBranch(client, owner, repo, branch, requestBody);
                }
                pub fn reposrenamebranchResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, requestBody: std.json.Value) !ApiResult(models.@"branch-with-protection") {
                    return reposRenameBranchResult(client, owner, repo, branch, requestBody);
                }
            };
        };
        pub const codeowners = struct {
            pub const errors = struct {
                pub fn reposcodeownerserrors(client: *Client, owner: []const u8, repo: []const u8, ref: ?[]const u8) !Owned(models.@"codeowners-errors") {
                    return reposCodeownersErrors(client, owner, repo, ref);
                }
                pub fn reposcodeownerserrorsResult(client: *Client, owner: []const u8, repo: []const u8, ref: ?[]const u8) !ApiResult(models.@"codeowners-errors") {
                    return reposCodeownersErrorsResult(client, owner, repo, ref);
                }
            };
        };
        pub const collaborators = struct {
            pub fn reposaddcollaborator(client: *Client, owner: []const u8, repo: []const u8, username: []const u8, requestBody: std.json.Value) !Owned(models.@"repository-invitation") {
                return reposAddCollaborator(client, owner, repo, username, requestBody);
            }
            pub fn reposaddcollaboratorResult(client: *Client, owner: []const u8, repo: []const u8, username: []const u8, requestBody: std.json.Value) !ApiResult(models.@"repository-invitation") {
                return reposAddCollaboratorResult(client, owner, repo, username, requestBody);
            }
            pub fn reposcheckcollaborator(client: *Client, owner: []const u8, repo: []const u8, username: []const u8) !void {
                return reposCheckCollaborator(client, owner, repo, username);
            }
            pub fn reposlistcollaborators(client: *Client, owner: []const u8, repo: []const u8, affiliation: ?[]const u8, permission_param: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                return reposListCollaborators(client, owner, repo, affiliation, permission_param, per_page, page);
            }
            pub fn reposlistcollaboratorsResult(client: *Client, owner: []const u8, repo: []const u8, affiliation: ?[]const u8, permission_param: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                return reposListCollaboratorsResult(client, owner, repo, affiliation, permission_param, per_page, page);
            }
            pub fn reposremovecollaborator(client: *Client, owner: []const u8, repo: []const u8, username: []const u8) !void {
                return reposRemoveCollaborator(client, owner, repo, username);
            }
            pub const permission = struct {
                pub fn reposgetcollaboratorpermissionlevel(client: *Client, owner: []const u8, repo: []const u8, username: []const u8) !Owned(models.@"repository-collaborator-permission") {
                    return reposGetCollaboratorPermissionLevel(client, owner, repo, username);
                }
                pub fn reposgetcollaboratorpermissionlevelResult(client: *Client, owner: []const u8, repo: []const u8, username: []const u8) !ApiResult(models.@"repository-collaborator-permission") {
                    return reposGetCollaboratorPermissionLevelResult(client, owner, repo, username);
                }
            };
        };
        pub const comments = struct {
            pub fn reposdeletecommitcomment(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !void {
                return reposDeleteCommitComment(client, owner, repo, comment_id);
            }
            pub fn reposgetcommitcomment(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !Owned(models.@"commit-comment") {
                return reposGetCommitComment(client, owner, repo, comment_id);
            }
            pub fn reposgetcommitcommentResult(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !ApiResult(models.@"commit-comment") {
                return reposGetCommitCommentResult(client, owner, repo, comment_id);
            }
            pub fn reposlistcommitcommentsforrepo(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                return reposListCommitCommentsForRepo(client, owner, repo, per_page, page);
            }
            pub fn reposlistcommitcommentsforrepoResult(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                return reposListCommitCommentsForRepoResult(client, owner, repo, per_page, page);
            }
            pub fn reposupdatecommitcomment(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64, requestBody: std.json.Value) !Owned(models.@"commit-comment") {
                return reposUpdateCommitComment(client, owner, repo, comment_id, requestBody);
            }
            pub fn reposupdatecommitcommentResult(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64, requestBody: std.json.Value) !ApiResult(models.@"commit-comment") {
                return reposUpdateCommitCommentResult(client, owner, repo, comment_id, requestBody);
            }
        };
        pub const commits = struct {
            pub fn reposgetcommit(client: *Client, owner: []const u8, repo: []const u8, page: ?i64, per_page: ?i64, ref: []const u8) !Owned(models.commit) {
                return reposGetCommit(client, owner, repo, page, per_page, ref);
            }
            pub fn reposgetcommitResult(client: *Client, owner: []const u8, repo: []const u8, page: ?i64, per_page: ?i64, ref: []const u8) !ApiResult(models.commit) {
                return reposGetCommitResult(client, owner, repo, page, per_page, ref);
            }
            pub fn reposlistcommits(client: *Client, owner: []const u8, repo: []const u8, sha: ?[]const u8, path: ?[]const u8, author: ?[]const u8, committer: ?[]const u8, since: ?[]const u8, until: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                return reposListCommits(client, owner, repo, sha, path, author, committer, since, until, per_page, page);
            }
            pub fn reposlistcommitsResult(client: *Client, owner: []const u8, repo: []const u8, sha: ?[]const u8, path: ?[]const u8, author: ?[]const u8, committer: ?[]const u8, since: ?[]const u8, until: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                return reposListCommitsResult(client, owner, repo, sha, path, author, committer, since, until, per_page, page);
            }
            pub const branches_where_head = struct {
                pub fn reposlistbranchesforheadcommit(client: *Client, owner: []const u8, repo: []const u8, commit_sha: []const u8) !Owned([]const std.json.Value) {
                    return reposListBranchesForHeadCommit(client, owner, repo, commit_sha);
                }
                pub fn reposlistbranchesforheadcommitResult(client: *Client, owner: []const u8, repo: []const u8, commit_sha: []const u8) !ApiResult([]const std.json.Value) {
                    return reposListBranchesForHeadCommitResult(client, owner, repo, commit_sha);
                }
            };
            pub const comments = struct {
                pub fn reposcreatecommitcomment(client: *Client, owner: []const u8, repo: []const u8, commit_sha: []const u8, requestBody: std.json.Value) !Owned(models.@"commit-comment") {
                    return reposCreateCommitComment(client, owner, repo, commit_sha, requestBody);
                }
                pub fn reposcreatecommitcommentResult(client: *Client, owner: []const u8, repo: []const u8, commit_sha: []const u8, requestBody: std.json.Value) !ApiResult(models.@"commit-comment") {
                    return reposCreateCommitCommentResult(client, owner, repo, commit_sha, requestBody);
                }
                pub fn reposlistcommentsforcommit(client: *Client, owner: []const u8, repo: []const u8, commit_sha: []const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                    return reposListCommentsForCommit(client, owner, repo, commit_sha, per_page, page);
                }
                pub fn reposlistcommentsforcommitResult(client: *Client, owner: []const u8, repo: []const u8, commit_sha: []const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                    return reposListCommentsForCommitResult(client, owner, repo, commit_sha, per_page, page);
                }
            };
            pub const pulls = struct {
                pub fn reposlistpullrequestsassociatedwithcommit(client: *Client, owner: []const u8, repo: []const u8, commit_sha: []const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                    return reposListPullRequestsAssociatedWithCommit(client, owner, repo, commit_sha, per_page, page);
                }
                pub fn reposlistpullrequestsassociatedwithcommitResult(client: *Client, owner: []const u8, repo: []const u8, commit_sha: []const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                    return reposListPullRequestsAssociatedWithCommitResult(client, owner, repo, commit_sha, per_page, page);
                }
            };
            pub const status = struct {
                pub fn reposgetcombinedstatusforref(client: *Client, owner: []const u8, repo: []const u8, ref: []const u8, per_page: ?i64, page: ?i64) !Owned(models.@"combined-commit-status") {
                    return reposGetCombinedStatusForRef(client, owner, repo, ref, per_page, page);
                }
                pub fn reposgetcombinedstatusforrefResult(client: *Client, owner: []const u8, repo: []const u8, ref: []const u8, per_page: ?i64, page: ?i64) !ApiResult(models.@"combined-commit-status") {
                    return reposGetCombinedStatusForRefResult(client, owner, repo, ref, per_page, page);
                }
            };
            pub const statuses = struct {
                pub fn reposlistcommitstatusesforref(client: *Client, owner: []const u8, repo: []const u8, ref: []const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                    return reposListCommitStatusesForRef(client, owner, repo, ref, per_page, page);
                }
                pub fn reposlistcommitstatusesforrefResult(client: *Client, owner: []const u8, repo: []const u8, ref: []const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                    return reposListCommitStatusesForRefResult(client, owner, repo, ref, per_page, page);
                }
            };
        };
        pub const community = struct {
            pub const profile = struct {
                pub fn reposgetcommunityprofilemetrics(client: *Client, owner: []const u8, repo: []const u8) !Owned(models.@"community-profile") {
                    return reposGetCommunityProfileMetrics(client, owner, repo);
                }
                pub fn reposgetcommunityprofilemetricsResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult(models.@"community-profile") {
                    return reposGetCommunityProfileMetricsResult(client, owner, repo);
                }
            };
        };
        pub const compare = struct {
            pub fn reposcomparecommits(client: *Client, owner: []const u8, repo: []const u8, page: ?i64, per_page: ?i64, basehead: []const u8) !Owned(models.@"commit-comparison") {
                return reposCompareCommits(client, owner, repo, page, per_page, basehead);
            }
            pub fn reposcomparecommitsResult(client: *Client, owner: []const u8, repo: []const u8, page: ?i64, per_page: ?i64, basehead: []const u8) !ApiResult(models.@"commit-comparison") {
                return reposCompareCommitsResult(client, owner, repo, page, per_page, basehead);
            }
        };
        pub const contents = struct {
            pub fn reposcreateorupdatefilecontents(client: *Client, owner: []const u8, repo: []const u8, path: []const u8, requestBody: std.json.Value) !Owned(models.@"file-commit") {
                return reposCreateOrUpdateFileContents(client, owner, repo, path, requestBody);
            }
            pub fn reposcreateorupdatefilecontentsResult(client: *Client, owner: []const u8, repo: []const u8, path: []const u8, requestBody: std.json.Value) !ApiResult(models.@"file-commit") {
                return reposCreateOrUpdateFileContentsResult(client, owner, repo, path, requestBody);
            }
            pub fn reposdeletefile(client: *Client, owner: []const u8, repo: []const u8, path: []const u8, requestBody: std.json.Value) !Owned(models.@"file-commit") {
                return reposDeleteFile(client, owner, repo, path, requestBody);
            }
            pub fn reposdeletefileResult(client: *Client, owner: []const u8, repo: []const u8, path: []const u8, requestBody: std.json.Value) !ApiResult(models.@"file-commit") {
                return reposDeleteFileResult(client, owner, repo, path, requestBody);
            }
            pub fn reposgetcontent(client: *Client, owner: []const u8, repo: []const u8, path: []const u8, ref: ?[]const u8, requestBody: std.json.Value) !Owned(std.json.Value) {
                return reposGetContent(client, owner, repo, path, ref, requestBody);
            }
            pub fn reposgetcontentResult(client: *Client, owner: []const u8, repo: []const u8, path: []const u8, ref: ?[]const u8, requestBody: std.json.Value) !ApiResult(std.json.Value) {
                return reposGetContentResult(client, owner, repo, path, ref, requestBody);
            }
        };
        pub const contributors = struct {
            pub fn reposlistcontributors(client: *Client, owner: []const u8, repo: []const u8, anon: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                return reposListContributors(client, owner, repo, anon, per_page, page);
            }
            pub fn reposlistcontributorsResult(client: *Client, owner: []const u8, repo: []const u8, anon: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                return reposListContributorsResult(client, owner, repo, anon, per_page, page);
            }
        };
        pub const deployments = struct {
            pub fn reposcreatedeployment(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.deployment) {
                return reposCreateDeployment(client, owner, repo, requestBody);
            }
            pub fn reposcreatedeploymentResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.deployment) {
                return reposCreateDeploymentResult(client, owner, repo, requestBody);
            }
            pub fn reposdeletedeployment(client: *Client, owner: []const u8, repo: []const u8, deployment_id: i64) !void {
                return reposDeleteDeployment(client, owner, repo, deployment_id);
            }
            pub fn reposgetdeployment(client: *Client, owner: []const u8, repo: []const u8, deployment_id: i64) !Owned(models.deployment) {
                return reposGetDeployment(client, owner, repo, deployment_id);
            }
            pub fn reposgetdeploymentResult(client: *Client, owner: []const u8, repo: []const u8, deployment_id: i64) !ApiResult(models.deployment) {
                return reposGetDeploymentResult(client, owner, repo, deployment_id);
            }
            pub fn reposlistdeployments(client: *Client, owner: []const u8, repo: []const u8, sha: ?[]const u8, ref: ?[]const u8, task: ?[]const u8, environment: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                return reposListDeployments(client, owner, repo, sha, ref, task, environment, per_page, page);
            }
            pub fn reposlistdeploymentsResult(client: *Client, owner: []const u8, repo: []const u8, sha: ?[]const u8, ref: ?[]const u8, task: ?[]const u8, environment: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                return reposListDeploymentsResult(client, owner, repo, sha, ref, task, environment, per_page, page);
            }
            pub const statuses = struct {
                pub fn reposcreatedeploymentstatus(client: *Client, owner: []const u8, repo: []const u8, deployment_id: i64, requestBody: std.json.Value) !Owned(models.@"deployment-status") {
                    return reposCreateDeploymentStatus(client, owner, repo, deployment_id, requestBody);
                }
                pub fn reposcreatedeploymentstatusResult(client: *Client, owner: []const u8, repo: []const u8, deployment_id: i64, requestBody: std.json.Value) !ApiResult(models.@"deployment-status") {
                    return reposCreateDeploymentStatusResult(client, owner, repo, deployment_id, requestBody);
                }
                pub fn reposgetdeploymentstatus(client: *Client, owner: []const u8, repo: []const u8, deployment_id: i64, status_id: i64) !Owned(models.@"deployment-status") {
                    return reposGetDeploymentStatus(client, owner, repo, deployment_id, status_id);
                }
                pub fn reposgetdeploymentstatusResult(client: *Client, owner: []const u8, repo: []const u8, deployment_id: i64, status_id: i64) !ApiResult(models.@"deployment-status") {
                    return reposGetDeploymentStatusResult(client, owner, repo, deployment_id, status_id);
                }
                pub fn reposlistdeploymentstatuses(client: *Client, owner: []const u8, repo: []const u8, deployment_id: i64, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                    return reposListDeploymentStatuses(client, owner, repo, deployment_id, per_page, page);
                }
                pub fn reposlistdeploymentstatusesResult(client: *Client, owner: []const u8, repo: []const u8, deployment_id: i64, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                    return reposListDeploymentStatusesResult(client, owner, repo, deployment_id, per_page, page);
                }
            };
        };
        pub const dispatches = struct {
            pub fn reposcreatedispatchevent(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !void {
                return reposCreateDispatchEvent(client, owner, repo, requestBody);
            }
        };
        pub const environments = struct {
            pub fn reposcreateorupdateenvironment(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, requestBody: std.json.Value) !Owned(models.environment) {
                return reposCreateOrUpdateEnvironment(client, owner, repo, environment_name, requestBody);
            }
            pub fn reposcreateorupdateenvironmentResult(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, requestBody: std.json.Value) !ApiResult(models.environment) {
                return reposCreateOrUpdateEnvironmentResult(client, owner, repo, environment_name, requestBody);
            }
            pub fn reposdeleteanenvironment(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8) !void {
                return reposDeleteAnEnvironment(client, owner, repo, environment_name);
            }
            pub fn reposgetallenvironments(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !Owned(std.json.Value) {
                return reposGetAllEnvironments(client, owner, repo, per_page, page);
            }
            pub fn reposgetallenvironmentsResult(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !ApiResult(std.json.Value) {
                return reposGetAllEnvironmentsResult(client, owner, repo, per_page, page);
            }
            pub fn reposgetenvironment(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8) !Owned(models.environment) {
                return reposGetEnvironment(client, owner, repo, environment_name);
            }
            pub fn reposgetenvironmentResult(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8) !ApiResult(models.environment) {
                return reposGetEnvironmentResult(client, owner, repo, environment_name);
            }
            pub const deployment_branch_policies = struct {
                pub fn reposcreatedeploymentbranchpolicy(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, requestBody: models.@"deployment-branch-policy-name-pattern-with-type") !Owned(models.@"deployment-branch-policy") {
                    return reposCreateDeploymentBranchPolicy(client, owner, repo, environment_name, requestBody);
                }
                pub fn reposcreatedeploymentbranchpolicyResult(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, requestBody: models.@"deployment-branch-policy-name-pattern-with-type") !ApiResult(models.@"deployment-branch-policy") {
                    return reposCreateDeploymentBranchPolicyResult(client, owner, repo, environment_name, requestBody);
                }
                pub fn reposdeletedeploymentbranchpolicy(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, branch_policy_id: i64) !void {
                    return reposDeleteDeploymentBranchPolicy(client, owner, repo, environment_name, branch_policy_id);
                }
                pub fn reposgetdeploymentbranchpolicy(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, branch_policy_id: i64) !Owned(models.@"deployment-branch-policy") {
                    return reposGetDeploymentBranchPolicy(client, owner, repo, environment_name, branch_policy_id);
                }
                pub fn reposgetdeploymentbranchpolicyResult(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, branch_policy_id: i64) !ApiResult(models.@"deployment-branch-policy") {
                    return reposGetDeploymentBranchPolicyResult(client, owner, repo, environment_name, branch_policy_id);
                }
                pub fn reposlistdeploymentbranchpolicies(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, per_page: ?i64, page: ?i64) !Owned(std.json.Value) {
                    return reposListDeploymentBranchPolicies(client, owner, repo, environment_name, per_page, page);
                }
                pub fn reposlistdeploymentbranchpoliciesResult(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, per_page: ?i64, page: ?i64) !ApiResult(std.json.Value) {
                    return reposListDeploymentBranchPoliciesResult(client, owner, repo, environment_name, per_page, page);
                }
                pub fn reposupdatedeploymentbranchpolicy(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, branch_policy_id: i64, requestBody: models.@"deployment-branch-policy-name-pattern") !Owned(models.@"deployment-branch-policy") {
                    return reposUpdateDeploymentBranchPolicy(client, owner, repo, environment_name, branch_policy_id, requestBody);
                }
                pub fn reposupdatedeploymentbranchpolicyResult(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, branch_policy_id: i64, requestBody: models.@"deployment-branch-policy-name-pattern") !ApiResult(models.@"deployment-branch-policy") {
                    return reposUpdateDeploymentBranchPolicyResult(client, owner, repo, environment_name, branch_policy_id, requestBody);
                }
            };
            pub const deployment_protection_rules = struct {
                pub fn reposcreatedeploymentprotectionrule(client: *Client, environment_name: []const u8, repo: []const u8, owner: []const u8, requestBody: std.json.Value) !Owned(models.@"deployment-protection-rule") {
                    return reposCreateDeploymentProtectionRule(client, environment_name, repo, owner, requestBody);
                }
                pub fn reposcreatedeploymentprotectionruleResult(client: *Client, environment_name: []const u8, repo: []const u8, owner: []const u8, requestBody: std.json.Value) !ApiResult(models.@"deployment-protection-rule") {
                    return reposCreateDeploymentProtectionRuleResult(client, environment_name, repo, owner, requestBody);
                }
                pub fn reposdisabledeploymentprotectionrule(client: *Client, environment_name: []const u8, repo: []const u8, owner: []const u8, protection_rule_id: i64) !void {
                    return reposDisableDeploymentProtectionRule(client, environment_name, repo, owner, protection_rule_id);
                }
                pub fn reposgetalldeploymentprotectionrules(client: *Client, environment_name: []const u8, repo: []const u8, owner: []const u8) !Owned(std.json.Value) {
                    return reposGetAllDeploymentProtectionRules(client, environment_name, repo, owner);
                }
                pub fn reposgetalldeploymentprotectionrulesResult(client: *Client, environment_name: []const u8, repo: []const u8, owner: []const u8) !ApiResult(std.json.Value) {
                    return reposGetAllDeploymentProtectionRulesResult(client, environment_name, repo, owner);
                }
                pub fn reposgetcustomdeploymentprotectionrule(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, protection_rule_id: i64) !Owned(models.@"deployment-protection-rule") {
                    return reposGetCustomDeploymentProtectionRule(client, owner, repo, environment_name, protection_rule_id);
                }
                pub fn reposgetcustomdeploymentprotectionruleResult(client: *Client, owner: []const u8, repo: []const u8, environment_name: []const u8, protection_rule_id: i64) !ApiResult(models.@"deployment-protection-rule") {
                    return reposGetCustomDeploymentProtectionRuleResult(client, owner, repo, environment_name, protection_rule_id);
                }
                pub const apps = struct {
                    pub fn reposlistcustomdeploymentruleintegrations(client: *Client, environment_name: []const u8, repo: []const u8, owner: []const u8, page: ?i64, per_page: ?i64) !Owned(std.json.Value) {
                        return reposListCustomDeploymentRuleIntegrations(client, environment_name, repo, owner, page, per_page);
                    }
                    pub fn reposlistcustomdeploymentruleintegrationsResult(client: *Client, environment_name: []const u8, repo: []const u8, owner: []const u8, page: ?i64, per_page: ?i64) !ApiResult(std.json.Value) {
                        return reposListCustomDeploymentRuleIntegrationsResult(client, environment_name, repo, owner, page, per_page);
                    }
                };
            };
        };
        pub const forks = struct {
            pub fn reposcreatefork(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.@"full-repository") {
                return reposCreateFork(client, owner, repo, requestBody);
            }
            pub fn reposcreateforkResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.@"full-repository") {
                return reposCreateForkResult(client, owner, repo, requestBody);
            }
            pub fn reposlistforks(client: *Client, owner: []const u8, repo: []const u8, sort: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                return reposListForks(client, owner, repo, sort, per_page, page);
            }
            pub fn reposlistforksResult(client: *Client, owner: []const u8, repo: []const u8, sort: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                return reposListForksResult(client, owner, repo, sort, per_page, page);
            }
        };
        pub const generate = struct {
            pub fn reposcreateusingtemplate(client: *Client, template_owner: []const u8, template_repo: []const u8, requestBody: std.json.Value) !Owned(models.@"full-repository") {
                return reposCreateUsingTemplate(client, template_owner, template_repo, requestBody);
            }
            pub fn reposcreateusingtemplateResult(client: *Client, template_owner: []const u8, template_repo: []const u8, requestBody: std.json.Value) !ApiResult(models.@"full-repository") {
                return reposCreateUsingTemplateResult(client, template_owner, template_repo, requestBody);
            }
        };
        pub const hash_algorithm = struct {
            pub fn reposgethashalgorithm(client: *Client, owner: []const u8, repo: []const u8) !Owned(models.@"repository-hash-algorithm") {
                return reposGetHashAlgorithm(client, owner, repo);
            }
            pub fn reposgethashalgorithmResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult(models.@"repository-hash-algorithm") {
                return reposGetHashAlgorithmResult(client, owner, repo);
            }
        };
        pub const hooks = struct {
            pub fn reposcreatewebhook(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.hook) {
                return reposCreateWebhook(client, owner, repo, requestBody);
            }
            pub fn reposcreatewebhookResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.hook) {
                return reposCreateWebhookResult(client, owner, repo, requestBody);
            }
            pub fn reposdeletewebhook(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64) !void {
                return reposDeleteWebhook(client, owner, repo, hook_id);
            }
            pub fn reposgetwebhook(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64) !Owned(models.hook) {
                return reposGetWebhook(client, owner, repo, hook_id);
            }
            pub fn reposgetwebhookResult(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64) !ApiResult(models.hook) {
                return reposGetWebhookResult(client, owner, repo, hook_id);
            }
            pub fn reposlistwebhooks(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                return reposListWebhooks(client, owner, repo, per_page, page);
            }
            pub fn reposlistwebhooksResult(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                return reposListWebhooksResult(client, owner, repo, per_page, page);
            }
            pub fn reposupdatewebhook(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64, requestBody: std.json.Value) !Owned(models.hook) {
                return reposUpdateWebhook(client, owner, repo, hook_id, requestBody);
            }
            pub fn reposupdatewebhookResult(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64, requestBody: std.json.Value) !ApiResult(models.hook) {
                return reposUpdateWebhookResult(client, owner, repo, hook_id, requestBody);
            }
            pub const config = struct {
                pub fn reposgetwebhookconfigforrepo(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64) !Owned(models.@"webhook-config") {
                    return reposGetWebhookConfigForRepo(client, owner, repo, hook_id);
                }
                pub fn reposgetwebhookconfigforrepoResult(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64) !ApiResult(models.@"webhook-config") {
                    return reposGetWebhookConfigForRepoResult(client, owner, repo, hook_id);
                }
                pub fn reposupdatewebhookconfigforrepo(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64, requestBody: std.json.Value) !Owned(models.@"webhook-config") {
                    return reposUpdateWebhookConfigForRepo(client, owner, repo, hook_id, requestBody);
                }
                pub fn reposupdatewebhookconfigforrepoResult(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64, requestBody: std.json.Value) !ApiResult(models.@"webhook-config") {
                    return reposUpdateWebhookConfigForRepoResult(client, owner, repo, hook_id, requestBody);
                }
            };
            pub const deliveries = struct {
                pub fn reposgetwebhookdelivery(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64, delivery_id: i64) !Owned(models.@"hook-delivery") {
                    return reposGetWebhookDelivery(client, owner, repo, hook_id, delivery_id);
                }
                pub fn reposgetwebhookdeliveryResult(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64, delivery_id: i64) !ApiResult(models.@"hook-delivery") {
                    return reposGetWebhookDeliveryResult(client, owner, repo, hook_id, delivery_id);
                }
                pub fn reposlistwebhookdeliveries(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64, per_page: ?i64, cursor: ?[]const u8, status: ?[]const u8) !Owned([]const std.json.Value) {
                    return reposListWebhookDeliveries(client, owner, repo, hook_id, per_page, cursor, status);
                }
                pub fn reposlistwebhookdeliveriesResult(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64, per_page: ?i64, cursor: ?[]const u8, status: ?[]const u8) !ApiResult([]const std.json.Value) {
                    return reposListWebhookDeliveriesResult(client, owner, repo, hook_id, per_page, cursor, status);
                }
                pub const attempts = struct {
                    pub fn reposredeliverwebhookdelivery(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64, delivery_id: i64) !void {
                        return reposRedeliverWebhookDelivery(client, owner, repo, hook_id, delivery_id);
                    }
                };
            };
            pub const pings = struct {
                pub fn repospingwebhook(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64) !void {
                    return reposPingWebhook(client, owner, repo, hook_id);
                }
            };
            pub const tests = struct {
                pub fn repostestpushwebhook(client: *Client, owner: []const u8, repo: []const u8, hook_id: i64) !void {
                    return reposTestPushWebhook(client, owner, repo, hook_id);
                }
            };
        };
        pub const immutable_releases = struct {
            pub fn reposcheckimmutablereleases(client: *Client, owner: []const u8, repo: []const u8) !Owned(models.@"check-immutable-releases") {
                return reposCheckImmutableReleases(client, owner, repo);
            }
            pub fn reposcheckimmutablereleasesResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult(models.@"check-immutable-releases") {
                return reposCheckImmutableReleasesResult(client, owner, repo);
            }
            pub fn reposdisableimmutablereleases(client: *Client, owner: []const u8, repo: []const u8) !void {
                return reposDisableImmutableReleases(client, owner, repo);
            }
            pub fn reposenableimmutablereleases(client: *Client, owner: []const u8, repo: []const u8) !void {
                return reposEnableImmutableReleases(client, owner, repo);
            }
        };
        pub const invitations = struct {
            pub fn reposdeleteinvitation(client: *Client, owner: []const u8, repo: []const u8, invitation_id: i64) !void {
                return reposDeleteInvitation(client, owner, repo, invitation_id);
            }
            pub fn reposlistinvitations(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                return reposListInvitations(client, owner, repo, per_page, page);
            }
            pub fn reposlistinvitationsResult(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                return reposListInvitationsResult(client, owner, repo, per_page, page);
            }
            pub fn reposupdateinvitation(client: *Client, owner: []const u8, repo: []const u8, invitation_id: i64, requestBody: std.json.Value) !Owned(models.@"repository-invitation") {
                return reposUpdateInvitation(client, owner, repo, invitation_id, requestBody);
            }
            pub fn reposupdateinvitationResult(client: *Client, owner: []const u8, repo: []const u8, invitation_id: i64, requestBody: std.json.Value) !ApiResult(models.@"repository-invitation") {
                return reposUpdateInvitationResult(client, owner, repo, invitation_id, requestBody);
            }
        };
        pub const issue_types = struct {
            pub fn reposlistissuetypes(client: *Client, owner: []const u8, repo: []const u8) !Owned([]const std.json.Value) {
                return reposListIssueTypes(client, owner, repo);
            }
            pub fn reposlistissuetypesResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult([]const std.json.Value) {
                return reposListIssueTypesResult(client, owner, repo);
            }
        };
        pub const issues = struct {
            pub fn issuescreate(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.issue) {
                return issuesCreate(client, owner, repo, requestBody);
            }
            pub fn issuescreateResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.issue) {
                return issuesCreateResult(client, owner, repo, requestBody);
            }
            pub fn issuesget(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64) !Owned(models.issue) {
                return issuesGet(client, owner, repo, issue_number);
            }
            pub fn issuesgetResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64) !ApiResult(models.issue) {
                return issuesGetResult(client, owner, repo, issue_number);
            }
            pub fn issueslistforrepo(client: *Client, owner: []const u8, repo: []const u8, milestone: ?[]const u8, state: ?[]const u8, assignee: ?[]const u8, @"type": ?[]const u8, creator: ?[]const u8, mentioned: ?[]const u8, issue_field_values_param: ?[]const u8, labels_param: ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                return issuesListForRepo(client, owner, repo, milestone, state, assignee, @"type", creator, mentioned, issue_field_values_param, labels_param, sort, direction, since, per_page, page);
            }
            pub fn issueslistforrepoResult(client: *Client, owner: []const u8, repo: []const u8, milestone: ?[]const u8, state: ?[]const u8, assignee: ?[]const u8, @"type": ?[]const u8, creator: ?[]const u8, mentioned: ?[]const u8, issue_field_values_param: ?[]const u8, labels_param: ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                return issuesListForRepoResult(client, owner, repo, milestone, state, assignee, @"type", creator, mentioned, issue_field_values_param, labels_param, sort, direction, since, per_page, page);
            }
            pub fn issuesupdate(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !Owned(std.json.Value) {
                return issuesUpdate(client, owner, repo, issue_number, requestBody);
            }
            pub fn issuesupdateResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !ApiResult(std.json.Value) {
                return issuesUpdateResult(client, owner, repo, issue_number, requestBody);
            }
            pub const assignees = struct {
                pub fn issuesaddassignees(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !Owned(models.issue) {
                    return issuesAddAssignees(client, owner, repo, issue_number, requestBody);
                }
                pub fn issuesaddassigneesResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !ApiResult(models.issue) {
                    return issuesAddAssigneesResult(client, owner, repo, issue_number, requestBody);
                }
                pub fn issuescheckusercanbeassignedtoissue(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, assignee: []const u8) !void {
                    return issuesCheckUserCanBeAssignedToIssue(client, owner, repo, issue_number, assignee);
                }
                pub fn issuesremoveassignees(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !Owned(models.issue) {
                    return issuesRemoveAssignees(client, owner, repo, issue_number, requestBody);
                }
                pub fn issuesremoveassigneesResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !ApiResult(models.issue) {
                    return issuesRemoveAssigneesResult(client, owner, repo, issue_number, requestBody);
                }
            };
            pub const comments = struct {
                pub fn issuescreatecomment(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !Owned(models.@"issue-comment") {
                    return issuesCreateComment(client, owner, repo, issue_number, requestBody);
                }
                pub fn issuescreatecommentResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !ApiResult(models.@"issue-comment") {
                    return issuesCreateCommentResult(client, owner, repo, issue_number, requestBody);
                }
                pub fn issuesdeletecomment(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !void {
                    return issuesDeleteComment(client, owner, repo, comment_id);
                }
                pub fn issuesgetcomment(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !Owned(models.@"issue-comment") {
                    return issuesGetComment(client, owner, repo, comment_id);
                }
                pub fn issuesgetcommentResult(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !ApiResult(models.@"issue-comment") {
                    return issuesGetCommentResult(client, owner, repo, comment_id);
                }
                pub fn issueslistcomments(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, since: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                    return issuesListComments(client, owner, repo, issue_number, since, per_page, page);
                }
                pub fn issueslistcommentsResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, since: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                    return issuesListCommentsResult(client, owner, repo, issue_number, since, per_page, page);
                }
                pub fn issueslistcommentsforrepo(client: *Client, owner: []const u8, repo: []const u8, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                    return issuesListCommentsForRepo(client, owner, repo, sort, direction, since, per_page, page);
                }
                pub fn issueslistcommentsforrepoResult(client: *Client, owner: []const u8, repo: []const u8, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                    return issuesListCommentsForRepoResult(client, owner, repo, sort, direction, since, per_page, page);
                }
                pub fn issuesupdatecomment(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64, requestBody: std.json.Value) !Owned(models.@"issue-comment") {
                    return issuesUpdateComment(client, owner, repo, comment_id, requestBody);
                }
                pub fn issuesupdatecommentResult(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64, requestBody: std.json.Value) !ApiResult(models.@"issue-comment") {
                    return issuesUpdateCommentResult(client, owner, repo, comment_id, requestBody);
                }
                pub const pin = struct {
                    pub fn issuespincomment(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !Owned(models.@"issue-comment") {
                        return issuesPinComment(client, owner, repo, comment_id);
                    }
                    pub fn issuespincommentResult(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !ApiResult(models.@"issue-comment") {
                        return issuesPinCommentResult(client, owner, repo, comment_id);
                    }
                    pub fn issuesunpincomment(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !void {
                        return issuesUnpinComment(client, owner, repo, comment_id);
                    }
                };
            };
            pub const dependencies = struct {
                pub const blocked_by = struct {
                    pub fn issuesaddblockedbydependency(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !Owned(models.issue) {
                        return issuesAddBlockedByDependency(client, owner, repo, issue_number, requestBody);
                    }
                    pub fn issuesaddblockedbydependencyResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !ApiResult(models.issue) {
                        return issuesAddBlockedByDependencyResult(client, owner, repo, issue_number, requestBody);
                    }
                    pub fn issueslistdependenciesblockedby(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                        return issuesListDependenciesBlockedBy(client, owner, repo, issue_number, per_page, page);
                    }
                    pub fn issueslistdependenciesblockedbyResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                        return issuesListDependenciesBlockedByResult(client, owner, repo, issue_number, per_page, page);
                    }
                    pub fn issuesremovedependencyblockedby(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, issue_id: i64) !Owned(models.issue) {
                        return issuesRemoveDependencyBlockedBy(client, owner, repo, issue_number, issue_id);
                    }
                    pub fn issuesremovedependencyblockedbyResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, issue_id: i64) !ApiResult(models.issue) {
                        return issuesRemoveDependencyBlockedByResult(client, owner, repo, issue_number, issue_id);
                    }
                };
                pub const blocking = struct {
                    pub fn issueslistdependenciesblocking(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                        return issuesListDependenciesBlocking(client, owner, repo, issue_number, per_page, page);
                    }
                    pub fn issueslistdependenciesblockingResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                        return issuesListDependenciesBlockingResult(client, owner, repo, issue_number, per_page, page);
                    }
                };
            };
            pub const events = struct {
                pub fn issuesgetevent(client: *Client, owner: []const u8, repo: []const u8, event_id: i64) !Owned(models.@"issue-event") {
                    return issuesGetEvent(client, owner, repo, event_id);
                }
                pub fn issuesgeteventResult(client: *Client, owner: []const u8, repo: []const u8, event_id: i64) !ApiResult(models.@"issue-event") {
                    return issuesGetEventResult(client, owner, repo, event_id);
                }
                pub fn issueslistevents(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                    return issuesListEvents(client, owner, repo, issue_number, per_page, page);
                }
                pub fn issueslisteventsResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                    return issuesListEventsResult(client, owner, repo, issue_number, per_page, page);
                }
                pub fn issueslisteventsforrepo(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                    return issuesListEventsForRepo(client, owner, repo, per_page, page);
                }
                pub fn issueslisteventsforrepoResult(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                    return issuesListEventsForRepoResult(client, owner, repo, per_page, page);
                }
            };
            pub const issue_field_values = struct {
                pub fn issuesaddissuefieldvalues(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !Owned([]const std.json.Value) {
                    return issuesAddIssueFieldValues(client, owner, repo, issue_number, requestBody);
                }
                pub fn issuesaddissuefieldvaluesResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
                    return issuesAddIssueFieldValuesResult(client, owner, repo, issue_number, requestBody);
                }
                pub fn issuesdeleteissuefieldvalue(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, issue_field_id: i64) !void {
                    return issuesDeleteIssueFieldValue(client, owner, repo, issue_number, issue_field_id);
                }
                pub fn issueslistissuefieldvaluesforissue(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                    return issuesListIssueFieldValuesForIssue(client, owner, repo, issue_number, per_page, page);
                }
                pub fn issueslistissuefieldvaluesforissueResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                    return issuesListIssueFieldValuesForIssueResult(client, owner, repo, issue_number, per_page, page);
                }
                pub fn issuessetissuefieldvalues(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !Owned([]const std.json.Value) {
                    return issuesSetIssueFieldValues(client, owner, repo, issue_number, requestBody);
                }
                pub fn issuessetissuefieldvaluesResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
                    return issuesSetIssueFieldValuesResult(client, owner, repo, issue_number, requestBody);
                }
            };
            pub const labels = struct {
                pub fn issuesaddlabels(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !Owned([]const std.json.Value) {
                    return issuesAddLabels(client, owner, repo, issue_number, requestBody);
                }
                pub fn issuesaddlabelsResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
                    return issuesAddLabelsResult(client, owner, repo, issue_number, requestBody);
                }
                pub fn issueslistlabelsonissue(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                    return issuesListLabelsOnIssue(client, owner, repo, issue_number, per_page, page);
                }
                pub fn issueslistlabelsonissueResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                    return issuesListLabelsOnIssueResult(client, owner, repo, issue_number, per_page, page);
                }
                pub fn issuesremovealllabels(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64) !void {
                    return issuesRemoveAllLabels(client, owner, repo, issue_number);
                }
                pub fn issuesremovelabel(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, name: []const u8) !Owned([]const std.json.Value) {
                    return issuesRemoveLabel(client, owner, repo, issue_number, name);
                }
                pub fn issuesremovelabelResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, name: []const u8) !ApiResult([]const std.json.Value) {
                    return issuesRemoveLabelResult(client, owner, repo, issue_number, name);
                }
                pub fn issuessetlabels(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !Owned([]const std.json.Value) {
                    return issuesSetLabels(client, owner, repo, issue_number, requestBody);
                }
                pub fn issuessetlabelsResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !ApiResult([]const std.json.Value) {
                    return issuesSetLabelsResult(client, owner, repo, issue_number, requestBody);
                }
            };
            pub const lock = struct {
                pub fn issueslock(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !void {
                    return issuesLock(client, owner, repo, issue_number, requestBody);
                }
                pub fn issuesunlock(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64) !void {
                    return issuesUnlock(client, owner, repo, issue_number);
                }
            };
            pub const parent = struct {
                pub fn issuesgetparent(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64) !Owned(models.issue) {
                    return issuesGetParent(client, owner, repo, issue_number);
                }
                pub fn issuesgetparentResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64) !ApiResult(models.issue) {
                    return issuesGetParentResult(client, owner, repo, issue_number);
                }
            };
            pub const sub_issue = struct {
                pub fn issuesremovesubissue(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !Owned(models.issue) {
                    return issuesRemoveSubIssue(client, owner, repo, issue_number, requestBody);
                }
                pub fn issuesremovesubissueResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !ApiResult(models.issue) {
                    return issuesRemoveSubIssueResult(client, owner, repo, issue_number, requestBody);
                }
            };
            pub const sub_issues = struct {
                pub fn issuesaddsubissue(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !Owned(models.issue) {
                    return issuesAddSubIssue(client, owner, repo, issue_number, requestBody);
                }
                pub fn issuesaddsubissueResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !ApiResult(models.issue) {
                    return issuesAddSubIssueResult(client, owner, repo, issue_number, requestBody);
                }
                pub fn issueslistsubissues(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                    return issuesListSubIssues(client, owner, repo, issue_number, per_page, page);
                }
                pub fn issueslistsubissuesResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                    return issuesListSubIssuesResult(client, owner, repo, issue_number, per_page, page);
                }
                pub const priority = struct {
                    pub fn issuesreprioritizesubissue(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !Owned(models.issue) {
                        return issuesReprioritizeSubIssue(client, owner, repo, issue_number, requestBody);
                    }
                    pub fn issuesreprioritizesubissueResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, requestBody: std.json.Value) !ApiResult(models.issue) {
                        return issuesReprioritizeSubIssueResult(client, owner, repo, issue_number, requestBody);
                    }
                };
            };
            pub const suggestions = struct {
                pub fn issueslistsuggestions(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, state: ?[]const u8, action: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                    return issuesListSuggestions(client, owner, repo, issue_number, state, action, per_page, page);
                }
                pub fn issueslistsuggestionsResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, state: ?[]const u8, action: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                    return issuesListSuggestionsResult(client, owner, repo, issue_number, state, action, per_page, page);
                }
                pub const approve = struct {
                    pub fn issuesapprovesuggestion(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, suggestion_id: i64) !Owned(models.@"issue-suggestion") {
                        return issuesApproveSuggestion(client, owner, repo, issue_number, suggestion_id);
                    }
                    pub fn issuesapprovesuggestionResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, suggestion_id: i64) !ApiResult(models.@"issue-suggestion") {
                        return issuesApproveSuggestionResult(client, owner, repo, issue_number, suggestion_id);
                    }
                };
                pub const dismiss = struct {
                    pub fn issuesdismisssuggestion(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, suggestion_id: i64) !Owned(models.@"issue-suggestion") {
                        return issuesDismissSuggestion(client, owner, repo, issue_number, suggestion_id);
                    }
                    pub fn issuesdismisssuggestionResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, suggestion_id: i64) !ApiResult(models.@"issue-suggestion") {
                        return issuesDismissSuggestionResult(client, owner, repo, issue_number, suggestion_id);
                    }
                };
            };
            pub const timeline = struct {
                pub fn issueslisteventsfortimeline(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64, exclude: ?[]const u8) !Owned([]const std.json.Value) {
                    return issuesListEventsForTimeline(client, owner, repo, issue_number, per_page, page, exclude);
                }
                pub fn issueslisteventsfortimelineResult(client: *Client, owner: []const u8, repo: []const u8, issue_number: i64, per_page: ?i64, page: ?i64, exclude: ?[]const u8) !ApiResult([]const std.json.Value) {
                    return issuesListEventsForTimelineResult(client, owner, repo, issue_number, per_page, page, exclude);
                }
            };
        };
        pub const keys = struct {
            pub fn reposcreatedeploykey(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.@"deploy-key") {
                return reposCreateDeployKey(client, owner, repo, requestBody);
            }
            pub fn reposcreatedeploykeyResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.@"deploy-key") {
                return reposCreateDeployKeyResult(client, owner, repo, requestBody);
            }
            pub fn reposdeletedeploykey(client: *Client, owner: []const u8, repo: []const u8, key_id: i64) !void {
                return reposDeleteDeployKey(client, owner, repo, key_id);
            }
            pub fn reposgetdeploykey(client: *Client, owner: []const u8, repo: []const u8, key_id: i64) !Owned(models.@"deploy-key") {
                return reposGetDeployKey(client, owner, repo, key_id);
            }
            pub fn reposgetdeploykeyResult(client: *Client, owner: []const u8, repo: []const u8, key_id: i64) !ApiResult(models.@"deploy-key") {
                return reposGetDeployKeyResult(client, owner, repo, key_id);
            }
            pub fn reposlistdeploykeys(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                return reposListDeployKeys(client, owner, repo, per_page, page);
            }
            pub fn reposlistdeploykeysResult(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                return reposListDeployKeysResult(client, owner, repo, per_page, page);
            }
        };
        pub const labels = struct {
            pub fn issuescreatelabel(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.label) {
                return issuesCreateLabel(client, owner, repo, requestBody);
            }
            pub fn issuescreatelabelResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.label) {
                return issuesCreateLabelResult(client, owner, repo, requestBody);
            }
            pub fn issuesdeletelabel(client: *Client, owner: []const u8, repo: []const u8, name: []const u8) !void {
                return issuesDeleteLabel(client, owner, repo, name);
            }
            pub fn issuesgetlabel(client: *Client, owner: []const u8, repo: []const u8, name: []const u8) !Owned(models.label) {
                return issuesGetLabel(client, owner, repo, name);
            }
            pub fn issuesgetlabelResult(client: *Client, owner: []const u8, repo: []const u8, name: []const u8) !ApiResult(models.label) {
                return issuesGetLabelResult(client, owner, repo, name);
            }
            pub fn issueslistlabelsforrepo(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                return issuesListLabelsForRepo(client, owner, repo, per_page, page);
            }
            pub fn issueslistlabelsforrepoResult(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                return issuesListLabelsForRepoResult(client, owner, repo, per_page, page);
            }
            pub fn issuesupdatelabel(client: *Client, owner: []const u8, repo: []const u8, name: []const u8, requestBody: std.json.Value) !Owned(models.label) {
                return issuesUpdateLabel(client, owner, repo, name, requestBody);
            }
            pub fn issuesupdatelabelResult(client: *Client, owner: []const u8, repo: []const u8, name: []const u8, requestBody: std.json.Value) !ApiResult(models.label) {
                return issuesUpdateLabelResult(client, owner, repo, name, requestBody);
            }
        };
        pub const languages = struct {
            pub fn reposlistlanguages(client: *Client, owner: []const u8, repo: []const u8) !Owned(models.language) {
                return reposListLanguages(client, owner, repo);
            }
            pub fn reposlistlanguagesResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult(models.language) {
                return reposListLanguagesResult(client, owner, repo);
            }
        };
        pub const merge_upstream = struct {
            pub fn reposmergeupstream(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.@"merged-upstream") {
                return reposMergeUpstream(client, owner, repo, requestBody);
            }
            pub fn reposmergeupstreamResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.@"merged-upstream") {
                return reposMergeUpstreamResult(client, owner, repo, requestBody);
            }
        };
        pub const merges = struct {
            pub fn reposmerge(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.commit) {
                return reposMerge(client, owner, repo, requestBody);
            }
            pub fn reposmergeResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.commit) {
                return reposMergeResult(client, owner, repo, requestBody);
            }
        };
        pub const milestones = struct {
            pub fn issuescreatemilestone(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.milestone) {
                return issuesCreateMilestone(client, owner, repo, requestBody);
            }
            pub fn issuescreatemilestoneResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.milestone) {
                return issuesCreateMilestoneResult(client, owner, repo, requestBody);
            }
            pub fn issuesdeletemilestone(client: *Client, owner: []const u8, repo: []const u8, milestone_number: i64) !void {
                return issuesDeleteMilestone(client, owner, repo, milestone_number);
            }
            pub fn issuesgetmilestone(client: *Client, owner: []const u8, repo: []const u8, milestone_number: i64) !Owned(models.milestone) {
                return issuesGetMilestone(client, owner, repo, milestone_number);
            }
            pub fn issuesgetmilestoneResult(client: *Client, owner: []const u8, repo: []const u8, milestone_number: i64) !ApiResult(models.milestone) {
                return issuesGetMilestoneResult(client, owner, repo, milestone_number);
            }
            pub fn issueslistmilestones(client: *Client, owner: []const u8, repo: []const u8, state: ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                return issuesListMilestones(client, owner, repo, state, sort, direction, per_page, page);
            }
            pub fn issueslistmilestonesResult(client: *Client, owner: []const u8, repo: []const u8, state: ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                return issuesListMilestonesResult(client, owner, repo, state, sort, direction, per_page, page);
            }
            pub fn issuesupdatemilestone(client: *Client, owner: []const u8, repo: []const u8, milestone_number: i64, requestBody: std.json.Value) !Owned(models.milestone) {
                return issuesUpdateMilestone(client, owner, repo, milestone_number, requestBody);
            }
            pub fn issuesupdatemilestoneResult(client: *Client, owner: []const u8, repo: []const u8, milestone_number: i64, requestBody: std.json.Value) !ApiResult(models.milestone) {
                return issuesUpdateMilestoneResult(client, owner, repo, milestone_number, requestBody);
            }
            pub const labels = struct {
                pub fn issueslistlabelsformilestone(client: *Client, owner: []const u8, repo: []const u8, milestone_number: i64, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                    return issuesListLabelsForMilestone(client, owner, repo, milestone_number, per_page, page);
                }
                pub fn issueslistlabelsformilestoneResult(client: *Client, owner: []const u8, repo: []const u8, milestone_number: i64, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                    return issuesListLabelsForMilestoneResult(client, owner, repo, milestone_number, per_page, page);
                }
            };
        };
        pub const pages = struct {
            pub fn reposcreatepagessite(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.page) {
                return reposCreatePagesSite(client, owner, repo, requestBody);
            }
            pub fn reposcreatepagessiteResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.page) {
                return reposCreatePagesSiteResult(client, owner, repo, requestBody);
            }
            pub fn reposdeletepagessite(client: *Client, owner: []const u8, repo: []const u8) !void {
                return reposDeletePagesSite(client, owner, repo);
            }
            pub fn reposgetpages(client: *Client, owner: []const u8, repo: []const u8) !Owned(models.page) {
                return reposGetPages(client, owner, repo);
            }
            pub fn reposgetpagesResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult(models.page) {
                return reposGetPagesResult(client, owner, repo);
            }
            pub fn reposupdateinformationaboutpagessite(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !void {
                return reposUpdateInformationAboutPagesSite(client, owner, repo, requestBody);
            }
            pub const builds = struct {
                pub fn reposgetpagesbuild(client: *Client, owner: []const u8, repo: []const u8, build_id: i64) !Owned(models.@"page-build") {
                    return reposGetPagesBuild(client, owner, repo, build_id);
                }
                pub fn reposgetpagesbuildResult(client: *Client, owner: []const u8, repo: []const u8, build_id: i64) !ApiResult(models.@"page-build") {
                    return reposGetPagesBuildResult(client, owner, repo, build_id);
                }
                pub fn reposlistpagesbuilds(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                    return reposListPagesBuilds(client, owner, repo, per_page, page);
                }
                pub fn reposlistpagesbuildsResult(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                    return reposListPagesBuildsResult(client, owner, repo, per_page, page);
                }
                pub fn reposrequestpagesbuild(client: *Client, owner: []const u8, repo: []const u8) !Owned(models.@"page-build-status") {
                    return reposRequestPagesBuild(client, owner, repo);
                }
                pub fn reposrequestpagesbuildResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult(models.@"page-build-status") {
                    return reposRequestPagesBuildResult(client, owner, repo);
                }
                pub const latest = struct {
                    pub fn reposgetlatestpagesbuild(client: *Client, owner: []const u8, repo: []const u8) !Owned(models.@"page-build") {
                        return reposGetLatestPagesBuild(client, owner, repo);
                    }
                    pub fn reposgetlatestpagesbuildResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult(models.@"page-build") {
                        return reposGetLatestPagesBuildResult(client, owner, repo);
                    }
                };
            };
            pub const deployments = struct {
                pub fn reposcreatepagesdeployment(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.@"page-deployment") {
                    return reposCreatePagesDeployment(client, owner, repo, requestBody);
                }
                pub fn reposcreatepagesdeploymentResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.@"page-deployment") {
                    return reposCreatePagesDeploymentResult(client, owner, repo, requestBody);
                }
                pub fn reposgetpagesdeployment(client: *Client, owner: []const u8, repo: []const u8, pages_deployment_id: []const u8) !Owned(models.@"pages-deployment-status") {
                    return reposGetPagesDeployment(client, owner, repo, pages_deployment_id);
                }
                pub fn reposgetpagesdeploymentResult(client: *Client, owner: []const u8, repo: []const u8, pages_deployment_id: []const u8) !ApiResult(models.@"pages-deployment-status") {
                    return reposGetPagesDeploymentResult(client, owner, repo, pages_deployment_id);
                }
                pub const cancel = struct {
                    pub fn reposcancelpagesdeployment(client: *Client, owner: []const u8, repo: []const u8, pages_deployment_id: []const u8) !void {
                        return reposCancelPagesDeployment(client, owner, repo, pages_deployment_id);
                    }
                };
            };
            pub const health = struct {
                pub fn reposgetpageshealthcheck(client: *Client, owner: []const u8, repo: []const u8) !Owned(models.@"pages-health-check") {
                    return reposGetPagesHealthCheck(client, owner, repo);
                }
                pub fn reposgetpageshealthcheckResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult(models.@"pages-health-check") {
                    return reposGetPagesHealthCheckResult(client, owner, repo);
                }
            };
        };
        pub const private_vulnerability_reporting = struct {
            pub fn reposcheckprivatevulnerabilityreporting(client: *Client, owner: []const u8, repo: []const u8) !Owned(std.json.Value) {
                return reposCheckPrivateVulnerabilityReporting(client, owner, repo);
            }
            pub fn reposcheckprivatevulnerabilityreportingResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult(std.json.Value) {
                return reposCheckPrivateVulnerabilityReportingResult(client, owner, repo);
            }
            pub fn reposdisableprivatevulnerabilityreporting(client: *Client, owner: []const u8, repo: []const u8) !void {
                return reposDisablePrivateVulnerabilityReporting(client, owner, repo);
            }
            pub fn reposenableprivatevulnerabilityreporting(client: *Client, owner: []const u8, repo: []const u8) !void {
                return reposEnablePrivateVulnerabilityReporting(client, owner, repo);
            }
        };
        pub const properties = struct {
            pub const values = struct {
                pub fn reposcustompropertiesforreposcreateorupdaterepositoryvalues(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !void {
                    return reposCustomPropertiesForReposCreateOrUpdateRepositoryValues(client, owner, repo, requestBody);
                }
                pub fn reposcustompropertiesforreposgetrepositoryvalues(client: *Client, owner: []const u8, repo: []const u8) !Owned([]const std.json.Value) {
                    return reposCustomPropertiesForReposGetRepositoryValues(client, owner, repo);
                }
                pub fn reposcustompropertiesforreposgetrepositoryvaluesResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult([]const std.json.Value) {
                    return reposCustomPropertiesForReposGetRepositoryValuesResult(client, owner, repo);
                }
            };
        };
        pub const pulls = struct {
            pub fn pullscreate(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.@"pull-request") {
                return pullsCreate(client, owner, repo, requestBody);
            }
            pub fn pullscreateResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.@"pull-request") {
                return pullsCreateResult(client, owner, repo, requestBody);
            }
            pub fn pullsget(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64) !Owned(models.@"pull-request") {
                return pullsGet(client, owner, repo, pull_number);
            }
            pub fn pullsgetResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64) !ApiResult(models.@"pull-request") {
                return pullsGetResult(client, owner, repo, pull_number);
            }
            pub fn pullslist(client: *Client, owner: []const u8, repo: []const u8, state: ?[]const u8, head: ?[]const u8, base: ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                return pullsList(client, owner, repo, state, head, base, sort, direction, per_page, page);
            }
            pub fn pullslistResult(client: *Client, owner: []const u8, repo: []const u8, state: ?[]const u8, head: ?[]const u8, base: ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                return pullsListResult(client, owner, repo, state, head, base, sort, direction, per_page, page);
            }
            pub fn pullsupdate(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !Owned(models.@"pull-request") {
                return pullsUpdate(client, owner, repo, pull_number, requestBody);
            }
            pub fn pullsupdateResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !ApiResult(models.@"pull-request") {
                return pullsUpdateResult(client, owner, repo, pull_number, requestBody);
            }
            pub const comments = struct {
                pub fn pullscreatereviewcomment(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !Owned(models.@"pull-request-review-comment") {
                    return pullsCreateReviewComment(client, owner, repo, pull_number, requestBody);
                }
                pub fn pullscreatereviewcommentResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !ApiResult(models.@"pull-request-review-comment") {
                    return pullsCreateReviewCommentResult(client, owner, repo, pull_number, requestBody);
                }
                pub fn pullsdeletereviewcomment(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !void {
                    return pullsDeleteReviewComment(client, owner, repo, comment_id);
                }
                pub fn pullsgetreviewcomment(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !Owned(models.@"pull-request-review-comment") {
                    return pullsGetReviewComment(client, owner, repo, comment_id);
                }
                pub fn pullsgetreviewcommentResult(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64) !ApiResult(models.@"pull-request-review-comment") {
                    return pullsGetReviewCommentResult(client, owner, repo, comment_id);
                }
                pub fn pullslistreviewcomments(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                    return pullsListReviewComments(client, owner, repo, pull_number, sort, direction, since, per_page, page);
                }
                pub fn pullslistreviewcommentsResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                    return pullsListReviewCommentsResult(client, owner, repo, pull_number, sort, direction, since, per_page, page);
                }
                pub fn pullslistreviewcommentsforrepo(client: *Client, owner: []const u8, repo: []const u8, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                    return pullsListReviewCommentsForRepo(client, owner, repo, sort, direction, since, per_page, page);
                }
                pub fn pullslistreviewcommentsforrepoResult(client: *Client, owner: []const u8, repo: []const u8, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                    return pullsListReviewCommentsForRepoResult(client, owner, repo, sort, direction, since, per_page, page);
                }
                pub fn pullsupdatereviewcomment(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64, requestBody: std.json.Value) !Owned(models.@"pull-request-review-comment") {
                    return pullsUpdateReviewComment(client, owner, repo, comment_id, requestBody);
                }
                pub fn pullsupdatereviewcommentResult(client: *Client, owner: []const u8, repo: []const u8, comment_id: i64, requestBody: std.json.Value) !ApiResult(models.@"pull-request-review-comment") {
                    return pullsUpdateReviewCommentResult(client, owner, repo, comment_id, requestBody);
                }
                pub const replies = struct {
                    pub fn pullscreatereplyforreviewcomment(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, comment_id: i64, requestBody: std.json.Value) !Owned(models.@"pull-request-review-comment") {
                        return pullsCreateReplyForReviewComment(client, owner, repo, pull_number, comment_id, requestBody);
                    }
                    pub fn pullscreatereplyforreviewcommentResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, comment_id: i64, requestBody: std.json.Value) !ApiResult(models.@"pull-request-review-comment") {
                        return pullsCreateReplyForReviewCommentResult(client, owner, repo, pull_number, comment_id, requestBody);
                    }
                };
            };
            pub const commits = struct {
                pub fn pullslistcommits(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                    return pullsListCommits(client, owner, repo, pull_number, per_page, page);
                }
                pub fn pullslistcommitsResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                    return pullsListCommitsResult(client, owner, repo, pull_number, per_page, page);
                }
            };
            pub const files = struct {
                pub fn pullslistfiles(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                    return pullsListFiles(client, owner, repo, pull_number, per_page, page);
                }
                pub fn pullslistfilesResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                    return pullsListFilesResult(client, owner, repo, pull_number, per_page, page);
                }
            };
            pub const merge = struct {
                pub fn pullscheckifmerged(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64) !void {
                    return pullsCheckIfMerged(client, owner, repo, pull_number);
                }
                pub fn pullsmerge(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !Owned(models.@"pull-request-merge-result") {
                    return pullsMerge(client, owner, repo, pull_number, requestBody);
                }
                pub fn pullsmergeResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !ApiResult(models.@"pull-request-merge-result") {
                    return pullsMergeResult(client, owner, repo, pull_number, requestBody);
                }
            };
            pub const merge_async = struct {
                pub fn pullsgetmergeasyncresult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, uuid: []const u8) !Owned(models.@"pull-request-merge-async-result") {
                    return pullsGetMergeAsyncResult(client, owner, repo, pull_number, uuid);
                }
                pub fn pullsgetmergeasyncresultResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, uuid: []const u8) !ApiResult(models.@"pull-request-merge-async-result") {
                    return pullsGetMergeAsyncResultResult(client, owner, repo, pull_number, uuid);
                }
                pub fn pullsmergeasync(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !Owned(models.@"pull-request-merge-async-result") {
                    return pullsMergeAsync(client, owner, repo, pull_number, requestBody);
                }
                pub fn pullsmergeasyncResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !ApiResult(models.@"pull-request-merge-async-result") {
                    return pullsMergeAsyncResult(client, owner, repo, pull_number, requestBody);
                }
            };
            pub const requested_reviewers = struct {
                pub fn pullslistrequestedreviewers(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64) !Owned(models.@"pull-request-review-request") {
                    return pullsListRequestedReviewers(client, owner, repo, pull_number);
                }
                pub fn pullslistrequestedreviewersResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64) !ApiResult(models.@"pull-request-review-request") {
                    return pullsListRequestedReviewersResult(client, owner, repo, pull_number);
                }
                pub fn pullsremoverequestedreviewers(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !Owned(models.@"pull-request-simple") {
                    return pullsRemoveRequestedReviewers(client, owner, repo, pull_number, requestBody);
                }
                pub fn pullsremoverequestedreviewersResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !ApiResult(models.@"pull-request-simple") {
                    return pullsRemoveRequestedReviewersResult(client, owner, repo, pull_number, requestBody);
                }
                pub fn pullsrequestreviewers(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !Owned(models.@"pull-request-simple") {
                    return pullsRequestReviewers(client, owner, repo, pull_number, requestBody);
                }
                pub fn pullsrequestreviewersResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !ApiResult(models.@"pull-request-simple") {
                    return pullsRequestReviewersResult(client, owner, repo, pull_number, requestBody);
                }
            };
            pub const reviews = struct {
                pub fn pullscreatereview(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !Owned(models.@"pull-request-review") {
                    return pullsCreateReview(client, owner, repo, pull_number, requestBody);
                }
                pub fn pullscreatereviewResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !ApiResult(models.@"pull-request-review") {
                    return pullsCreateReviewResult(client, owner, repo, pull_number, requestBody);
                }
                pub fn pullsdeletependingreview(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, review_id: i64) !Owned(models.@"pull-request-review") {
                    return pullsDeletePendingReview(client, owner, repo, pull_number, review_id);
                }
                pub fn pullsdeletependingreviewResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, review_id: i64) !ApiResult(models.@"pull-request-review") {
                    return pullsDeletePendingReviewResult(client, owner, repo, pull_number, review_id);
                }
                pub fn pullsgetreview(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, review_id: i64) !Owned(models.@"pull-request-review") {
                    return pullsGetReview(client, owner, repo, pull_number, review_id);
                }
                pub fn pullsgetreviewResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, review_id: i64) !ApiResult(models.@"pull-request-review") {
                    return pullsGetReviewResult(client, owner, repo, pull_number, review_id);
                }
                pub fn pullslistreviews(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                    return pullsListReviews(client, owner, repo, pull_number, per_page, page);
                }
                pub fn pullslistreviewsResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                    return pullsListReviewsResult(client, owner, repo, pull_number, per_page, page);
                }
                pub fn pullsupdatereview(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, review_id: i64, requestBody: std.json.Value) !Owned(models.@"pull-request-review") {
                    return pullsUpdateReview(client, owner, repo, pull_number, review_id, requestBody);
                }
                pub fn pullsupdatereviewResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, review_id: i64, requestBody: std.json.Value) !ApiResult(models.@"pull-request-review") {
                    return pullsUpdateReviewResult(client, owner, repo, pull_number, review_id, requestBody);
                }
                pub const comments = struct {
                    pub fn pullslistcommentsforreview(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, review_id: i64, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                        return pullsListCommentsForReview(client, owner, repo, pull_number, review_id, per_page, page);
                    }
                    pub fn pullslistcommentsforreviewResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, review_id: i64, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                        return pullsListCommentsForReviewResult(client, owner, repo, pull_number, review_id, per_page, page);
                    }
                };
                pub const dismissals = struct {
                    pub fn pullsdismissreview(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, review_id: i64, requestBody: std.json.Value) !Owned(models.@"pull-request-review") {
                        return pullsDismissReview(client, owner, repo, pull_number, review_id, requestBody);
                    }
                    pub fn pullsdismissreviewResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, review_id: i64, requestBody: std.json.Value) !ApiResult(models.@"pull-request-review") {
                        return pullsDismissReviewResult(client, owner, repo, pull_number, review_id, requestBody);
                    }
                };
                pub const events = struct {
                    pub fn pullssubmitreview(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, review_id: i64, requestBody: std.json.Value) !Owned(models.@"pull-request-review") {
                        return pullsSubmitReview(client, owner, repo, pull_number, review_id, requestBody);
                    }
                    pub fn pullssubmitreviewResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, review_id: i64, requestBody: std.json.Value) !ApiResult(models.@"pull-request-review") {
                        return pullsSubmitReviewResult(client, owner, repo, pull_number, review_id, requestBody);
                    }
                };
            };
            pub const update_branch = struct {
                pub fn pullsupdatebranch(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !Owned(std.json.Value) {
                    return pullsUpdateBranch(client, owner, repo, pull_number, requestBody);
                }
                pub fn pullsupdatebranchResult(client: *Client, owner: []const u8, repo: []const u8, pull_number: i64, requestBody: std.json.Value) !ApiResult(std.json.Value) {
                    return pullsUpdateBranchResult(client, owner, repo, pull_number, requestBody);
                }
            };
        };
        pub const readme = struct {
            pub fn reposgetreadme(client: *Client, owner: []const u8, repo: []const u8, ref: ?[]const u8) !Owned(models.@"content-file") {
                return reposGetReadme(client, owner, repo, ref);
            }
            pub fn reposgetreadmeResult(client: *Client, owner: []const u8, repo: []const u8, ref: ?[]const u8) !ApiResult(models.@"content-file") {
                return reposGetReadmeResult(client, owner, repo, ref);
            }
            pub fn reposgetreadmeindirectory(client: *Client, owner: []const u8, repo: []const u8, dir: []const u8, ref: ?[]const u8) !Owned(models.@"content-file") {
                return reposGetReadmeInDirectory(client, owner, repo, dir, ref);
            }
            pub fn reposgetreadmeindirectoryResult(client: *Client, owner: []const u8, repo: []const u8, dir: []const u8, ref: ?[]const u8) !ApiResult(models.@"content-file") {
                return reposGetReadmeInDirectoryResult(client, owner, repo, dir, ref);
            }
        };
        pub const releases = struct {
            pub fn reposcreaterelease(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.release) {
                return reposCreateRelease(client, owner, repo, requestBody);
            }
            pub fn reposcreatereleaseResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.release) {
                return reposCreateReleaseResult(client, owner, repo, requestBody);
            }
            pub fn reposdeleterelease(client: *Client, owner: []const u8, repo: []const u8, release_id: i64) !void {
                return reposDeleteRelease(client, owner, repo, release_id);
            }
            pub fn reposgetrelease(client: *Client, owner: []const u8, repo: []const u8, release_id: i64) !Owned(models.release) {
                return reposGetRelease(client, owner, repo, release_id);
            }
            pub fn reposgetreleaseResult(client: *Client, owner: []const u8, repo: []const u8, release_id: i64) !ApiResult(models.release) {
                return reposGetReleaseResult(client, owner, repo, release_id);
            }
            pub fn reposlistreleases(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                return reposListReleases(client, owner, repo, per_page, page);
            }
            pub fn reposlistreleasesResult(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                return reposListReleasesResult(client, owner, repo, per_page, page);
            }
            pub fn reposupdaterelease(client: *Client, owner: []const u8, repo: []const u8, release_id: i64, requestBody: std.json.Value) !Owned(models.release) {
                return reposUpdateRelease(client, owner, repo, release_id, requestBody);
            }
            pub fn reposupdatereleaseResult(client: *Client, owner: []const u8, repo: []const u8, release_id: i64, requestBody: std.json.Value) !ApiResult(models.release) {
                return reposUpdateReleaseResult(client, owner, repo, release_id, requestBody);
            }
            pub const assets = struct {
                pub fn reposdeletereleaseasset(client: *Client, owner: []const u8, repo: []const u8, asset_id: i64) !void {
                    return reposDeleteReleaseAsset(client, owner, repo, asset_id);
                }
                pub fn reposgetreleaseasset(client: *Client, owner: []const u8, repo: []const u8, asset_id: i64) !Owned(models.@"release-asset") {
                    return reposGetReleaseAsset(client, owner, repo, asset_id);
                }
                pub fn reposgetreleaseassetResult(client: *Client, owner: []const u8, repo: []const u8, asset_id: i64) !ApiResult(models.@"release-asset") {
                    return reposGetReleaseAssetResult(client, owner, repo, asset_id);
                }
                pub fn reposlistreleaseassets(client: *Client, owner: []const u8, repo: []const u8, release_id: i64, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                    return reposListReleaseAssets(client, owner, repo, release_id, per_page, page);
                }
                pub fn reposlistreleaseassetsResult(client: *Client, owner: []const u8, repo: []const u8, release_id: i64, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                    return reposListReleaseAssetsResult(client, owner, repo, release_id, per_page, page);
                }
                pub fn reposupdatereleaseasset(client: *Client, owner: []const u8, repo: []const u8, asset_id: i64, requestBody: std.json.Value) !Owned(models.@"release-asset") {
                    return reposUpdateReleaseAsset(client, owner, repo, asset_id, requestBody);
                }
                pub fn reposupdatereleaseassetResult(client: *Client, owner: []const u8, repo: []const u8, asset_id: i64, requestBody: std.json.Value) !ApiResult(models.@"release-asset") {
                    return reposUpdateReleaseAssetResult(client, owner, repo, asset_id, requestBody);
                }
                pub fn reposuploadreleaseasset(client: *Client, owner: []const u8, repo: []const u8, release_id: i64, name: []const u8, label: ?[]const u8, requestBody: []const u8) !Owned(models.@"release-asset") {
                    return reposUploadReleaseAsset(client, owner, repo, release_id, name, label, requestBody);
                }
                pub fn reposuploadreleaseassetResult(client: *Client, owner: []const u8, repo: []const u8, release_id: i64, name: []const u8, label: ?[]const u8, requestBody: []const u8) !ApiResult(models.@"release-asset") {
                    return reposUploadReleaseAssetResult(client, owner, repo, release_id, name, label, requestBody);
                }
            };
            pub const generate_notes = struct {
                pub fn reposgeneratereleasenotes(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.@"release-notes-content") {
                    return reposGenerateReleaseNotes(client, owner, repo, requestBody);
                }
                pub fn reposgeneratereleasenotesResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.@"release-notes-content") {
                    return reposGenerateReleaseNotesResult(client, owner, repo, requestBody);
                }
            };
            pub const latest = struct {
                pub fn reposgetlatestrelease(client: *Client, owner: []const u8, repo: []const u8) !Owned(models.release) {
                    return reposGetLatestRelease(client, owner, repo);
                }
                pub fn reposgetlatestreleaseResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult(models.release) {
                    return reposGetLatestReleaseResult(client, owner, repo);
                }
            };
            pub const tags = struct {
                pub fn reposgetreleasebytag(client: *Client, owner: []const u8, repo: []const u8, tag: []const u8) !Owned(models.release) {
                    return reposGetReleaseByTag(client, owner, repo, tag);
                }
                pub fn reposgetreleasebytagResult(client: *Client, owner: []const u8, repo: []const u8, tag: []const u8) !ApiResult(models.release) {
                    return reposGetReleaseByTagResult(client, owner, repo, tag);
                }
            };
        };
        pub const rules = struct {
            pub const branches = struct {
                pub fn reposgetbranchrules(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                    return reposGetBranchRules(client, owner, repo, branch, per_page, page);
                }
                pub fn reposgetbranchrulesResult(client: *Client, owner: []const u8, repo: []const u8, branch: []const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                    return reposGetBranchRulesResult(client, owner, repo, branch, per_page, page);
                }
            };
        };
        pub const rulesets = struct {
            pub fn reposcreatereporuleset(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.@"repository-ruleset") {
                return reposCreateRepoRuleset(client, owner, repo, requestBody);
            }
            pub fn reposcreatereporulesetResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.@"repository-ruleset") {
                return reposCreateRepoRulesetResult(client, owner, repo, requestBody);
            }
            pub fn reposdeletereporuleset(client: *Client, owner: []const u8, repo: []const u8, ruleset_id: i64) !void {
                return reposDeleteRepoRuleset(client, owner, repo, ruleset_id);
            }
            pub fn reposgetreporuleset(client: *Client, owner: []const u8, repo: []const u8, ruleset_id: i64, includes_parents: ?bool) !Owned(models.@"repository-ruleset") {
                return reposGetRepoRuleset(client, owner, repo, ruleset_id, includes_parents);
            }
            pub fn reposgetreporulesetResult(client: *Client, owner: []const u8, repo: []const u8, ruleset_id: i64, includes_parents: ?bool) !ApiResult(models.@"repository-ruleset") {
                return reposGetRepoRulesetResult(client, owner, repo, ruleset_id, includes_parents);
            }
            pub fn reposgetreporulesets(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64, includes_parents: ?bool, targets: ?[]const u8) !Owned([]const std.json.Value) {
                return reposGetRepoRulesets(client, owner, repo, per_page, page, includes_parents, targets);
            }
            pub fn reposgetreporulesetsResult(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64, includes_parents: ?bool, targets: ?[]const u8) !ApiResult([]const std.json.Value) {
                return reposGetRepoRulesetsResult(client, owner, repo, per_page, page, includes_parents, targets);
            }
            pub fn reposupdatereporuleset(client: *Client, owner: []const u8, repo: []const u8, ruleset_id: i64, requestBody: std.json.Value) !Owned(models.@"repository-ruleset") {
                return reposUpdateRepoRuleset(client, owner, repo, ruleset_id, requestBody);
            }
            pub fn reposupdatereporulesetResult(client: *Client, owner: []const u8, repo: []const u8, ruleset_id: i64, requestBody: std.json.Value) !ApiResult(models.@"repository-ruleset") {
                return reposUpdateRepoRulesetResult(client, owner, repo, ruleset_id, requestBody);
            }
            pub const history = struct {
                pub fn reposgetreporulesethistory(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64, ruleset_id: i64) !Owned([]const std.json.Value) {
                    return reposGetRepoRulesetHistory(client, owner, repo, per_page, page, ruleset_id);
                }
                pub fn reposgetreporulesethistoryResult(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64, ruleset_id: i64) !ApiResult([]const std.json.Value) {
                    return reposGetRepoRulesetHistoryResult(client, owner, repo, per_page, page, ruleset_id);
                }
                pub fn reposgetreporulesetversion(client: *Client, owner: []const u8, repo: []const u8, ruleset_id: i64, version_id: i64) !Owned(models.@"ruleset-version-with-state") {
                    return reposGetRepoRulesetVersion(client, owner, repo, ruleset_id, version_id);
                }
                pub fn reposgetreporulesetversionResult(client: *Client, owner: []const u8, repo: []const u8, ruleset_id: i64, version_id: i64) !ApiResult(models.@"ruleset-version-with-state") {
                    return reposGetRepoRulesetVersionResult(client, owner, repo, ruleset_id, version_id);
                }
            };
            pub const rule_suites = struct {
                pub fn reposgetreporulesuite(client: *Client, owner: []const u8, repo: []const u8, rule_suite_id: i64) !Owned(models.@"rule-suite") {
                    return reposGetRepoRuleSuite(client, owner, repo, rule_suite_id);
                }
                pub fn reposgetreporulesuiteResult(client: *Client, owner: []const u8, repo: []const u8, rule_suite_id: i64) !ApiResult(models.@"rule-suite") {
                    return reposGetRepoRuleSuiteResult(client, owner, repo, rule_suite_id);
                }
                pub fn reposgetreporulesuites(client: *Client, owner: []const u8, repo: []const u8, ref: ?[]const u8, time_period: ?[]const u8, actor_name: ?[]const u8, rule_suite_result: ?[]const u8, evaluate_status: ?[]const u8, per_page: ?i64, page: ?i64) !Owned(models.@"rule-suites") {
                    return reposGetRepoRuleSuites(client, owner, repo, ref, time_period, actor_name, rule_suite_result, evaluate_status, per_page, page);
                }
                pub fn reposgetreporulesuitesResult(client: *Client, owner: []const u8, repo: []const u8, ref: ?[]const u8, time_period: ?[]const u8, actor_name: ?[]const u8, rule_suite_result: ?[]const u8, evaluate_status: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult(models.@"rule-suites") {
                    return reposGetRepoRuleSuitesResult(client, owner, repo, ref, time_period, actor_name, rule_suite_result, evaluate_status, per_page, page);
                }
            };
        };
        pub const stacks = struct {
            pub fn pullrequeststackscreate(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(std.json.Value) {
                return pullRequestStacksCreate(client, owner, repo, requestBody);
            }
            pub fn pullrequeststackscreateResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(std.json.Value) {
                return pullRequestStacksCreateResult(client, owner, repo, requestBody);
            }
            pub fn pullrequeststacksget(client: *Client, owner: []const u8, repo: []const u8, stack_number: i64) !Owned(std.json.Value) {
                return pullRequestStacksGet(client, owner, repo, stack_number);
            }
            pub fn pullrequeststacksgetResult(client: *Client, owner: []const u8, repo: []const u8, stack_number: i64) !ApiResult(std.json.Value) {
                return pullRequestStacksGetResult(client, owner, repo, stack_number);
            }
            pub fn pullrequeststackslist(client: *Client, owner: []const u8, repo: []const u8, pull_request: ?i64, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                return pullRequestStacksList(client, owner, repo, pull_request, per_page, page);
            }
            pub fn pullrequeststackslistResult(client: *Client, owner: []const u8, repo: []const u8, pull_request: ?i64, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                return pullRequestStacksListResult(client, owner, repo, pull_request, per_page, page);
            }
            pub const add = struct {
                pub fn pullrequeststacksadd(client: *Client, owner: []const u8, repo: []const u8, stack_number: i64, requestBody: std.json.Value) !Owned(std.json.Value) {
                    return pullRequestStacksAdd(client, owner, repo, stack_number, requestBody);
                }
                pub fn pullrequeststacksaddResult(client: *Client, owner: []const u8, repo: []const u8, stack_number: i64, requestBody: std.json.Value) !ApiResult(std.json.Value) {
                    return pullRequestStacksAddResult(client, owner, repo, stack_number, requestBody);
                }
            };
            pub const unstack = struct {
                pub fn pullrequeststacksunstack(client: *Client, owner: []const u8, repo: []const u8, stack_number: i64) !Owned(std.json.Value) {
                    return pullRequestStacksUnstack(client, owner, repo, stack_number);
                }
                pub fn pullrequeststacksunstackResult(client: *Client, owner: []const u8, repo: []const u8, stack_number: i64) !ApiResult(std.json.Value) {
                    return pullRequestStacksUnstackResult(client, owner, repo, stack_number);
                }
            };
        };
        pub const stats = struct {
            pub const code_frequency = struct {
                pub fn reposgetcodefrequencystats(client: *Client, owner: []const u8, repo: []const u8) !Owned([]const std.json.Value) {
                    return reposGetCodeFrequencyStats(client, owner, repo);
                }
                pub fn reposgetcodefrequencystatsResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult([]const std.json.Value) {
                    return reposGetCodeFrequencyStatsResult(client, owner, repo);
                }
            };
            pub const commit_activity = struct {
                pub fn reposgetcommitactivitystats(client: *Client, owner: []const u8, repo: []const u8) !Owned([]const std.json.Value) {
                    return reposGetCommitActivityStats(client, owner, repo);
                }
                pub fn reposgetcommitactivitystatsResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult([]const std.json.Value) {
                    return reposGetCommitActivityStatsResult(client, owner, repo);
                }
            };
            pub const contributors = struct {
                pub fn reposgetcontributorsstats(client: *Client, owner: []const u8, repo: []const u8) !Owned([]const std.json.Value) {
                    return reposGetContributorsStats(client, owner, repo);
                }
                pub fn reposgetcontributorsstatsResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult([]const std.json.Value) {
                    return reposGetContributorsStatsResult(client, owner, repo);
                }
            };
            pub const participation = struct {
                pub fn reposgetparticipationstats(client: *Client, owner: []const u8, repo: []const u8) !Owned(models.@"participation-stats") {
                    return reposGetParticipationStats(client, owner, repo);
                }
                pub fn reposgetparticipationstatsResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult(models.@"participation-stats") {
                    return reposGetParticipationStatsResult(client, owner, repo);
                }
            };
            pub const punch_card = struct {
                pub fn reposgetpunchcardstats(client: *Client, owner: []const u8, repo: []const u8) !Owned([]const std.json.Value) {
                    return reposGetPunchCardStats(client, owner, repo);
                }
                pub fn reposgetpunchcardstatsResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult([]const std.json.Value) {
                    return reposGetPunchCardStatsResult(client, owner, repo);
                }
            };
        };
        pub const statuses = struct {
            pub fn reposcreatecommitstatus(client: *Client, owner: []const u8, repo: []const u8, sha: []const u8, requestBody: std.json.Value) !Owned(models.status) {
                return reposCreateCommitStatus(client, owner, repo, sha, requestBody);
            }
            pub fn reposcreatecommitstatusResult(client: *Client, owner: []const u8, repo: []const u8, sha: []const u8, requestBody: std.json.Value) !ApiResult(models.status) {
                return reposCreateCommitStatusResult(client, owner, repo, sha, requestBody);
            }
        };
        pub const tags = struct {
            pub fn reposlisttags(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                return reposListTags(client, owner, repo, per_page, page);
            }
            pub fn reposlisttagsResult(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                return reposListTagsResult(client, owner, repo, per_page, page);
            }
        };
        pub const tarball = struct {
            pub fn reposdownloadtarballarchive(client: *Client, owner: []const u8, repo: []const u8, ref: []const u8) !void {
                return reposDownloadTarballArchive(client, owner, repo, ref);
            }
        };
        pub const teams = struct {
            pub fn reposlistteams(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                return reposListTeams(client, owner, repo, per_page, page);
            }
            pub fn reposlistteamsResult(client: *Client, owner: []const u8, repo: []const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                return reposListTeamsResult(client, owner, repo, per_page, page);
            }
        };
        pub const topics = struct {
            pub fn reposgetalltopics(client: *Client, owner: []const u8, repo: []const u8, page: ?i64, per_page: ?i64) !Owned(models.topic) {
                return reposGetAllTopics(client, owner, repo, page, per_page);
            }
            pub fn reposgetalltopicsResult(client: *Client, owner: []const u8, repo: []const u8, page: ?i64, per_page: ?i64) !ApiResult(models.topic) {
                return reposGetAllTopicsResult(client, owner, repo, page, per_page);
            }
            pub fn reposreplacealltopics(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.topic) {
                return reposReplaceAllTopics(client, owner, repo, requestBody);
            }
            pub fn reposreplacealltopicsResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.topic) {
                return reposReplaceAllTopicsResult(client, owner, repo, requestBody);
            }
        };
        pub const traffic = struct {
            pub const clones = struct {
                pub fn reposgetclones(client: *Client, owner: []const u8, repo: []const u8, per: ?[]const u8) !Owned(models.@"clone-traffic") {
                    return reposGetClones(client, owner, repo, per);
                }
                pub fn reposgetclonesResult(client: *Client, owner: []const u8, repo: []const u8, per: ?[]const u8) !ApiResult(models.@"clone-traffic") {
                    return reposGetClonesResult(client, owner, repo, per);
                }
            };
            pub const popular = struct {
                pub const paths = struct {
                    pub fn reposgettoppaths(client: *Client, owner: []const u8, repo: []const u8) !Owned([]const std.json.Value) {
                        return reposGetTopPaths(client, owner, repo);
                    }
                    pub fn reposgettoppathsResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult([]const std.json.Value) {
                        return reposGetTopPathsResult(client, owner, repo);
                    }
                };
                pub const referrers = struct {
                    pub fn reposgettopreferrers(client: *Client, owner: []const u8, repo: []const u8) !Owned([]const std.json.Value) {
                        return reposGetTopReferrers(client, owner, repo);
                    }
                    pub fn reposgettopreferrersResult(client: *Client, owner: []const u8, repo: []const u8) !ApiResult([]const std.json.Value) {
                        return reposGetTopReferrersResult(client, owner, repo);
                    }
                };
            };
            pub const views = struct {
                pub fn reposgetviews(client: *Client, owner: []const u8, repo: []const u8, per: ?[]const u8) !Owned(models.@"view-traffic") {
                    return reposGetViews(client, owner, repo, per);
                }
                pub fn reposgetviewsResult(client: *Client, owner: []const u8, repo: []const u8, per: ?[]const u8) !ApiResult(models.@"view-traffic") {
                    return reposGetViewsResult(client, owner, repo, per);
                }
            };
        };
        pub const transfer = struct {
            pub fn repostransfer(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !Owned(models.@"minimal-repository") {
                return reposTransfer(client, owner, repo, requestBody);
            }
            pub fn repostransferResult(client: *Client, owner: []const u8, repo: []const u8, requestBody: std.json.Value) !ApiResult(models.@"minimal-repository") {
                return reposTransferResult(client, owner, repo, requestBody);
            }
        };
        pub const vulnerability_alerts = struct {
            pub fn reposcheckvulnerabilityalerts(client: *Client, owner: []const u8, repo: []const u8) !void {
                return reposCheckVulnerabilityAlerts(client, owner, repo);
            }
            pub fn reposdisablevulnerabilityalerts(client: *Client, owner: []const u8, repo: []const u8) !void {
                return reposDisableVulnerabilityAlerts(client, owner, repo);
            }
            pub fn reposenablevulnerabilityalerts(client: *Client, owner: []const u8, repo: []const u8) !void {
                return reposEnableVulnerabilityAlerts(client, owner, repo);
            }
        };
        pub const zipball = struct {
            pub fn reposdownloadzipballarchive(client: *Client, owner: []const u8, repo: []const u8, ref: []const u8) !void {
                return reposDownloadZipballArchive(client, owner, repo, ref);
            }
        };
    };
    pub const repositories = struct {
        pub fn reposlistpublic(client: *Client, since: ?i64) !Owned([]const std.json.Value) {
            return reposListPublic(client, since);
        }
        pub fn reposlistpublicResult(client: *Client, since: ?i64) !ApiResult([]const std.json.Value) {
            return reposListPublicResult(client, since);
        }
    };
    pub const user = struct {
        pub const issues = struct {
            pub fn issueslistforauthenticateduser(client: *Client, filter: ?[]const u8, state: ?[]const u8, labels: ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                return issuesListForAuthenticatedUser(client, filter, state, labels, sort, direction, since, per_page, page);
            }
            pub fn issueslistforauthenticateduserResult(client: *Client, filter: ?[]const u8, state: ?[]const u8, labels: ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, since: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                return issuesListForAuthenticatedUserResult(client, filter, state, labels, sort, direction, since, per_page, page);
            }
        };
        pub const repos = struct {
            pub fn reposcreateforauthenticateduser(client: *Client, requestBody: std.json.Value) !Owned(models.@"full-repository") {
                return reposCreateForAuthenticatedUser(client, requestBody);
            }
            pub fn reposcreateforauthenticateduserResult(client: *Client, requestBody: std.json.Value) !ApiResult(models.@"full-repository") {
                return reposCreateForAuthenticatedUserResult(client, requestBody);
            }
            pub fn reposlistforauthenticateduser(client: *Client, visibility: ?[]const u8, affiliation: ?[]const u8, @"type": ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, per_page: ?i64, page: ?i64, since: ?[]const u8, before: ?[]const u8) !Owned([]const std.json.Value) {
                return reposListForAuthenticatedUser(client, visibility, affiliation, @"type", sort, direction, per_page, page, since, before);
            }
            pub fn reposlistforauthenticateduserResult(client: *Client, visibility: ?[]const u8, affiliation: ?[]const u8, @"type": ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, per_page: ?i64, page: ?i64, since: ?[]const u8, before: ?[]const u8) !ApiResult([]const std.json.Value) {
                return reposListForAuthenticatedUserResult(client, visibility, affiliation, @"type", sort, direction, per_page, page, since, before);
            }
        };
        pub const repository_invitations = struct {
            pub fn reposacceptinvitationforauthenticateduser(client: *Client, invitation_id: i64) !void {
                return reposAcceptInvitationForAuthenticatedUser(client, invitation_id);
            }
            pub fn reposdeclineinvitationforauthenticateduser(client: *Client, invitation_id: i64) !void {
                return reposDeclineInvitationForAuthenticatedUser(client, invitation_id);
            }
            pub fn reposlistinvitationsforauthenticateduser(client: *Client, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                return reposListInvitationsForAuthenticatedUser(client, per_page, page);
            }
            pub fn reposlistinvitationsforauthenticateduserResult(client: *Client, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                return reposListInvitationsForAuthenticatedUserResult(client, per_page, page);
            }
        };
    };
    pub const users = struct {
        pub const repos = struct {
            pub fn reposlistforuser(client: *Client, username: []const u8, @"type": ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, per_page: ?i64, page: ?i64) !Owned([]const std.json.Value) {
                return reposListForUser(client, username, @"type", sort, direction, per_page, page);
            }
            pub fn reposlistforuserResult(client: *Client, username: []const u8, @"type": ?[]const u8, sort: ?[]const u8, direction: ?[]const u8, per_page: ?i64, page: ?i64) !ApiResult([]const std.json.Value) {
                return reposListForUserResult(client, username, @"type", sort, direction, per_page, page);
            }
        };
    };
};

pub const issues = resources.issues;
pub const repos = resources.repos;
pub const repositories = resources.repositories;
pub const user = resources.user;
pub const users = resources.users;
