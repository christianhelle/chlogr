# Orchestration Log — McManus (2026-03-18T07:22:56Z)

**Agent:** McManus — DevRel / Product  
**Role:** Scope decisions for issues #5, #8, #10  
**Status:** Background (in progress)

## Assigned Issues

- **#5:** Assign each PR to exactly one release; use full timestamps (6h est.)
- **#8:** Implement or reject `--since-tag` / `--until-tag` (decision + implementation)
- **#10:** Use actual repo slug in release links (P2, deferred)

## Work Scope

**Immediate (this cycle):**
1. **#8 Scope Decision (by EOD):**
   - Decide: implement end-to-end or remove/reject?
   - If implement: add to #5 sequence, refactor release filtering
   - If reject: remove from parser/help, close issue
   - Document decision in RFC

2. **#5 Release Assignment RFC:**
   - Define window semantics: Does PR belong to `release[i]` if `release[i].published_at <= merged_at < release[i-1].published_at`?
   - Write test cases for same-day merges, multi-release scenarios
   - This choice affects pagination (#7) and `--since-tag` (#8) filtering

**P1 Sprint (after #8 decision):**
1. If #8 rejected: implement #5 timestamp comparison immediately
2. If #8 accepted: add date-range filtering to #5 implementation
3. Validate correctness semantics with Hockney extended test suite

## Blocked By

None (RFC and decision in parallel with Fenster P0).

## Unblocks

- Fenster (#5 implementation) — awaits RFC definition
- Hockney (test suite validation) — needs finalized semantics
